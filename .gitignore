# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Environments
.pixi/
.venv/
*.lock
!pixi.lock

# IDE
.vscode/
.idea/
.DS_Store

# Testing / tooling caches
.coverage
coverage.xml
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ML artifacts — weights live on HF Hub, never in git. See docs/ and registry/.
*.pt
*.pth
*.ckpt
*.onnx
*.engine
*.plan
*.safetensors
.cache/
wandb/
mlruns/
lightning_logs/

# Data — external, CC BY 4.0, fetched via manifest. Never committed.
# Anchored to repo root so it doesn't shadow src/object_detection_eval/data/
# or tests/data/ (the harness's data-tier package and its tests).
/data/
*.hdf5
*.h5

# Local run output
outputs/
multirun/
site/
# MkDocs staging tree, assembled by scripts/stage_docs.py from README + reports + docs
site_src/
.env
