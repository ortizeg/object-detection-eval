---
id: 260801-e01
slug: vlm-prompt-parity-and-new-models
date: 2026-08-01
status: in-progress
mode: quick (inline, no subagents — user directive)
branch: gsd/vlm-prompt-parity-and-new-models
base: gsd/report-corrections-2026-07-30 (PR #14) — stacked, NOT main
---

# Plan — 260801-e01: VLM prompt parity, label accuracy, and new models

Chunk (e). Continues 260731-d01, which fixed the Grounding-DINO label collapse
and the Florence-2 task token but could not re-run them (no GPU). The harness is
correct; the committed numbers are not.

## Decisions taken with the user (2026-08-01)

1. **Compute substrate: vast.ai CUDA for published numbers.** Local MPS was
   measured and works for all four open-weights models (~13 min/sweep, free),
   but the existing targets were set on CUDA and the user chose to keep CUDA as
   the single published substrate. **MPS is used for the exploratory prompt
   search only** (relative comparisons across dozens of cheap sweeps); every
   number that reaches the report or a manifest target comes from the CUDA run.
2. **API-tier models: build and run, segregated.** DINO-X Pro and Grounding DINO
   1.5/1.6 Pro get inferencers and a table that is explicitly walled off from the
   reproducible open-weights rows. Blocked on a user-supplied DeepDataSpace
   token; spend confirmed before any billed call.
3. **YOLO-World: add.** Licence corrected — it is **GPL-3.0**, not the Apache-2.0
   the report currently publishes.
4. **YOLOE: do not add.** Verified **AGPL-3.0**, built on ultralytics. The user's
   criterion was "add it if permissive"; it is not. Record the verified licence
   in place of last session's guess.

## Tasks

- [ ] T1 — Correct the two published licence claims (YOLO-World Apache-2.0 →
      GPL-3.0; YOLOE "likely inherits AGPL" → verified AGPL-3.0).
- [ ] T2 — Equalise OmDet-Turbo's prompt vocabulary (the known fairness gap: it
      still runs the generic COCO list).
- [ ] T3 — Build a prompt-search harness that scores candidate vocabularies per
      model on MPS without touching committed results, so prompt choices are
      measured rather than asserted.
- [ ] T4 — Run the search: contrastive `referee`/`player` phrasing, small-object
      phrasing for `ball`/`rim`, per-model vocabulary. Cap effort equally per
      model and record what each received.
- [ ] T5 — Add the YOLO-World inferencer + manifest row + tests.
- [ ] T6 — Add DINO-X Pro and Grounding DINO 1.5/1.6 Pro inferencers + tests,
      offline-testable, gated behind a key.
- [ ] T7 — Final authoritative CUDA sweep on vast.ai; restore real
      `expected_map5095` for every row. **Blocked on vast.ai credentials.**
- [ ] T8 — Regenerate report tables, rewrite the stale-numbers caveat, add the
      segregated API-tier section, keep the honest per-class failure analysis.
- [ ] T9 — Verify: pytest (default env), ruff, mypy, both report drift gates.
- [ ] T10 — SUMMARY.md, STATE.md, stacked PR against
      `gsd/report-corrections-2026-07-30`.

## Verification

- `pytest tests/` in `-e default` — 314+ passed
- `ruff check .` and `mypy src/` clean
- `scripts/generate_report.py --report vlm_vs_finetuned --check` exit 0
- `scripts/generate_report.py --report final_comparison_640 --check` exit 0

## Known non-goals

- Latency for the new models. Timing on a shared vast.ai box is invalid
  (260730-a01: contention made a 6.6 ms model read 43 ms and inverted the
  ranking). Accuracy only unless a dedicated instance is provisioned.
- Re-running the fine-tuned detectors. Untouched by this chunk.
