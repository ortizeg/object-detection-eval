---
phase: 01-provenance-rescue-public-repo
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [INFRA-01]

must_haves:
  truths:
    - "github.com/ortizeg/object-detection-eval exists and is PUBLIC (INFRA-01)"
    - "The first public push includes the docs/provenance/ material committed in Plan 01 (INFRA-01 + SAFE-01)"
    - "main is protected: required status checks `lint` and `test`, squash-merge only, delete-branch-on-merge, force-push disabled (INFRA-01)"
    - "A direct push to main is rejected once protection is applied (INFRA-01)"
  artifacts:
    - "GitHub repo ortizeg/object-detection-eval (public)"
    - "Branch protection rule on main (gh api repos/.../branches/main/protection)"
  key_links:
    - "Provenance MUST be committed locally (Plan 01) and pushed BEFORE protection is applied — protecting first deadlocks the provenance push to main"
    - "Required check contexts MUST equal the CI job ids exactly (`lint` from ci.yml, `test` from test.yml) or main can never go green and all merges block"
---

<objective>
Create the PUBLIC GitHub repo and lock down `main`. This runs in Wave 2, AFTER Plan 01, because the
first push must carry the rescued provenance and because protecting `main` before the provenance commit
is pushed would block that push. The user has explicitly authorized creating the public repo (locked
decision) — this plan executes it, with a single pre-push safety gate on the one-way public exposure.

Purpose: INFRA-01 (protected public remote).
Output: the public repo + a branch-protection rule on main requiring `lint` and `test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/FORK_PLAN.md
@.github/workflows/ci.yml
@.github/workflows/test.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Pre-push safety review before public exposure (one-way gate)</name>
  <what-built>
    Plan 01 committed docs/provenance/ (2 docs + 6 configs + gcs-manifest.md) to local `main`. The next
    action creates a PUBLIC repo and pushes the ENTIRE local history — an irreversible exposure.
  </what-built>
  <how-to-verify>
    Confirm the payload about to go public is clean text only, no binaries, no secrets:
    - `git -C . ls-files | grep -E '\.(pth|onnx|ckpt|engine)$'` returns NOTHING (no weights tracked).
    - `git -C . log --oneline -5` shows the Plan 01 provenance + gcs-manifest commits on `main`.
    - Spot-check `git ls-files docs/provenance/` and confirm training-runs.md carries no live credential
      (vast.ai hostnames and the private `gs://` bucket path are expected and acceptable).
    - Confirm the target repo name and owner: `ortizeg/object-detection-eval`, visibility PUBLIC.
  </how-to-verify>
  <resume-signal>Type "approved" to create the public repo and push, or describe what to redact first.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create the public repo and push all history (INFRA-01)</name>
  <files></files>
  <read_first>
    THIS repo is a local git repo on branch `main` with commits and NO remote yet (`git remote -v` empty).
    `gh` is authenticated as `ortizeg`. CI workflows exist: `.github/workflows/ci.yml` (job id `lint`) and
    `.github/workflows/test.yml` (job id `test`) — both trigger on push and pull_request.
  </read_first>
  <action>
    From THIS repo root, create the public remote and push in one step:
    `gh repo create ortizeg/object-detection-eval --public --source=. --remote=origin --push`
    (Apache-2.0 LICENSE already committed in the repo, so no --license flag needed.) This publishes the
    full local history including the Plan 01 provenance, and the push to `main` triggers the `lint` and
    `test` workflows so GitHub registers those check contexts. Confirm the remote with `git remote -v` and
    that `origin/main` exists via `git ls-remote --heads origin main`.
  </action>
  <verify>
    <automated>gh repo view ortizeg/object-detection-eval --json visibility,isPrivate --jq '.visibility=="PUBLIC" and .isPrivate==false' | grep -q true && git ls-remote --heads origin main | grep -q refs/heads/main</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view ortizeg/object-detection-eval` reports visibility PUBLIC (isPrivate false)
    - `origin` remote is set and `origin/main` exists on GitHub
    - The pushed tree contains docs/provenance/ (verify `gh api repos/ortizeg/object-detection-eval/contents/docs/provenance` returns entries)
  </acceptance_criteria>
  <done>INFRA-01: github.com/ortizeg/object-detection-eval is PUBLIC, origin/main exists and carries the Plan 01 docs/provenance/ material.</done>
  <reversibility rating="costly">Public repo creation is a locked, user-authorized decision; deletable but the pushed history exposure is costly to fully unwind — the preceding human-verify gate confirms the payload is clean text before exposure.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Protect main (required lint+test, squash-only, delete-on-merge, no force-push) and prove a direct push is rejected (INFRA-01)</name>
  <files></files>
  <read_first>
    Required status check contexts MUST be the exact CI job ids: `lint` (ci.yml) and `test` (test.yml).
    On a brand-new repo the checks may not have completed their first run yet; the GitHub protection API
    accepts context strings that have not run — set them anyway (they gate future PRs).
    Reference the github-repo-setup skill's `gh api` protection pattern.
  </read_first>
  <action>
    (a) Apply branch protection via `gh api -X PUT repos/ortizeg/object-detection-eval/branches/main/protection`
    with: `required_status_checks` strict=true and contexts `["lint","test"]`; `enforce_admins=true`;
    `required_pull_request_reviews=null` (solo maintainer — see threat T-01-03); `restrictions=null`;
    `allow_force_pushes=false`; `allow_deletions=false`. Pass the JSON body via `--input` from a temp file
    (the protection endpoint needs a full body). Then set repo merge settings so squash is the only merge
    method and branches auto-delete: `gh api -X PATCH repos/ortizeg/object-detection-eval` with
    `allow_squash_merge=true`, `allow_merge_commit=false`, `allow_rebase_merge=false`, `delete_branch_on_merge=true`.
    (b) Prove enforcement: create a throwaway empty commit on a temp local branch, attempt to push it
    straight to `main` (`git push origin HEAD:main`), CONFIRM the push is REJECTED (non-zero exit /
    "protected branch" message), then discard the throwaway commit. Do not leave the temp commit behind.
  </action>
  <verify>
    <automated>gh api repos/ortizeg/object-detection-eval/branches/main/protection --jq '.required_status_checks.contexts' | grep -q lint && gh api repos/ortizeg/object-detection-eval/branches/main/protection --jq '.required_status_checks.contexts' | grep -q test && gh api repos/ortizeg/object-detection-eval --jq '.allow_squash_merge==true and .allow_merge_commit==false and .delete_branch_on_merge==true' | grep -q true</automated>
  </verify>
  <acceptance_criteria>
    - `gh api .../branches/main/protection` shows required contexts `lint` and `test`, enforce_admins true, force-push disabled
    - `gh api repos/ortizeg/object-detection-eval` shows allow_squash_merge true, allow_merge_commit false, allow_rebase_merge false, delete_branch_on_merge true
    - A deliberate direct `git push origin HEAD:main` is rejected as a protected-branch violation, and the throwaway commit is discarded
  </acceptance_criteria>
  <done>INFRA-01: main protection requires the `lint` and `test` checks, squash-only merges, delete-branch-on-merge, force-push disabled; a direct push to main is rejected.</done>
  <reversibility rating="reversible">Protection settings are editable via the same API.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → public GitHub | Full history + provenance becomes world-readable |
| contributor → main | Untrusted or accidental writes to the protected default branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | git history pushed to a public repo | high | mitigate | Pre-push human-verify checkpoint; .gitignore blocks `*.onnx/*.pth/*.ckpt`; 2048 KB large-file hook; verify `git ls-files` has no weights before push. |
| T-01-02 | Tampering | Direct / force push to main on a public repo | high | mitigate | Branch protection: required `lint`+`test` checks, enforce_admins, force-push + deletion disabled; proven by a rejected direct push. |
| T-01-03 | Elevation of Privilege | Merge without review | medium | accept | Solo maintainer — required PR reviews omitted deliberately; status checks + squash-only are the integrity gate. |
</threat_model>

<verification>
- Repo is PUBLIC with origin/main present and carrying docs/provenance/.
- Protection JSON shows required `lint`+`test`, squash-only, delete-on-merge, no force-push; a direct push to main is rejected.
</verification>

<success_criteria>
Roadmap SC #5 (public repo, main protected with lint+test required, squash merge, delete-on-merge,
direct push to main rejected) is met.
</success_criteria>

## Artifacts this plan produces
- Public GitHub repo `github.com/ortizeg/object-detection-eval` with all Phase-1 history + provenance
- Branch-protection rule on `main` (required `lint`+`test`, squash-only, delete-on-merge, force-push off)

<output>
Create `.planning/phases/01-provenance-rescue-public-repo/01-03-SUMMARY.md` when done
</output>
