---
phase: 01-provenance-rescue-public-repo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/.gitignore"
autonomous: true
requirements: [SAFE-02, SAFE-03]

must_haves:
  truths:
    - "A `git add -A` in the SOURCE repo stages no file under eval_output/ prediction dirs, inference_output/, or the mypy/pytest/ruff caches (SAFE-02)"
    - "EVAL_REPORT.md, EVAL_REPORT_FINAL.md, and summary.csv stay tracked in the source repo (SAFE-02)"
    - "The source repo eval_output/ tree is under ~20 MB after compact re-dump + gzip, down from 113 MB (SAFE-03)"
    - "Each gzipped prediction JSON round-trips (gzip.open + json.load) to data byte-for-byte identical to the pre-compression object (SAFE-03)"
  artifacts:
    - "SOURCE repo .gitignore (new ignore entries, committed)"
    - "eval_output/**/predictions_*.json.gz (compact + gzipped, local)"
  key_links:
    - "gitignore patterns must be targeted (ignore the prediction DIRS, not all of eval_output/) or the 3 tracked report files get untracked"
    - "round-trip verification MUST pass before any uncompressed original is deleted — never a blind delete"
---

<objective>
Make the SOURCE repo (`object-detection-training`) safe to `git add` before it is archived, and shrink
its 113 MB eval_output/ tree to under ~20 MB without losing a single prediction. This is entirely
source-repo hygiene and is independent of the rescue/mirror work — it runs in parallel (Wave 1).

Purpose: SAFE-02 (nothing raw-committable) + SAFE-03 (compressed, integrity-preserved results).
Output: an updated + committed source-repo .gitignore; compact-gzipped prediction JSONs (local).

Decision (planner): the .gitignore change IS committed to the source repo (durable protection is the
whole point of SAFE-02). The compressed eval_output/ files are NOT committed — they fall under the
now-ignored prediction dirs and live locally + in GCS; the GCS mirror (Plan 01) is their second copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/FORK_PLAN.md
</context>

<precondition>
The SOURCE repo `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training` is on branch
`feat/dinox-phase-7-ablation-configs` and has a pre-existing uncommitted change to eval_output/EVAL_REPORT.md;
stage ONLY .gitignore for the SAFE-02 commit so that unrelated change is not swept in.
</precondition>

<tasks>

<task type="auto">
  <name>Task 1: Add targeted ignores to the source repo .gitignore, keeping the 3 reports tracked (SAFE-02)</name>
  <files>/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/.gitignore</files>
  <read_first>
    Source repo root: `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/`.
    Currently NOT ignored (112 MB untracked-and-un-ignored): `eval_output/coco_reference/`,
    `eval_output/official_2026-07-13/`, `inference_output/`, `.mypy_cache/`, `.pytest_cache/`,
    `.ruff_cache/`, `checkpoints/`, `weights/`.
    Currently TRACKED inside eval_output/ (must STAY tracked): `eval_output/EVAL_REPORT.md`,
    `eval_output/EVAL_REPORT_FINAL.md`, `eval_output/summary.csv` (`git -C <src> ls-files eval_output/`).
  </read_first>
  <action>
    Append a "Throwaway eval/inference artifacts" block to the source repo `.gitignore`. Use targeted
    directory patterns so the 3 tracked report files are unaffected: ignore `eval_output/coco_reference/`,
    `eval_output/official_2026-07-13/`, `inference_output/`, `.mypy_cache/`, `.pytest_cache/`, `.ruff_cache/`,
    `checkpoints/`, and `weights/`. Do NOT add a bare `eval_output/` line — that would untrack the reports.
    Then commit ONLY `.gitignore` in the source repo, honoring its pre-commit + ruff-version conventions:
    from the source repo directory run the pixi-wrapped commit `/Users/ortizeg/.pixi/bin/pixi run git commit`
    after `git add .gitignore` (do not `git add -A`; a stray EVAL_REPORT.md change is present and out of scope).
    If the pre-commit hook modifies files on the first attempt, re-stage `.gitignore` and make a NEW commit.
  </action>
  <verify>
    <automated>SRC="/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training"; git -C "$SRC" check-ignore -q eval_output/coco_reference && git -C "$SRC" check-ignore -q eval_output/official_2026-07-13 && git -C "$SRC" ls-files eval_output/ | grep -q 'EVAL_REPORT_FINAL.md' && git -C "$SRC" ls-files eval_output/ | grep -q 'summary.csv' && ! git -C "$SRC" status --porcelain | grep -qE 'eval_output/(coco_reference|official_2026-07-13)/'</automated>
  </verify>
  <acceptance_criteria>
    - `git -C <src> check-ignore` returns each newly-ignored path (exit 0)
    - `git -C <src> ls-files eval_output/` still lists EVAL_REPORT.md, EVAL_REPORT_FINAL.md, summary.csv
    - `git -C <src> status --porcelain` no longer shows the two prediction dirs as untracked
    - The .gitignore change is committed on branch `feat/dinox-phase-7-ablation-configs`
  </acceptance_criteria>
  <done>SAFE-02: source repo .gitignore ignores the two prediction dirs + inference_output/ + tool caches + checkpoints/weights (committed); the 3 report/csv files stay tracked; `git add .` stages no eval artifact.</done>
  <reversibility rating="reversible">Editing .gitignore + a small commit is trivially revertible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Compact re-dump + gzip the prediction JSONs with round-trip integrity, target under ~20 MB (SAFE-03)</name>
  <files>/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/</files>
  <read_first>
    Largest offenders (2-space pretty-printed COCO predictions):
    - `eval_output/coco_reference/predictions_yolox_val.json` (~47.7 MB) and `predictions_yolox_test.json` (~47.7 MB) — 91 MB of the 113 MB
    - `eval_output/official_2026-07-13/**/predictions_*.json` (many 0.4–3 MB files, ~21 MB total)
    All are gitignored after Task 1, so this mutation touches no tracked file.
  </read_first>
  <action>
    Write a one-off Python step (run via the source repo's pixi env) that, for every
    `eval_output/**/predictions_*.json`: loads the object, re-dumps it COMPACT with
    `json.dump(obj, f, separators=(",", ":"))`, gzips to a sibling `<name>.json.gz`, then REOPENS the
    `.gz` with `gzip.open` + `json.load` and asserts the reloaded object equals the originally-loaded
    object (deep equality). Only after that assertion passes for a file may its uncompressed original be
    removed. Never delete before the round-trip assert (T-01-07). Print a per-file before/after size line
    and a final `du -sh eval_output/` total. Do NOT touch EVAL_REPORT*.md or summary.csv.
  </action>
  <verify>
    <automated>SRC="/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training"; python3 -c "import gzip,json,glob,sys; fs=glob.glob('$SRC/eval_output/**/predictions_*.json.gz',recursive=True); assert fs, 'no .gz produced'; [json.load(gzip.open(f)) for f in fs]; print('roundtrip OK', len(fs))" && [ $(du -sm "$SRC/eval_output" | cut -f1) -lt 20 ]</automated>
  </verify>
  <acceptance_criteria>
    - At least one `predictions_*.json.gz` exists and every `.gz` reloads via gzip.open + json.load (no exception)
    - The round-trip equality assertion passed for every compressed file (reported by the step)
    - `du -sm eval_output/` reports < 20 (MB)
    - EVAL_REPORT.md, EVAL_REPORT_FINAL.md, summary.csv are unchanged
  </acceptance_criteria>
  <done>SAFE-03: every prediction JSON is compact-gzipped with a passing round-trip equality check; eval_output/ is under ~20 MB; no original deleted before its round-trip passed.</done>
  <reversibility rating="costly">Deleting the uncompressed originals is space-reclaiming and gated by a verified round-trip; the .gz + GCS mirror are the durable copies.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index | An accidental `git add .` could stage 112 MB of raw eval blobs into a soon-public history |
| original JSON → compressed JSON | Compression must preserve every prediction value exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | Source repo git index | high | mitigate | Targeted .gitignore entries + committed; verify via `git check-ignore` and a clean `git status`. |
| T-01-07 | Denial of Service (data loss) | Uncompressed prediction JSONs | medium | mitigate | gzip round-trip deep-equality assert gates deletion of every original; no blind delete. |
| T-01-09 | Tampering | The 3 tracked report/csv files | medium | mitigate | Stage only .gitignore; compression step excludes *.md/*.csv; verify the 3 remain tracked + unchanged. |
</threat_model>

<verification>
- SAFE-02: check-ignore hits the new paths; the 3 reports remain tracked; prediction dirs no longer untracked.
- SAFE-03: every `.gz` reloads; round-trip equality held; eval_output/ under 20 MB.
</verification>

<success_criteria>
Roadmap SC #2 (no raw eval artifact committable via `git add .`) and SC #4 (results tree under ~20 MB,
compressed predictions still load to the same data) are met.
</success_criteria>

## Artifacts this plan produces
- SOURCE repo `.gitignore` — new targeted ignore block, committed on `feat/dinox-phase-7-ablation-configs`
- `eval_output/**/predictions_*.json.gz` — compact + gzipped predictions (local; second copy in GCS)

<output>
Create `.planning/phases/01-provenance-rescue-public-repo/01-02-SUMMARY.md` when done
</output>
