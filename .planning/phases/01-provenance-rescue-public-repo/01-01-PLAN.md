---
phase: 01-provenance-rescue-public-repo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/provenance/training-runs.md
  - docs/provenance/artifact-tracker.md
  - docs/provenance/configs/damo_m/damoyolo_basketball_m.py
  - docs/provenance/configs/deim_m/deim_basketball_m.yml
  - docs/provenance/configs/rfdetr_m/train_rfdetr.py
  - docs/provenance/configs/rtdetrv2_m/rtdetrv2_basketball_m.yml
  - docs/provenance/configs/rtmdet_m/rtmdet_basketball.py
  - docs/provenance/configs/rtmdet_m_rewarmup/rtmdet_basketball_rewarmup.py
  - docs/provenance/gcs-manifest.md
autonomous: true
requirements: [SAFE-01, SAFE-04]

must_haves:
  truths:
    - "docs/provenance/training-runs.md and artifact-tracker.md exist in THIS repo and are readable without the source repo (SAFE-01)"
    - "The six per-framework training configs (DAMO, DEIM, RF-DETR, RT-DETRv2, RTMDet, RTMDet-rewarmup) are committed under docs/provenance/configs/ (SAFE-01)"
    - "Every eval-target ONNX and label map named in artifact-tracker.md resolves to a gs:// object under .../eval/, recorded in docs/provenance/gcs-manifest.md (SAFE-04)"
  artifacts:
    - docs/provenance/training-runs.md
    - docs/provenance/artifact-tracker.md
    - docs/provenance/configs/*/
    - docs/provenance/gcs-manifest.md
  key_links:
    - "gcs-manifest.md cross-checks each artifact-tracker.md entry against a real gsutil ls hit — breakage = a named artifact with no gs:// object"
    - "The six rescued configs are the ONLY surviving record once .deploy_comparison/ is gone — breakage = an incomplete copy loses model provenance permanently"
---

<objective>
Rescue the irreplaceable, laptop-only training provenance for all 7 evaluated models out of the
source repo's gitignored `.deploy_comparison/` and into version-controlled `docs/provenance/` in
THIS repo, then prove every eval-target ONNX and label map exists in the private GCS mirror.

This is the time-sensitive core of the phase (per D/roadmap: "irreplaceable provenance ... currently
exists only on one laptop, outside any git history"). Task 1 completes the rescue and MUST NOT depend
on anything else; Task 2 verifies the second copy in GCS against the rescued tracker.

Purpose: SAFE-01 (provenance in git) + SAFE-04 (every artifact in two places).
Output: docs/provenance/{training-runs.md, artifact-tracker.md, configs/, gcs-manifest.md}, committed
here; plus any missing official-eval ONNX/label maps pushed to GCS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/FORK_PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rescue provenance docs + configs into docs/provenance/ and commit (SAFE-01)</name>
  <files>docs/provenance/training-runs.md, docs/provenance/artifact-tracker.md, docs/provenance/configs/damo_m/damoyolo_basketball_m.py, docs/provenance/configs/deim_m/deim_basketball_m.yml, docs/provenance/configs/rfdetr_m/train_rfdetr.py, docs/provenance/configs/rtdetrv2_m/rtdetrv2_basketball_m.yml, docs/provenance/configs/rtmdet_m/rtmdet_basketball.py, docs/provenance/configs/rtmdet_m_rewarmup/rtmdet_basketball_rewarmup.py</files>
  <read_first>
    Source (gitignored, laptop-only) tree: `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/.deploy_comparison/`
    - `INSTANCES.md` (vast.ai instance IDs, SSH prefixes, per-framework train commands)
    - `RESULTS.md` (per-model harness numbers + GCS push status = the artifact tracker)
    - `artifacts/<framework>/` per-framework config files
    THIS repo `.gitignore` already blocks `*.onnx *.pth *.ckpt` and pre-commit caps files at 2048 KB.
  </read_first>
  <action>
    Create `docs/provenance/` and copy TEXT provenance only (never `.pth/.onnx/.ckpt`):
    (a) `cp` source `.deploy_comparison/INSTANCES.md` to `docs/provenance/training-runs.md` and
    `.deploy_comparison/RESULTS.md` to `docs/provenance/artifact-tracker.md`.
    (b) Create `docs/provenance/configs/<framework>/` for each of the six frameworks and `cp` exactly
    these six config files, preserving the framework subdir names: `artifacts/damo_m/damoyolo_basketball_m.py`,
    `artifacts/deim_m/deim_basketball_m.yml`, `artifacts/rfdetr_m/train_rfdetr.py`,
    `artifacts/rtdetrv2_m/rtdetrv2_basketball_m.yml`, `artifacts/rtmdet_m/rtmdet_basketball.py`,
    `artifacts/rtmdet_m_rewarmup/rtmdet_basketball_rewarmup.py`.
    (c) SECURITY (T-01-04): before committing, scan the two `.md` files for live credentials — grep them
    for `AKIA`, `ssh-rsa`, `PRIVATE KEY`, `token`, `api_key`, `password`. The vast.ai host names and the
    `gs://deep-ego-model-training/...` bucket path are NOT secrets (instances destroyed; bucket is private
    and path alone grants no access) and stay. If any real credential is found, redact it before commit.
    (d) Commit in THIS repo with a normal `git commit` (pre-commit hooks installed). The gitignore +
    2048 KB large-file hook are the backstop — all copied files are small text, so the commit passes.
    Do NOT copy any weights, checkpoints, ONNX, or the basketball-data tarball.
  </action>
  <verify>
    <automated>test -f "docs/provenance/training-runs.md" && test -f "docs/provenance/artifact-tracker.md" && test $(ls docs/provenance/configs/*/ | grep -Ec '\.(py|yml)$') -eq 6 && git ls-files docs/provenance/ | grep -qE 'training-runs\.md' && ! git ls-files docs/provenance/ | grep -qE '\.(onnx|pth|ckpt)$'</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files docs/provenance/` lists training-runs.md, artifact-tracker.md, and the 6 config files
    - No `*.onnx/*.pth/*.ckpt` under docs/provenance/ (grep returns empty)
    - Credential grep over the two docs returns no live secret (host names / bucket path allowed)
    - The provenance commit exists on the local `main` branch (git log shows it)
  </acceptance_criteria>
  <done>SAFE-01: docs/provenance/ holds training-runs.md, artifact-tracker.md, and the 6 framework configs, committed on main with no binaries tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory GCS, mirror the 3 missing official-eval ONNX, write gcs-manifest.md (SAFE-04)</name>
  <files>docs/provenance/gcs-manifest.md</files>
  <read_first>
    Bucket source of truth: `gs://deep-ego-model-training/ego-training-data/basketball-data/eval/`.
    Verified already mirrored: all 6 models under `eval/final-comparison-640/{damo_m,deim_m,rfdetr_m,rtdetrv2_m,rtmdet_m,rtmdet_m_rewarmup}/`
    (1.89 GiB), and YOLO26m/s ONNX+labels under `eval/onnx-export/{yolo26m,yolo26s}/`.
    Local official-eval ONNX NOT yet found anywhere under eval/ (the gaps to upload):
    - `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/YOLOX/training_results/basketball_m/yolox_m_basketball_800.onnx` + that dir's `labels_mapping.json`
    - `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/YOLOX/training_results/basketball_s800/yolox_s_basketball_800.onnx` + `labels_mapping.json`
    - `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/YOLOX/training_results/rfdetr_v2/rfdetr_small_basketball_v2.onnx` + `labels_mapping.json`
    docs/provenance/artifact-tracker.md (from Task 1) is the list to cross-check against. `gsutil`/`gcloud` are authenticated.
  </read_first>
  <action>
    (a) Inventory: run `gsutil ls -r "gs://deep-ego-model-training/ego-training-data/basketball-data/eval/**"`
    and capture it. For each ONNX + label map named in artifact-tracker.md (the 6 final-comparison models,
    YOLO26m/s, and the 3 official-eval reuse models above), confirm whether it already resolves to a gs:// object.
    (b) Upload only the genuinely-missing artifacts (expected: the 3 YOLOX-M-800 / YOLOX-S-800 / RF-DETR-S ONNX
    + their label maps) with `gsutil cp -n` (T-01-05: `-n` = no-clobber, so the verified 1.89 GiB is never
    overwritten) into a new clearly-named prefix `eval/official-eval-inputs/<model>/` — one subdir per model.
    After each upload, `gsutil ls` the destination to confirm the object exists.
    (c) Write `docs/provenance/gcs-manifest.md`: a table with one row per named artifact — artifact name,
    local path (if any), and its resolved `gs://` URI — so every entry in artifact-tracker.md maps to a
    verified bucket object. Note the two acceptable "no .pth on GCS" cases already recorded in the tracker
    (rtdetrv2 best.pth not pulled; box uplink failed — reproducible from cfg). Commit the manifest in THIS repo.
  </action>
  <verify>
    <automated>test -f docs/provenance/gcs-manifest.md && grep -q 'gs://' docs/provenance/gcs-manifest.md && gsutil ls "gs://deep-ego-model-training/ego-training-data/basketball-data/eval/official-eval-inputs/**" 2>/dev/null | grep -Eq 'yolox_m_basketball_800\.onnx' && gsutil ls "gs://deep-ego-model-training/ego-training-data/basketball-data/eval/official-eval-inputs/**" 2>/dev/null | grep -Eq 'rfdetr_small_basketball_v2\.onnx'</automated>
  </verify>
  <acceptance_criteria>
    - Every ONNX + label map named in artifact-tracker.md has a `gs://` URI row in gcs-manifest.md
    - `gsutil ls` confirms each URI in the manifest resolves to a real object (exit 0)
    - The 3 previously-missing official-eval ONNX now exist under `eval/official-eval-inputs/`
    - No existing GCS object was overwritten (cp used `-n`)
    - gcs-manifest.md is committed on local `main`
  </acceptance_criteria>
  <done>SAFE-04: every ONNX + label map named in artifact-tracker.md resolves to a verified gs:// URI in gcs-manifest.md (committed); the 3 missing official-eval ONNX are uploaded with cp -n.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop → git (soon public) | Provenance text moves from a private laptop dir into a repo that Plan 03 pushes public |
| laptop → GCS | Local artifacts uploaded to a private bucket over authenticated gsutil |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | training-runs.md / artifact-tracker.md content | medium | mitigate | Grep the two docs for live credentials (AKIA/ssh-rsa/PRIVATE KEY/token/api_key/password) before commit; redact if found. vast.ai hostnames (destroyed) and the private bucket path are not secrets. |
| T-01-05 | Tampering | Existing 1.89 GiB of mirrored GCS objects | low | mitigate | Upload with `gsutil cp -n` (no-clobber); only missing artifacts are written. |
| T-01-08 | Tampering | Accidental commit of a weight/ONNX into docs/provenance/ | medium | mitigate | .gitignore blocks `*.onnx/*.pth/*.ckpt`; 2048 KB large-file hook; verify grep confirms none tracked. |
</threat_model>

<verification>
- SAFE-01: `git ls-files docs/provenance/` shows the 2 docs + 6 configs; no binaries.
- SAFE-04: every artifact-tracker.md entry has a verified `gs://` URI in gcs-manifest.md.
</verification>

<success_criteria>
Roadmap SC #1 (provenance in git, readable without source repo) and SC #3 (every ONNX + label map
resolves under gs://.../eval/) are met and committed on local `main`.
</success_criteria>

## Artifacts this plan produces
- `docs/provenance/training-runs.md` (from `.deploy_comparison/INSTANCES.md`)
- `docs/provenance/artifact-tracker.md` (from `.deploy_comparison/RESULTS.md`)
- `docs/provenance/configs/<framework>/<config>` — 6 text training configs
- `docs/provenance/gcs-manifest.md` — artifact → gs:// URI cross-check
- GCS objects under `eval/official-eval-inputs/` — the 3 previously laptop-only ONNX + label maps

<output>
Create `.planning/phases/01-provenance-rescue-public-repo/01-01-SUMMARY.md` when done
</output>
