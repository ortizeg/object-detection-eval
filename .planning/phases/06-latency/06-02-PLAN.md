---
phase: 06-latency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pixi.toml
  - pyproject.toml
  - .github/workflows/test.yml
  - scripts/graft_efficientnms.py
  - tests/scripts/test_graft_efficientnms.py
autonomous: true
requirements: [LAT-03]
user_setup: []

must_haves:
  truths:
    - "scripts/graft_efficientnms.py grafts a single EfficientNMS_TRT node onto ONLY the 3 dense-head models (YOLOX-M, DAMO-YOLO-M, RTMDet-M), and for RTMDet strips the mmdeploy pre-NMS TopK node first (TRT's hard K>3840 limit); the other 4 (YOLO26m, RF-DETR-M, DEIM-M, RT-DETRv2-M) are never grafted (LAT-03)"
    - "each grafted model's box_coding / score_activation / iou_threshold graft attributes are taken from that model's own postprocessor decode convention in inference/postprocess.py, not guessed from the plugin docs (Pitfall 7) (LAT-03)"
    - "a new linux-64-only trt pixi environment provides onnxruntime-gpu + tensorrt WITHOUT the conflicting core CPU onnxruntime, and a cross-platform graphsurgeon environment provides onnx + onnx-graphsurgeon so the graft script develops and unit-tests on this CPU/macOS machine with no GPU (LAT-03, Pitfall 1)"
    - "pyproject [trt] pins onnx-graphsurgeon>=0.6.1 (avoiding the 0.5.8 float32_to_bfloat16 break) plus the other 3 packages at their verified versions; default CI stays green with the graphsurgeon-marked test deselected (LAT-03, Pitfall 2)"
  artifacts:
    - scripts/graft_efficientnms.py
    - tests/scripts/test_graft_efficientnms.py
    - pixi.toml
    - pyproject.toml
  key_links:
    - "the grafted *_nms.onnx are sibling artifact files (gitignored via *.onnx, regenerable) consumed only by build_trt_engines.py at engine-build time — never imported by the inference library (LAT-03)"
    - "the CPU test asserts structure (exactly one EfficientNMS_TRT node + its 4 outputs, RTMDet's TopK removed) on a synthetic graph; real plugin-schema + semantic validation happens at Plan 06-03's T4 engine build (LAT-03, Open Question 1)"
---

<objective>
Land LAT-03: the committed `onnx-graphsurgeon` graph-surgery script that grafts the `EfficientNMS_TRT` plugin op onto the raw dense head of the 3 models that still do NMS outside the graph (YOLOX-M, DAMO-YOLO-M, and RTMDet-M — RTMDet first has its mmdeploy pre-NMS `TopK` stripped, since `K>3840` exceeds TensorRT's hard limit), while leaving the 4 already-end-to-end models untouched (YOLO26m is NMS-free; RF-DETR-M / DEIM-M / RT-DETRv2-M decode fully in-graph). It also lands the environment plumbing this requires: a cross-platform `graphsurgeon` pixi environment (`onnx` + `onnx-graphsurgeon`) so the graft script is written and CPU-tested on this macOS machine with no GPU, and a linux-64-only `trt` pixi environment (`onnxruntime-gpu` + `tensorrt`) that excludes the conflicting core CPU `onnxruntime` for Plan 06-03's T4 build — plus pinning the four `[trt]` packages in `pyproject.toml`.

Purpose: graph surgery is pure offline ONNX manipulation — `onnx-graphsurgeon` never touches a GPU or runs the model — so LAT-03's code and unit tests are fully developable on CPU today, ahead of the rented T4. Getting the pixi environments right here (the `onnxruntime` vs `onnxruntime-gpu` conflict is a real footgun, Pitfall 1) is what makes Plan 06-03's box provisioning reproducible rather than ad-hoc. (LAT-03)
Output: `scripts/graft_efficientnms.py`, `tests/scripts/test_graft_efficientnms.py`, the `graphsurgeon` + `trt` pixi environments, the pinned `[trt]` extra, and the new `graphsurgeon` pytest marker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-latency/06-RESEARCH.md
@CLAUDE.md
@pixi.toml
@pyproject.toml
@.github/workflows/test.yml
@src/object_detection_eval/inference/postprocess.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: pixi graphsurgeon + trt environments, pinned [trt] extra, graphsurgeon marker</name>
  <read_first>
    - pixi.toml (in context): the existing feature/environment pattern — [feature.vlm.dependencies]/[feature.vlm.pypi-dependencies] and [environments] default=["dev"], vlm=["dev","vlm"]. The new features follow this shape.
    - pyproject.toml (in context): the [trt] optional-dependencies block (4 bare unpinned packages) to pin, and [tool.pytest.ini_options].markers to extend.
    - .github/workflows/test.yml (in context): line running `pixi run test-cov -m "not vlm and not trt and not external"` — the default CI selection to extend with `and not graphsurgeon`.
    - 06-RESEARCH.md Standard Stack table + Pitfall 1 + Pitfall 2 + Open Question 2 (the verified version pins, the onnxruntime/onnxruntime-gpu coexistence conflict, and the note to route exact pixi syntax through the pixi skill).
  </read_first>
  <files>pixi.toml, pyproject.toml, .github/workflows/test.yml</files>
  <action>Route the pixi environment-composition syntax through the `pixi` skill before editing (06-RESEARCH.md Open Question 2/Assumption A4 flag it as unverified this session). Make these changes:

1. pyproject.toml [trt] extra — pin the four packages to the 06-RESEARCH.md-verified versions: `onnxruntime-gpu>=1.28.0,<2`, `tensorrt>=10.16,<11`, `onnx>=1.22,<2`, and CRUCIALLY `onnx-graphsurgeon>=0.6.1` (NOT the bare name — 0.5.8 calls the removed `onnx.helper.float32_to_bfloat16` and breaks against onnx>=1.20; 0.6.1 released 2026-04-08 is the fix, Pitfall 2). Keep the existing `# TensorRT latency benchmarking. Linux-64 + NVIDIA only.` comment.

2. pyproject.toml [tool.pytest.ini_options].markers — add a `graphsurgeon` marker meaning "requires the onnx + onnx-graphsurgeon CPU graph tooling (the [trt] extra's graph-surgery subset), but NOT a CUDA GPU" (distinct from the existing `trt` marker which requires a GPU). This lets LAT-03's graph tests run on this Mac / any linux CI without a GPU.

3. pixi.toml — add a `graphsurgeon` feature with pypi-dependencies `onnx>=1.22,<2` and `onnx-graphsurgeon>=0.6.1` (both have osx-arm64 AND linux-64 wheels, so this feature is cross-platform and does NOT pull onnxruntime-gpu — no conflict with the core CPU onnxruntime). Add a linux-64-only `trt` feature (scope its platforms to `["linux-64"]`) with pypi-dependencies `onnxruntime-gpu`, `tensorrt`, plus `onnx` + `onnx-graphsurgeon` (so the T4 box has both the engine builder and the graph tooling). Compose the environments: add `graphsurgeon = ["dev", "graphsurgeon"]` (keeps core CPU onnxruntime — fine, no gpu package present) and a `trt` environment that EXCLUDES the core CPU `onnxruntime` so it cannot coexist with `onnxruntime-gpu` (Pitfall 1) — use pixi's `no-default-feature = true` on the trt environment plus an explicit re-listing of the non-onnxruntime core deps the T4 run needs (numpy, pydantic, loguru, pycocotools, opencv, pyyaml, supervision, orjson, the editable package, pytest/pytest-cov for the marked test), per whatever the pixi skill confirms is the correct current syntax. If `no-default-feature` cannot cleanly re-express the core minus onnxruntime, fall back to a documented manual step (a comment in pixi.toml: "on the T4, `pip uninstall onnxruntime` before installing the trt env") rather than shipping a silently-conflicting environment.

4. .github/workflows/test.yml — extend the default selection to `-m "not vlm and not trt and not external and not graphsurgeon"` so the graphsurgeon-marked graft test (which needs onnx-graphsurgeon, absent from the default env) is deselected in the torch-free CI job, mirroring how `vlm`/`trt` are deselected.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi install -e graphsurgeon && /Users/ortizeg/.pixi/bin/pixi run -e graphsurgeon python -c "import onnx, onnx_graphsurgeon; print('gs', onnx_graphsurgeon.__version__)" && grep -q "onnx-graphsurgeon>=0.6.1" pyproject.toml && grep -q "graphsurgeon:" pyproject.toml && grep -q "not graphsurgeon" .github/workflows/test.yml && echo OK</automated>
  </verify>
  <done>`pixi install -e graphsurgeon` resolves on this macOS machine and `import onnx_graphsurgeon` works (version >=0.6.1); the linux-64-only `trt` environment is declared and excludes core CPU onnxruntime (or carries the documented uninstall-first note); pyproject pins all four [trt] packages and registers the `graphsurgeon` marker; test.yml deselects `graphsurgeon` in default CI.</done>
</task>

<task type="auto">
  <name>Task 2: graft_efficientnms.py — EfficientNMS_TRT graft for the 3 dense-head models</name>
  <read_first>
    - src/object_detection_eval/inference/postprocess.py (in context): the ground-truth decode convention for each grafted model, which the graft attributes MUST match (Pitfall 7). YOLOXPostProcessor: score = obj_conf * class_conf (already activated → score_activation False), boxes decoded cxcywh→xyxy CORNER (box_coding 0), default nms_iou_threshold 0.45. DamoPostProcessor: per-class sigmoid scores (score_activation False), bboxes xyxy CORNER (box_coding 0), default nms_iou_threshold 0.7. RTMDetPostProcessor: the end2end export ALREADY runs NMS in-graph (dets [1,N,5] xyxy+score, labels [1,N]) — so RTMDet needs its pre-NMS TopK stripped to re-expose the raw dense head BEFORE grafting.
    - 06-RESEARCH.md Pattern 2 (the onnx_graphsurgeon import_onnx → locate box/score tensors → append EfficientNMS_TRT → export_onnx sketch) and Assumption A1 / Open Question 1 (the EXACT plugin attribute names + defaults are LOW-confidence, aggregated from web search — the script must be written so the T4 build validates them, not assert them as fact).
    - src/object_detection_eval/inference/detectors/{yolox,damo,rtmdet}.py (in context): confirm which onnx each detector loads and its raw output tensor names.
  </read_first>
  <files>scripts/graft_efficientnms.py</files>
  <action>Write `scripts/graft_efficientnms.py`: a CPU-only, no-GPU onnx-graphsurgeon tool that reads a raw ONNX and writes a sibling `*_nms.onnx` with a single `EfficientNMS_TRT` node grafted onto the dense head. Structure: a small frozen pydantic config per grafted model (model name, input onnx path, output onnx path, iou_threshold, score_threshold, box_coding, score_activation, max_output_boxes 300, background_class -1) whose iou/box-coding/score-activation values are set FROM each model's postprocessor convention (YOLOX iou 0.45 box_coding 0 score_activation False; DAMO iou 0.7 box_coding 0 score_activation False; RTMDet — after TopK strip — from its head). Functions: (a) `strip_pre_nms_topk(graph)` that locates and removes the mmdeploy pre-NMS TopK node (and any downstream in-graph NMS/gather chain) to re-expose RTMDet's raw box/score tensors, used ONLY for rtmdet; (b) `graft_efficient_nms(graph, cfg)` that locates the raw box tensor ([batch, N, 4], corner xyxy) and score tensor ([batch, N, num_classes]), appends one `gs.Node(op="EfficientNMS_TRT", attrs=..., inputs=[boxes, scores], outputs=[num_detections int32 [1,1], detection_boxes f32 [1,300,4], detection_scores f32 [1,300], detection_classes int32 [1,300]])`, sets graph.outputs to those 4, and cleanup().toposort(); (c) a main() with `--model {yolox,damo,rtmdet}`, `--in`, `--out` that dispatches. Guard the 4 non-grafted models explicitly: if invoked with any of yolo26/rfdetr/deim/rtdetrv2, exit non-zero with a message that those models are already end-to-end and MUST NOT be grafted (Anti-Pattern in 06-RESEARCH.md). Add a module docstring stating the EfficientNMS_TRT attribute schema is LOW-confidence (Open Question 1) and MUST be confirmed on the T4 via `tensorrt.get_plugin_registry()` / `trtexec --verbose` before the grafted engine's detections are trusted — the CPU path here guarantees graph STRUCTURE, not plugin SEMANTICS. Import only onnx + onnx_graphsurgeon + numpy + pydantic + loguru (no torch, no onnxruntime-gpu, no tensorrt — keeps it importable in the graphsurgeon env). Route all output through loguru (T20).</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e graphsurgeon python -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('g','scripts/graft_efficientnms.py'); m=importlib.util.module_from_spec(s); sys.modules['g']=m; s.loader.exec_module(m); print('import OK')" && /Users/ortizeg/.pixi/bin/pixi run lint</automated>
  </verify>
  <done>graft_efficientnms.py imports in the graphsurgeon env; it exposes strip_pre_nms_topk + graft_efficient_nms + per-model configs whose iou/box_coding/score_activation match the postprocessors; invoking it for any of the 4 end-to-end models exits non-zero; `pixi run lint` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CPU-only graph-surgery test on a synthetic ONNX (graphsurgeon-marked)</name>
  <read_first>
    - tests/scripts/test_run_benchmark.py (in context): the spec_from_file_location file-path load pattern (scripts/ is not a package).
    - 06-RESEARCH.md "Offline test pattern for the graft script" (build a tiny synthetic Conv→dense box/score graph with onnx-graphsurgeon itself, graft, then assert graph.outputs are exactly the 4 EfficientNMS_TRT outputs and exactly one op=="EfficientNMS_TRT" node — no CUDA, no trtexec, no real weights).
  </read_first>
  <files>tests/scripts/test_graft_efficientnms.py</files>
  <behavior>
    - Grafting a synthetic tiny graph (a dense [1,N,4] boxes tensor + [1,N,num_classes] scores tensor) yields a graph whose node list contains exactly one node with op=="EfficientNMS_TRT".
    - After grafting, graph.outputs are exactly the 4 tensors [num_detections, detection_boxes, detection_scores, detection_classes] with the documented dtypes/shapes.
    - strip_pre_nms_topk removes a synthetic pre-NMS TopK node: a graph built WITH a TopK node has zero TopK nodes after the strip, and the raw box/score tensors are re-exposed as graph tensors.
    - Invoking the graft for a non-grafted model (e.g. yolo26) raises / exits non-zero — the 4 end-to-end models are guarded.
  </behavior>
  <action>Write `tests/scripts/test_graft_efficientnms.py`, marked `@pytest.mark.graphsurgeon` (needs onnx-graphsurgeon but NOT a GPU), loading graft_efficientnms.py by file path. Build synthetic tiny graphs with onnx_graphsurgeon (gs.Variable/gs.Node) — a dense-head graph for the graft assertions and a TopK-bearing graph for the strip assertion — and assert the behaviors above purely on graph shape (one EfficientNMS_TRT node, the 4 outputs with correct dtype/shape, TopK removed, the non-grafted-model guard). No CUDA, no trtexec, no real model weights. Use `--no-cov` friendliness (the file is outside src/). This test runs via `pixi run -e graphsurgeon pytest -m graphsurgeon` and is deselected in default CI.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e graphsurgeon pytest tests/scripts/test_graft_efficientnms.py -m graphsurgeon --no-cov -q</automated>
  </verify>
  <done>The graph-surgery test passes CPU-only in the graphsurgeon env: it asserts exactly one EfficientNMS_TRT node, the 4 correctly-typed outputs, RTMDet's TopK removed, and the non-grafted-model guard — all on synthetic graphs, no GPU. Default CI (`not graphsurgeon`) stays green without it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw ONNX file → graft script | gitignored model artifacts read + transformed offline into sibling *_nms.onnx |
| [trt]/graphsurgeon package installs → environments | onnxruntime-gpu/tensorrt/onnx/onnx-graphsurgeon pulled from PyPI |
| grafted *_nms.onnx → T4 engine build (Plan 06-03) | the surgery output feeds trtexec; wrong graft attrs silently produce wrong boxes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | EfficientNMS_TRT box_coding/score_activation mismatch → silently wrong boxes (not an error) | high | mitigate | Graft attrs are set FROM each model's own postprocessor decode convention (Pitfall 7); the docstring mandates confirming the plugin schema on the T4 via tensorrt.get_plugin_registry before trusting detections; a grafted model whose detections don't align with the numpy path is a defect, not a number to keep |
| T-06-05 | Tampering | grafting EfficientNMS onto an already-end-to-end model (YOLO26/DETRs) → corrupted output semantics | medium | mitigate | The script hard-guards the 4 non-grafted models (exit non-zero); the test asserts the guard |
| T-06-SC | Tampering | supply chain of the four [trt] packages | high | mitigate | All four pinned to 06-RESEARCH.md-verified versions (onnx-graphsurgeon>=0.6.1 specifically avoids the 0.5.8 break); per the Package Legitimacy Audit all four are pre-existing official NVIDIA/Microsoft/Linux-Foundation packages already declared in pyproject [trt] — no legitimacy checkpoint warranted (not new adoptions) |
| T-06-06 | Tampering | onnxruntime (CPU) + onnxruntime-gpu coexisting in one env → broken/undefined CUDA EP | high | mitigate | The trt pixi environment excludes core CPU onnxruntime (no-default-feature + explicit re-listing) or carries a documented uninstall-first note; verified before the Plan 06-03 box run (Pitfall 1) |
</threat_model>

<artifacts>
Artifacts this plan produces:
- `scripts/graft_efficientnms.py` — LAT-03 CPU-only EfficientNMS_TRT graft + RTMDet TopK strip
- `tests/scripts/test_graft_efficientnms.py` — graphsurgeon-marked CPU-only graph-surgery test
- `pixi.toml` — new cross-platform `graphsurgeon` env + linux-64-only `trt` env (excludes core onnxruntime)
- `pyproject.toml` — pinned [trt] extra (onnx-graphsurgeon>=0.6.1 + 3 others) + `graphsurgeon` pytest marker
- `.github/workflows/test.yml` — default CI selection extended with `and not graphsurgeon`
- grafted `*_nms.onnx` sibling files — produced at Plan 06-03's box run (gitignored via *.onnx, regenerable)
</artifacts>

<verification>
- `pixi install -e graphsurgeon` resolves on macOS; `import onnx_graphsurgeon` works (>=0.6.1).
- `pixi run -e graphsurgeon pytest tests/scripts/test_graft_efficientnms.py -m graphsurgeon --no-cov` passes CPU-only.
- The 4 end-to-end models are guarded (graft exits non-zero for yolo26/rfdetr/deim/rtdetrv2).
- Default CI selection `-m "not vlm and not trt and not external and not graphsurgeon"` stays green.
- pyproject [trt] pins all four packages; `pixi run lint` clean.
</verification>

<success_criteria>
A committed `scripts/graft_efficientnms.py` grafts EfficientNMS_TRT onto exactly the 3 dense-head models (with RTMDet's pre-NMS TopK stripped first) and refuses the 4 end-to-end models, with graft attributes sourced from each postprocessor's convention; the graph surgery is CPU-unit-tested on synthetic graphs in a cross-platform `graphsurgeon` env; a linux-64-only `trt` env cleanly separates onnxruntime-gpu from the core CPU onnxruntime; and the four [trt] packages are pinned (onnx-graphsurgeon>=0.6.1). (LAT-03)
</success_criteria>

<output>
Create `.planning/phases/06-latency/06-02-SUMMARY.md` when done, noting the pixi environment design chosen (and any pixi-skill findings), the per-model graft attributes, and the Open-Question-1 caveat carried into Plan 06-03.
</output>
