---
phase: 04-reproduction-gate
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - scripts/run_bootstrap_gate.py
  - tests/scripts/test_run_bootstrap_gate.py
  - docs/methodology.md
autonomous: true
requirements: [REPRO-03]
user_setup:
  - service: local-predictions
    why: "The stored test-split predictions and the published bootstrap anchor live in the source repo, outside this repo, and are required for the bootstrap reproduction"
    env_vars: []
must_haves:
  truths:
    - "scripts/run_bootstrap_gate.py reproduces the seeded paired bootstrap (seed=0, n_boot=1000) over the 94-image test predictions and matches bootstrap_5c_test_7models.json per-model point estimates and CIs within tolerance, with every adjacent pair significant (REPRO-03)"
    - "the joint-best comparison reproduces YOLOX-M @800 (0.723) vs YOLO26m @640 (0.716) as a statistical tie: point_diff ~= +0.0073 (+0.73pt), CI ~= [-0.0033, +0.0190], ci_excludes_zero is False (REPRO-03)"
    - "docs/methodology.md states the corrected finding: all 7 models reproduce, and the earlier YOLOX-M/RTMDet-M gap was a variant mix-up (the drift script scored YOLOX-M@800 and the RTMDet rewarmup ablation against the @640 base anchor), not lost artifacts (REPRO-03)"
  artifacts:
    - scripts/run_bootstrap_gate.py
    - tests/scripts/test_run_bootstrap_gate.py
    - docs/methodology.md
  key_links:
    - "run_bootstrap_gate reuses run_bootstrap/build_report from metrics/bootstrap.py and the correct-variant prediction paths declared in the 04-01 reproduction_640.yaml manifest"
    - "the tie uses the @800 YOLOX-M predictions while the 7-model table uses the @640 file — the same two-variant distinction the corrected methodology note documents"
---

<objective>
Build `scripts/run_bootstrap_gate.py`, the REPRO-03 statistical gate, and correct the methodology doc. The script runs the ported seeded paired bootstrap (`run_bootstrap` / `build_report`, seed=0, n_boot=1000) over the 94-image test predictions and asserts two things: (1) it reproduces the published `bootstrap_5c_test_7models.json` anchor — every model's point estimate and 95% CI within tolerance, and every adjacent pair in the @640 ranking significant (`ci_excludes_zero`); and (2) it reproduces the headline joint-best result — YOLOX-M @800 (0.723) vs YOLO26m @640 (0.716) is a statistical TIE (point_diff ≈ +0.73pt, CI ≈ [−0.33, +1.90]pt, `ci_excludes_zero` False).

The plan also rewrites the `docs/methodology.md` supervision-drift narrative, which currently (incorrectly) claims YOLOX-M/RTMDet-M anchor snapshots were "lost." The correct finding: all 7 reproduce; the earlier gap was an artifact-selection mix-up (the drift script fed the @800 YOLOX-M and the RTMDet rewarmup ablation into the @640 base anchor), now resolved by pointing at the correct-variant files — the same distinction that separates the @640 table (REPRO-01) from the @800 tie (this plan).

Purpose: the study's statistical-honesty claim ("YOLOX-M vs YOLO26m is a tie") is a headline the reports must lead with; it has to reproduce from data, and the methodology doc that feeds the blog must state the true reason all 7 reproduce.
Output: `scripts/run_bootstrap_gate.py` (external data, not in CI) + a CI-safe synthetic bootstrap test + a corrected methodology note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/object_detection_eval/metrics/bootstrap.py
@src/object_detection_eval/metrics/detection_map.py
@src/object_detection_eval/data/coco_gt.py
@src/object_detection_eval/data/taxonomy.py
@benchmarks/basketball/conf/reproduction_640.yaml
@docs/methodology.md
@scripts/measure_supervision_drift.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_bootstrap_gate.py — reproduce the anchor CIs and the joint-best tie (REPRO-03)</name>
  <files>scripts/run_bootstrap_gate.py</files>
  <precondition>The source-repo artifacts exist locally (halt with a per-path message if any is missing): the anchor `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/.deploy_comparison/bootstrap_5c_test_7models.json`; the seven correct-variant @640 test predictions declared in benchmarks/basketball/conf/reproduction_640.yaml (from 04-01); the @800 YOLOX-M test predictions `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/official_2026-07-13/YOLOX-M-800/merged5/predictions_yolox_test.json.gz`; the YOLO26m @640 predictions from the manifest; and the basketball test GT `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/data/basketball-player-detection-3/test/_annotations.coco.json`. supervision==0.29.1 is already pinned in the pixi env.</precondition>
  <reversibility rating="costly">The reproduced CIs and the tie verdict are the statistical baseline the reports lead with; if a CI does not reproduce, that is a defect against the ported bootstrap (Phase 2), not a tolerance to widen. Keep n_boot=1000 and seed=0 fixed to match the anchor.</reversibility>
  <read_first>
    - src/object_detection_eval/metrics/bootstrap.py (in context): `load_predictions`, `run_bootstrap(gt_map, pred_maps, n_boot, seed)`, `build_report(...)` — build_report emits per_model {point_estimate, ci_2.5, ci_97.5} and pairwise {point_diff, ci_2.5, ci_97.5, ci_excludes_zero}; the paired shared-draw-per-iteration structure must be reused as-is (seed=0, n_boot=1000 reproduces the anchor).
    - benchmarks/basketball/conf/reproduction_640.yaml (in context, from 04-01): the correct-variant per-model `predictions` paths for the 7-model anchor reproduction — reuse them, do not re-declare paths.
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/.deploy_comparison/bootstrap_5c_test_7models.json — config {n_boot:1000, seed:0, n_images:94, models:[YOLO26m, DEIM-M, YOLOX-M, RF-DETR-M, RTMDet-M, DAMO-YOLO-M, RT-DETRv2-M]}; per_model[*].mAP_50_95 {point_estimate, ci_2.5, ci_97.5}; pairwise[*].mAP_50_95.ci_excludes_zero (every adjacent pair is True in this @640 anchor).
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT.md lines 40-46 and 652-653 — the headline joint-best tie: YOLOX-M @800 (72.3) vs YOLO26m @640 (71.6), paired-bootstrap Δ = +0.73 pt, 95% CI [−0.33, +1.90], statistically tied. The @800 YOLOX-M predictions produce 0.7228; YOLO26m @640 produce 0.7155; their paired diff is the tie.
    - scripts/measure_supervision_drift.py (in context): the temp-dir gunzip helper for `.json.gz` predictions, the GT-from-tarball / GT-from-file loading, the precondition-assert pattern, and the documented note that rtdetrv2_m's predictions file is named predictions_deim_test.json.
  </read_first>
  <action>Write `scripts/run_bootstrap_gate.py` with two sub-checks over the same loaded GT (`load_coco_gt(test _annotations.coco.json, resolve_taxonomy("merged5")[0])`). Args: `--source-repo` (default the object-detection-training absolute path), `--data-root` (default the basketball dataset dir), `--manifest` (default benchmarks/basketball/conf/reproduction_640.yaml), `--n-boot` (default 1000), `--seed` (default 0), `--tolerance` (default 0.01 on point estimates and CI bounds). Assert preconditions. Check A (anchor reproduction): read the 7 correct-variant `predictions` paths from the manifest, `load_predictions` each into a `pred_maps` dict keyed by the anchor's model names, run `run_bootstrap` + `build_report` at seed/n_boot, and assert every model's mAP_50_95 point_estimate and ci_2.5/ci_97.5 match the anchor within `--tolerance`, AND that every adjacent-in-rank pair has `ci_excludes_zero` True (all significant). Check B (joint-best tie): build a 2-model `pred_maps` of {YOLO26m: @640 predictions from the manifest, "YOLOX-M@800": load_predictions of the gunzipped @800 file}, run the same paired bootstrap, and assert the YOLOX-M@800-minus-YOLO26m pair reproduces point_diff ≈ +0.0073 and CI ≈ [−0.0033, +0.0190] within `--tolerance`, with `ci_excludes_zero` False (a tie). Log a table for each check (model/pair, expected, measured, within-tolerance) and a one-line tie conclusion in percentage points. Exit non-zero on any assertion failure. Route ALL output through loguru; no print(). Do NOT wire this script into pytest — it reads source-repo-only artifacts absent from CI.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python scripts/run_bootstrap_gate.py && /Users/ortizeg/.pixi/bin/pixi run lint</automated>
  </verify>
  <done>`scripts/run_bootstrap_gate.py` reproduces the `bootstrap_5c_test_7models.json` per-model point estimates and CIs within tolerance with every adjacent pair significant, AND reproduces the YOLOX-M@800-vs-YOLO26m joint-best pair as a tie (Δ ≈ +0.73pt, CI ≈ [−0.33,+1.90]pt, ci_excludes_zero False), then exits 0. `pixi run lint` clean (T20). Any non-reproducing CI is recorded in the SUMMARY as a Phase 2 bootstrap defect, not absorbed by widening tolerance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CI-safe synthetic test for bootstrap determinism + tie detection (REPRO-03)</name>
  <files>tests/scripts/test_run_bootstrap_gate.py</files>
  <behavior>
    - On a small synthetic gt_map + two pred_maps, run_bootstrap at a fixed seed twice yields byte-identical per-model and pairwise-difference CI arrays (determinism the gate relies on).
    - The tie-classification helper from run_bootstrap_gate reports a tie when the pairwise CI straddles zero (ci_2.5 < 0 < ci_97.5 -> ci_excludes_zero False) and significant when the CI excludes zero.
    - The anchor-comparison helper passes when measured point estimate and CI bounds are within tolerance of the anchor values and fails on an out-of-tolerance CI bound.
  </behavior>
  <action>Add `tests/scripts/test_run_bootstrap_gate.py`, offline and dataset-free. Build a tiny synthetic gt_map plus two model pred_maps (a few images of sv.Detections) and assert two seeded run_bootstrap calls give identical CI arrays for both a single model and the pairwise difference. Import the pure helpers from scripts/run_bootstrap_gate.py — the tie-classifier (straddles-zero) and the anchor-within-tolerance comparator — and test their boundaries on synthetic numbers (a straddling CI is a tie; a CI excluding zero is significant; an out-of-tolerance CI bound fails). Do NOT read source-repo predictions or the anchor json; if the helpers are not import-safe, refactor them to module-level pure functions. loguru only.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/scripts/test_run_bootstrap_gate.py -x -q</automated>
  </verify>
  <done>The synthetic test passes offline with no external data: it locks bootstrap same-seed determinism and the tie/significance + anchor-tolerance helpers. Default `pixi run test` stays green without the source-repo predictions.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the supervision-drift narrative in methodology.md (REPRO-03)</name>
  <files>docs/methodology.md</files>
  <reversibility rating="reversible">A documentation edit; fully revertable via git.</reversibility>
  <read_first>
    - docs/methodology.md (in context): the "Isolating the cause" and "Conclusion and pin rationale" sections currently claim the YOLOX-M/RTMDet-M anchor snapshots were "lost" / a data-provenance gap and advise Phase 4 not to trust `bootstrap_5c_test_7models.json` verbatim — this narrative is factually wrong and must be rewritten.
    - benchmarks/basketball/conf/reproduction_640.yaml (in context, from 04-01) and this plan's Task 1 result: all 7 reproduce from the correct-variant files, and the @640 anchor reproduces verbatim.
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT.md line 46 (the +0.73pt / CI [−0.33,+1.90] tie is the @800-vs-@640 joint-best comparison, a different comparison from the @640 table).
  </read_first>
  <action>Rewrite the incorrect portion of docs/methodology.md (the "Isolating the cause" narrative and any Phase-4 implication that says the anchor cannot be trusted or that snapshots were lost). Replace it with the corrected finding: an investigation resolved the apparent two-model gap as an artifact-SELECTION mix-up, not lost data — the drift script scored the @800 YOLOX-M predictions and the RTMDet rewarmup-ablation predictions against a @640 BASE anchor, so it compared different model variants. Pointing at the correct-variant files (YOLOX-M @640 for the 0.672 comparison row and base RTMDet-M for 0.628) reproduces the `bootstrap_5c_test_7models.json` anchor verbatim for all 7 models; the @800 YOLOX-M predictions (0.723) belong to a different, deliberate comparison — the joint-best tie against YOLO26m (Δ +0.73pt, CI [−0.33,+1.90], statistically tied). State plainly that all seven models reproduce and that the earlier concern is closed; keep the supervision-pin decision (0.29.1) and the measured-drift table, but reframe the two outliers as the variant mix-up rather than a data-provenance gap. Do NOT delete the pin rationale or the reproduction-command section. Preserve loguru/no-print-relevant guidance and the doc's existing structure/headings where still accurate.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && grep -qi "variant" docs/methodology.md && grep -qi "all seven\|all 7" docs/methodology.md && grep -qi "joint-best\|statistically tied\|statistical tie" docs/methodology.md && ! grep -qi "were .*lost\|artifacts .*did not\|data-provenance gap in the anchor" docs/methodology.md && echo OK</automated>
  </verify>
  <done>docs/methodology.md states that all seven models reproduce and that the earlier YOLOX-M/RTMDet-M gap was a variant-selection mix-up (not lost artifacts), documents the @640-vs-@800 distinction and the joint-best tie, and no longer claims snapshots were lost or that the anchor cannot be trusted verbatim. The supervision-pin decision and reproduction instructions remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored prediction JSON + anchor json -> bootstrap | source-repo artifacts read once for the CI reproduction, outside this repo |
| @640 vs @800 YOLOX-M predictions -> comparison identity | the correct file must feed each comparison (table vs tie), or the wrong claim is reproduced |
| methodology narrative -> blog | this doc feeds the public post, so a factual error propagates downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | wrong YOLOX-M variant fed into a comparison (the exact mix-up being corrected) | high | mitigate | The gate reads the correct @640 paths from the 04-01 manifest for the table and the explicit @800 file only for the tie; the synthetic test locks tie/significance logic offline |
| T-04-07 | Repudiation | a published methodology claim that misstates why models reproduce | medium | mitigate | The narrative is rewritten to the verified finding and the verify step positively asserts the corrected wording and negatively asserts the removed "lost" claim |
| T-04-08 | Tampering | a CI not reproducing being absorbed by widening tolerance | high | mitigate | Tolerance and seed/n_boot are fixed to the anchor and flagged reversibility=costly; a miss is a Phase 2 bootstrap defect recorded in the SUMMARY |
</threat_model>

<verification>
- `scripts/run_bootstrap_gate.py` reproduces the anchor per-model CIs (all adjacent pairs significant) AND the joint-best tie (Δ ≈ +0.73pt, CI ≈ [−0.33,+1.90]pt, ci_excludes_zero False), exit 0.
- `pixi run pytest tests/scripts/test_run_bootstrap_gate.py -x` green offline (determinism + tie/significance + anchor-tolerance helpers).
- `docs/methodology.md` states all seven reproduce, documents the variant mix-up + the @640/@800 distinction + the tie, and drops the "lost snapshots" narrative.
- `pixi run lint` clean (T20).
</verification>

<success_criteria>
The seeded paired bootstrap reproduces the published CIs — every model's point estimate and CI from `bootstrap_5c_test_7models.json` within tolerance with every adjacent pair significant, and the YOLOX-M@800-vs-YOLO26m joint-best pair as a statistical tie (+0.73pt, CI [−0.33,+1.90]) — and the methodology doc records the corrected finding that all seven models reproduce and the earlier gap was a variant mix-up, not lost data.
</success_criteria>

<output>
Create `.planning/phases/04-reproduction-gate/04-03-SUMMARY.md` when done, including the reproduced anchor-CI table, the tie result in percentage points, and a note that the methodology narrative was corrected.
</output>

## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `scripts/run_bootstrap_gate.py` | script (external data, not in CI) | REPRO-03 |
| `tests/scripts/test_run_bootstrap_gate.py` | CI-safe test | REPRO-03 |
| `docs/methodology.md` (corrected drift narrative) | doc | REPRO-03 |
</output>
