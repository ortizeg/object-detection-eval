---
phase: 04-reproduction-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_coco_reference.py
  - tests/scripts/test_run_coco_reference.py
autonomous: true
requirements: [REPRO-02]
user_setup:
  - service: local-coco
    why: "COCO val2017 (~1.9 GB) and the COCO-pretrained YOLOX-S ONNX live outside this repo and are required for the reference run"
    env_vars: []
must_haves:
  truths:
    - "scripts/run_coco_reference.py scores the COCO-pretrained YOLOX-S ONNX on val2017 under the identity taxonomy through compute_metrics and reproduces mAP@50:95 ~= 39.6 within tolerance (REPRO-02)"
    - "the reproduced ~39.6 lands materially below the 40.5 pycocotools-published figure, confirming the known ~0.9pt supervision-vs-pycocotools systematic gap rather than a harness defect (REPRO-02)"
  artifacts:
    - scripts/run_coco_reference.py
    - tests/scripts/test_run_coco_reference.py
  key_links:
    - "run_coco_reference reuses load_coco_gt + identity_taxonomy_from_coco + compute_metrics — the same scorer used for the basketball @640 table, so a COCO score near the published figure proves the harness is dataset-agnostic and not the source of the preprocessing swings"
---

<objective>
Build `scripts/run_coco_reference.py`, the REPRO-02 sanity check: score the COCO-pretrained YOLOX-S ONNX on COCO val2017 (80-class, identity taxonomy) through the SAME `compute_metrics` used for the basketball table, and assert it reproduces mAP@50:95 ≈ 39.6 — materially below the 40.5 figure published under pycocotools. This confirms the ~0.9pt `supervision`-vs-`pycocotools` systematic gap and, crucially, that the harness scorer is NOT the source of the large preprocessing-driven accuracy swings the study reports (YOLOX-M 30.8→72.3). The COCO-pretrained YOLOX-S ONNX (80-class) and val2017 already exist locally, so no artifact must be located or downloaded.

Purpose: the preprocessing finding is only falsifiable if the scorer is independently validated against a public benchmark; this is that validation, run through the identical harness path.
Output: `scripts/run_coco_reference.py` (external-data, not wired into CI) + a CI-safe synthetic test of the identity-taxonomy pipeline and the gap assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/object_detection_eval/inference/detectors/yolox.py
@src/object_detection_eval/metrics/detection_map.py
@src/object_detection_eval/data/coco_gt.py
@src/object_detection_eval/data/taxonomy.py
@src/object_detection_eval/data/image.py
@scripts/measure_supervision_drift.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_coco_reference.py — YOLOX-S on val2017 reproduces 39.6 (REPRO-02)</name>
  <files>scripts/run_coco_reference.py</files>
  <precondition>COCO val2017 exists locally at `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/data/coco2017/` with `val2017/` images and `annotations/instances_val2017.json`, and the COCO-pretrained YOLOX-S ONNX + label map exist at `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/data/coco2017/yolox_s_coco.onnx` and `.../yolox_coco_labels_mapping.json`. Assert each path exists up front and halt with a clear message naming the missing path if not. onnxruntime and supervision==0.29.1 are already in the pixi env.</precondition>
  <reversibility rating="costly">The 39.6-vs-40.5 verdict is the evidence that the harness scorer is sound; if COCO does not reproduce near 39.6, that is a scorer defect to investigate, not a number to overwrite. Do not fabricate or back-fit the reference number.</reversibility>
  <read_first>
    - src/object_detection_eval/data/taxonomy.py (in context): `identity_taxonomy_from_coco(coco_json)` builds contiguous eval ids 0..N-1 from a COCO file's own categories in ascending category-id order, keys lowercased; `resolve_taxonomy("identity", coco_json_path=...)` returns (name_to_id, id_to_name); `remap_detections(dets, label_map, name_to_id)` maps a detector's native class names to identity eval ids.
    - src/object_detection_eval/inference/detectors/yolox.py (in context): `YOLOXDetector(model_path, label_map, confidence_threshold, input_height, input_width)`; construct with the YOLOX-S COCO ONNX, label_map from yolox_coco_labels_mapping.json `id_to_name` (int keys), and the ONNX's own input size (read it from the onnxruntime session input shape; YOLOX COCO export is typically 640).
    - src/object_detection_eval/metrics/detection_map.py (in context): `detections_to_sv` + `compute_metrics` — the same scorer used for the basketball table.
    - src/object_detection_eval/data/coco_gt.py (in context): `load_coco_gt(instances_val2017.json, name_to_id)` returns filename -> sv.Detections; note COCO category names must string-match between the YOLOX label map and the val2017 categories (e.g. "traffic light"); log and surface any name that fails to map instead of silently dropping the class.
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT_FINAL.md (the COCO reference note: 39.6 harness vs 40.5 published, framed as the supervision-vs-pycocotools gap that proves the harness is not the source of accuracy swings).
    - scripts/measure_supervision_drift.py (in context): the precondition-assert and --source-repo/root argument style to mirror; not wired into pytest.
  </read_first>
  <action>Write `scripts/run_coco_reference.py`. CLI args: `--coco-root` (default `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/data/coco2017` absolute), `--onnx` (default `<coco-root>/yolox_s_coco.onnx`), `--labels` (default `<coco-root>/yolox_coco_labels_mapping.json`), `--tolerance` (default 0.005, i.e. 0.5pt), `--max-images` (default 0 = all; a positive value scores only the first N images for a quick pipeline smoke — the gate uses all). Assert preconditions. Build identity taxonomy from `annotations/instances_val2017.json` via `resolve_taxonomy("identity", coco_json_path=...)`; load GT with `load_coco_gt`. Construct `YOLOXDetector` with the COCO ONNX, the label_map (int keys) from the YOLOX COCO labels file, confidence_threshold=0.01, and input size read from the session. Loop over the val2017 image files present in GT: read with ImageLoader from `<coco-root>/val2017/<filename>`, `predict`, `remap_detections` to identity, `detections_to_sv(remapped, w, h)`; score with `compute_metrics(gt_map, pred_map)`. Log the resulting mAP@50:95 / mAP@50 and the delta against 39.6 and against the 40.5 pycocotools figure. Assert and exit non-zero unless: (1) measured mAP@50:95 is within `--tolerance` of 0.396; (2) measured is strictly below 0.405 (confirming the supervision-vs-pycocotools gap direction). Print a one-line conclusion that the harness reproduces the COCO reference within the known gap. Route ALL output through loguru; no print(). Do NOT wire this script into pytest — it reads external, gitignored COCO data.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python scripts/run_coco_reference.py && /Users/ortizeg/.pixi/bin/pixi run lint</automated>
  </verify>
  <done>`scripts/run_coco_reference.py` runs the YOLOX-S COCO ONNX over all of val2017 under the identity taxonomy and reports mAP@50:95 within 0.005 of 0.396 and strictly below 0.405, printing the gap conclusion, and exits 0. `pixi run lint` clean (T20). If the measured value is not near 39.6, the executor records the value + gap analysis in the SUMMARY as a scorer finding rather than adjusting the target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CI-safe synthetic test for the identity pipeline + gap assertion (REPRO-02)</name>
  <files>tests/scripts/test_run_coco_reference.py</files>
  <behavior>
    - identity_taxonomy_from_coco on a tiny synthetic COCO dict (3 categories with non-contiguous ids) yields contiguous eval ids 0..2 in ascending category-id order, with lowercased name keys.
    - The gap-assertion helper from run_coco_reference passes when measured is within tolerance of 0.396 AND strictly below 0.405, and fails when measured >= 0.405 (wrong direction) or outside tolerance.
    - The end-to-end remap path (Detection in a detector's native class space -> remap_detections under an identity name_to_id -> detections_to_sv) preserves box geometry and maps a known class name to the right identity id on a tiny fixture, with no onnxruntime session involved.
  </behavior>
  <action>Add `tests/scripts/test_run_coco_reference.py`, offline and dataset-free. Build a tiny synthetic COCO categories dict (non-contiguous ids) and assert `identity_taxonomy_from_coco` (or resolve_taxonomy("identity", ...) over a temp json) produces the expected contiguous, lowercased mapping. Import the pure gap-assertion helper from scripts/run_coco_reference.py and test its pass/fail boundaries (within-tolerance-and-below-0.405 passes; 0.405+ fails; out-of-tolerance fails). Construct a couple of hand-built Detection objects, run remap_detections under a small identity name_to_id + label_map, and assert the class ids and normalized boxes come through detections_to_sv correctly. Do NOT load the COCO ONNX or any real image; if the gap helper is not import-safe without heavy deps, refactor it to a module-level pure function. loguru only.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/scripts/test_run_coco_reference.py -x -q</automated>
  </verify>
  <done>The synthetic test passes offline with no COCO data: it locks the identity-taxonomy construction, the remap/detections_to_sv geometry, and the 39.6-vs-40.5 gap-direction assertion. Default `pixi run test` stays green without the COCO dataset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external COCO val2017 + YOLOX-S ONNX -> harness | gitignored ~1.9 GB dataset and a model binary read from outside the repo |
| COCO category-name strings -> identity mapping | class names must string-match across the label map and the annotations, or classes are silently dropped |
| reproduced 39.6 -> scorer-soundness verdict | the reference number is the evidence the scorer is not the source of accuracy swings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | silent class-name mismatch dropping COCO classes and deflating mAP | medium | mitigate | Surface any unmapped category name via loguru and assert against 39.6 within a tight tolerance so a large silent drop fails the gate |
| T-04-05 | Tampering | back-fitting the reference target to force a pass | high | mitigate | The 0.396 target and the strictly-below-0.405 direction are asserted and flagged reversibility=costly; a miss is recorded as a scorer finding, not a re-targeted number |
</threat_model>

<verification>
- `scripts/run_coco_reference.py` scores YOLOX-S on all of val2017 under identity taxonomy and reports mAP@50:95 within 0.005 of 0.396 and below 0.405 (exit 0).
- `pixi run pytest tests/scripts/test_run_coco_reference.py -x` green offline (identity mapping + remap geometry + gap-direction helper).
- `pixi run lint` clean (T20).
</verification>

<success_criteria>
The COCO reference check reproduces YOLOX-S on val2017 at ~39.6 mAP@50:95 through the same harness scorer used for the basketball table, materially below the 40.5 pycocotools figure — confirming the known supervision-vs-pycocotools gap and that the harness is not the source of the preprocessing-driven accuracy swings; the CI-safe test locks the pipeline logic offline.
</success_criteria>

<output>
Create `.planning/phases/04-reproduction-gate/04-02-SUMMARY.md` when done, including the measured COCO mAP@50:95, its delta to 39.6 and to 40.5, and any unmapped-class notes.
</output>

## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `scripts/run_coco_reference.py` | script (external data, not in CI) | REPRO-02 |
| `tests/scripts/test_run_coco_reference.py` | CI-safe test | REPRO-02 |
</output>
