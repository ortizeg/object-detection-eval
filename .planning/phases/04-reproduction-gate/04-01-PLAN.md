---
phase: 04-reproduction-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_benchmark.py
  - benchmarks/basketball/conf/reproduction_640.yaml
  - tests/scripts/test_run_benchmark.py
  - docs/provenance/gcs-manifest.md
autonomous: true
requirements: [REPRO-01]
user_setup:
  - service: local-artifacts
    why: "The @640 ONNX weights + stored predictions + basketball test split live outside this repo (gitignored/external) and must be present locally for the full reproduction run"
    env_vars: []
must_haves:
  truths:
    - "scripts/run_benchmark.py scores the 7 @640 models through compute_metrics and reproduces the EVAL_REPORT_FINAL.md §2 5c mAP@50:95 table within tolerance: YOLO26m 0.716, DEIM-M 0.686, YOLOX-M 0.672, RF-DETR-M 0.646, RTMDet-M 0.628, DAMO-YOLO-M 0.619, RT-DETRv2-M 0.581 (REPRO-01)"
    - "the reproduced table is in the exact rank order YOLO26m > DEIM-M > YOLOX-M > RF-DETR-M > RTMDet-M > DAMO-YOLO-M > RT-DETRv2-M (REPRO-01)"
    - "the @640 comparison resolves the correct model variants — YOLOX-M @640 (not the @800 file) and base RTMDet-M (not the rewarmup ablation) (REPRO-01)"
    - "the YOLOX-M @640 ONNX is mirrored to gs://deep-ego-model-training/.../eval/final-comparison-640/yolox_m/ so every comparison model's weights exist in at least two places (REPRO-01)"
  artifacts:
    - scripts/run_benchmark.py
    - benchmarks/basketball/conf/reproduction_640.yaml
    - tests/scripts/test_run_benchmark.py
  key_links:
    - "run_benchmark composes the 7 detectors + remap_detections + detections_to_sv + compute_metrics — the same metrics path validated in 02-04"
    - "the manifest's predictions_fallback paths are the correct-variant files that reproduce the published numbers exactly (the artifact-selection gap 02-04 flagged, now resolved)"
---

<objective>
Build `scripts/run_benchmark.py`, the REPRO-01 reproduction gate: it re-scores the 7 medium detectors @640 through the refactored harness and asserts the published `EVAL_REPORT_FINAL.md` §2 5-class test table (YOLO26m 0.716, DEIM-M 0.686, YOLOX-M 0.672, RF-DETR-M 0.646, RTMDet-M 0.628, DAMO-YOLO-M 0.619, RT-DETRv2-M 0.581) within tolerance AND in exact rank order. The script is parameterized by external data/weights roots and stages nothing into the repo (dataset + ONNX are gitignored). It supports two modes over the SAME `compute_metrics`: full end-to-end ONNX inference (preferred, all 7 ONNX are local) and a from-predictions fallback that scores the correct-variant stored prediction files. A committed manifest encodes the per-model paths, expected numbers, and rank order; the ONNX/data live outside the repo behind CLI roots.

This plan also closes the last SAFE-04-style mirror gap: the YOLOX-M @640 ONNX is the only comparison weight file that exists in one place (external laptop-only), so it is mirrored to GCS.

Purpose: this is the HARD GATE — no downstream phase begins until the refactored harness proves it reproduces the published @640 table. Any drift is a Phase 2/3 defect, not a tolerance to widen.
Output: `scripts/run_benchmark.py`, the committed `reproduction_640.yaml` manifest, a CI-safe synthetic test, the GCS mirror, and a provenance record of it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/object_detection_eval/inference/detectors/__init__.py
@src/object_detection_eval/inference/detectors/yolox.py
@src/object_detection_eval/metrics/detection_map.py
@src/object_detection_eval/metrics/bootstrap.py
@src/object_detection_eval/data/coco_gt.py
@src/object_detection_eval/data/taxonomy.py
@src/object_detection_eval/data/image.py
@scripts/measure_supervision_drift.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_benchmark.py — reproduce the 7-model @640 table (REPRO-01)</name>
  <files>scripts/run_benchmark.py, benchmarks/basketball/conf/reproduction_640.yaml</files>
  <precondition>The external artifacts exist locally (halt with a clear per-path message if any is missing): (a) basketball test split at `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/data/basketball-player-detection-3/test/_annotations.coco.json` plus its 94 `.jpg` images; (b) the six source-repo ONNX under `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/.deploy_comparison/` (reuse_onnx/yolo26m/model.onnx and artifacts/{deim_m,rfdetr_m,rtmdet_m,damo_m,rtdetrv2_m}/*_640.onnx) with their sibling labels_mapping.json; (c) the YOLOX-M @640 ONNX + labels at `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/YOLOX/training_results/basketball_m/`; (d) the correct-variant stored predictions listed in the manifest. onnxruntime and supervision==0.29.1 are already in the pixi env.</precondition>
  <reversibility rating="costly">The chosen tolerance and the pass/defect verdict are the reproduction-gate baseline the whole downstream roadmap trusts; loosening the tolerance later to force a pass would silently invalidate the gate. Keep the tolerance tight and treat divergence as a defect to file, not to absorb.</reversibility>
  <read_first>
    - src/object_detection_eval/inference/detectors/yolox.py and rtmdet.py (already in context): each detector's __init__ takes `model_path`, `label_map: dict[int,str]`, `confidence_threshold`, `input_height`, `input_width`; `predict(image_bgr) -> list[Detection]` in normalized xywh. Note default input sizes vary (RF-DETR defaults to 560; the @640 comparison MUST pass input_height=input_width=640 for every model) and default conf varies (pass confidence_threshold=0.01 for every model to match the ranking protocol).
    - src/object_detection_eval/metrics/detection_map.py (in context): `detections_to_sv(list[Detection], w, h) -> sv.Detections` (normalized xywh -> pixel xyxy) and `compute_metrics(gt_map, pred_map) -> {mAP_50_95, mAP_50, mAP_75, per_class_ap50}`.
    - src/object_detection_eval/data/taxonomy.py (in context): `resolve_taxonomy("merged5") -> (name_to_id, id_to_name)`; `remap_detections(dets, label_map, name_to_id)` translates a detector's native class ids to merged5 eval ids and drops unmapped classes.
    - src/object_detection_eval/metrics/bootstrap.py (in context): `load_predictions(path) -> dict[str, sv.Detections]` reads the stored-prediction JSON format (filename -> [{bbox_xyxy, class_id, confidence}]) — reuse it for the from-predictions mode.
    - scripts/measure_supervision_drift.py (in context): the precondition-assert / temp-dir gunzip / --source-repo argument pattern to mirror; note it is NOT wired into pytest.
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT_FINAL.md §1 (protocol: conf=0.01 for ranking, IoU 0.50:0.95, maxDets=100, per-model NMS for YOLOX/RTMDet, NMS-free for YOLO26/DEIM/RF-DETR) and §2 (the primary table = the 7 target numbers).
  </read_first>
  <action>Write `benchmarks/basketball/conf/reproduction_640.yaml` first: a committed manifest listing the 7 models in published rank order, each entry carrying `name`, `detector` (one of yolo26|deim|yolox|rfdetr|rtmdet|damo|rtdetrv2), a `root` selector (source_repo or yolox), `onnx`, `labels`, `predictions` (the correct-variant stored file), `input_size: 640`, `confidence_threshold: 0.01`, and `expected_map5095`. Use these exact correct-variant values — YOLO26m: onnx reuse_onnx/yolo26m/model.onnx, labels reuse_onnx/yolo26m/labels_mapping.json, predictions eval_reuse/YOLO26m-640/merged5/predictions_yolo26_test.json, expected 0.716; DEIM-M: artifacts/deim_m/deim_m_640.onnx + artifacts/deim_m/labels_mapping.json + eval_new/deim_m/merged5/predictions_deim_test.json, 0.686; YOLOX-M (root yolox): basketball_m/yolox_m_basketball_640.onnx + basketball_m/labels_mapping.json, predictions (root source_repo) eval_reuse/YOLOX-M-640/merged5/predictions_yolox_test.json, 0.672; RF-DETR-M: artifacts/rfdetr_m/rfdetr_m_640.onnx + labels + eval_new/rfdetr_m/merged5/predictions_rf-detr_test.json, 0.646; RTMDet-M: artifacts/rtmdet_m/rtmdet_m_640.onnx + labels + rtmdet_validate_out/merged5/predictions_rtmdet_test.json, 0.628; DAMO-YOLO-M: artifacts/damo_m/damo_m_640.onnx + labels + eval_new/damo_m/merged5/predictions_damo-yolo_test.json, 0.619; RT-DETRv2-M: artifacts/rtdetrv2_m/rtdetrv2_m_640.onnx + labels + eval_new/rtdetrv2_m/merged5/predictions_deim_test.json (filename says deim but this dir holds rtdetrv2's own predictions — see the note in measure_supervision_drift.py), 0.581. Add a short header comment: this manifest deliberately points at the @640 YOLOX-M and the BASE (non-rewarmup) RTMDet-M — the two variants a prior drift script mixed up.

Then write `scripts/run_benchmark.py`. CLI args: `--data-root` (default the basketball dataset dir absolute path), `--source-repo` (default the object-detection-training absolute path), `--yolox-root` (default the external YOLOX training_results absolute path), `--manifest` (default benchmarks/basketball/conf/reproduction_640.yaml), `--mode {end2end,from-predictions}` (default end2end), `--tolerance` (default 0.01), `--taxonomy` (default merged5). Assert every required path exists up front (per-path halt message). Load GT once via `load_coco_gt(data_root/test/_annotations.coco.json, name_to_id)` under `resolve_taxonomy("merged5")`. For each manifest model build a per-file pred_map: in end2end mode, construct the detector class (map the `detector` key to the imported class from object_detection_eval.inference.detectors) with its resolved onnx path, `label_map` built from that model's labels_mapping.json `id_to_name` (convert the string keys to int), input_height=input_width=640, confidence_threshold=0.01; for each GT filename read `data_root/test/<filename>` with ImageLoader, call `detector.predict(img)`, `remap_detections(...)` to merged5, then `detections_to_sv(remapped, w, h)`; in from-predictions mode call `load_predictions(resolved predictions path)` directly. Score each model with `compute_metrics(gt_map, pred_map)`, collect mAP@50:95 (and mAP@50). Print a 7-row table (model, expected, measured, delta, within-tolerance, running rank) through loguru. Then assert, and exit non-zero on any failure: (1) every model's measured mAP@50:95 is within `--tolerance` of its `expected_map5095`; (2) the measured ordering equals the manifest (published) rank order exactly. Take a `--strict` flag that in from-predictions mode tightens the tolerance to 0.001 (the stored predictions reproduce exactly). Route ALL output through loguru; no print(). Do NOT wire this script into pytest — it reads external, gitignored data.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python scripts/run_benchmark.py --mode from-predictions --strict && /Users/ortizeg/.pixi/bin/pixi run python scripts/run_benchmark.py --mode end2end --tolerance 0.02 && /Users/ortizeg/.pixi/bin/pixi run lint</automated>
  </verify>
  <done>`scripts/run_benchmark.py --mode from-predictions --strict` prints the 7-row table and exits 0, reproducing YOLO26m 0.716 / DEIM-M 0.686 / YOLOX-M 0.672 / RF-DETR-M 0.646 / RTMDet-M 0.628 / DAMO-YOLO-M 0.619 / RT-DETRv2-M 0.581 within 0.001 and in exact rank order. `--mode end2end --tolerance 0.02` runs all 7 ONNX over the 94 images and reproduces the same table within tolerance and rank order; if end2end diverges beyond tolerance for any model, the executor records the divergence table in the SUMMARY as a Phase 2/3 defect (the gate's designed pass-or-defect output) rather than widening the tolerance. `pixi run lint` is clean (T20).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CI-safe synthetic test for the gate logic (REPRO-01)</name>
  <files>tests/scripts/test_run_benchmark.py</files>
  <behavior>
    - The manifest benchmarks/basketball/conf/reproduction_640.yaml loads, lists exactly 7 models, and its `expected_map5095` values in file order are [0.716, 0.686, 0.672, 0.646, 0.628, 0.619, 0.581] (strictly descending — the published rank order).
    - The manifest points YOLOX-M at a path containing `_640` (not `_800`) and RTMDet-M at a predictions path containing `rtmdet_validate_out` (base, not `rewarmup`) — the correct-variant guard.
    - The within-tolerance check helper passes when measured is inside tolerance of expected and fails otherwise (boundary case at exactly tolerance).
    - The rank-order check helper passes for a descending measured sequence matching the manifest order and fails when two adjacent models swap.
  </behavior>
  <action>Add `tests/scripts/test_run_benchmark.py`, offline and dataset-free: parse the committed manifest and assert its shape (7 entries, strictly-descending expected values, the @640 / base-RTMDet variant guards described above). Import the small pure helpers from scripts/run_benchmark.py (the tolerance check and the rank-order check) and unit-test them on synthetic number sequences — a passing case, a just-outside-tolerance failing case, and an adjacent-swap rank failure. Do NOT touch external data, ONNX, or onnxruntime sessions; if run_benchmark's helpers are not import-safe without the heavy deps, refactor the two check helpers into module-level pure functions so the test imports cleanly. loguru only.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/scripts/test_run_benchmark.py -x -q</automated>
  </verify>
  <done>The synthetic test passes offline with no external data; it locks the manifest to the 7 published numbers in rank order and to the correct @640 / base-RTMDet variants, and it exercises the tolerance + rank-order helpers. Default `pixi run test` stays green without the datasets.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the YOLOX-M @640 ONNX to GCS and record it (REPRO-01 / SAFE-04 gap)</name>
  <files>docs/provenance/gcs-manifest.md</files>
  <precondition>`gsutil` is on PATH and authenticated (gsutil is already installed at /opt/homebrew/bin/gsutil), and the source ONNX exists at `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/YOLOX/training_results/basketball_m/yolox_m_basketball_640.onnx` with its sibling `labels_mapping.json`. Halt with a clear message if gsutil is unauthenticated or the file is missing.</precondition>
  <reversibility rating="reversible">`gsutil cp -n` is no-clobber (skips if the object already exists), so re-running is safe and non-destructive.</reversibility>
  <read_first>
    - docs/provenance/gcs-manifest.md (the existing GCS artifact manifest this repo keeps; find the eval-inputs section and match its row/entry format)
    - docs/provenance/artifact-tracker.md (cross-check how the YOLOX-M @640 ONNX is currently described as local-only, so the update reads consistently)
  </read_first>
  <action>Copy the YOLOX-M @640 ONNX and its labels_mapping.json to GCS with the no-clobber flag: `gsutil cp -n "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/YOLOX/training_results/basketball_m/yolox_m_basketball_640.onnx" gs://deep-ego-model-training/ego-training-data/basketball-data/eval/final-comparison-640/yolox_m/` and the same for `labels_mapping.json`. Then confirm both objects are present with `gsutil ls gs://deep-ego-model-training/ego-training-data/basketball-data/eval/final-comparison-640/yolox_m/`. Update docs/provenance/gcs-manifest.md to add the new mirror location for the YOLOX-M @640 ONNX + label map, so the repo records that this weight now exists in at least two places (was external/laptop-only). Keep the manifest's existing formatting.</action>
  <verify>
    <automated>gsutil ls "gs://deep-ego-model-training/ego-training-data/basketball-data/eval/final-comparison-640/yolox_m/" | grep -E "yolox_m_basketball_640\.onnx|labels_mapping\.json" && grep -qi "final-comparison-640/yolox_m" "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/docs/provenance/gcs-manifest.md" && echo OK</automated>
  </verify>
  <done>`gsutil ls` on the destination lists both `yolox_m_basketball_640.onnx` and `labels_mapping.json`; docs/provenance/gcs-manifest.md records the mirror location. The last comparison weight that lived only on one machine now exists in at least two places.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external ONNX / stored-prediction JSON -> harness | gitignored artifacts read from the source repo and the external YOLOX tree, outside this repo's history |
| local weight file -> GCS bucket | gsutil upload of a model binary to the shared private bucket |
| reproduced numbers -> gate verdict | the measured table decides whether the whole downstream roadmap proceeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | wrong model variant silently scored (the @800 YOLOX / rewarmup RTMDet mix-up) | high | mitigate | The committed manifest pins the correct @640 / base variants and the synthetic test asserts the variant guards (`_640`, `rtmdet_validate_out`); a swapped variant fails Task 2 offline |
| T-04-02 | Tampering | gate passing on a loosened tolerance rather than a true reproduction | high | mitigate | from-predictions strict mode asserts within 0.001; end2end divergence is filed as a defect, not absorbed; the tolerance decision is flagged reversibility=costly |
| T-04-03 | Information disclosure | gsutil upload to the wrong/public path | low | accept | Destination is a fixed private-bucket path under the existing eval prefix; `cp -n` is no-clobber; no new ACLs are set |
</threat_model>

<verification>
- `scripts/run_benchmark.py --mode from-predictions --strict` reproduces the 7 published @640 numbers within 0.001 and in exact rank order (exit 0).
- `scripts/run_benchmark.py --mode end2end --tolerance 0.02` runs all 7 ONNX over the 94 test images and reproduces the table within tolerance + rank order (or the executor files a Phase 2/3 defect table).
- `pixi run pytest tests/scripts/test_run_benchmark.py -x` green offline (manifest shape + variant guards + gate-logic helpers).
- `gsutil ls` confirms the YOLOX-M @640 ONNX + labels mirror; gcs-manifest.md records it.
- `pixi run lint` clean (T20).
</verification>

<success_criteria>
`scripts/run_benchmark.py` reproduces the EVAL_REPORT_FINAL.md §2 medium-@640 5-class test table (YOLO26m 0.716, DEIM-M 0.686, YOLOX-M 0.672, RF-DETR-M 0.646, RTMDet-M 0.628, DAMO-YOLO-M 0.619, RT-DETRv2-M 0.581) within tolerance and in the exact published rank order, using the correct @640 / base-RTMDet variants; the CI-safe test locks this offline; and the YOLOX-M @640 ONNX is mirrored to GCS so no comparison weight exists in only one place.
</success_criteria>

<output>
Create `.planning/phases/04-reproduction-gate/04-01-SUMMARY.md` when done, including the reproduced 7-row table (expected vs measured, both modes) and the GCS mirror confirmation.
</output>

## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `scripts/run_benchmark.py` | script | REPRO-01 |
| `benchmarks/basketball/conf/reproduction_640.yaml` | manifest (committed; paths external) | REPRO-01 |
| `tests/scripts/test_run_benchmark.py` | CI-safe test | REPRO-01 |
| YOLOX-M @640 ONNX + labels mirrored to `gs://deep-ego-model-training/.../eval/final-comparison-640/yolox_m/` | GCS objects | REPRO-01 / SAFE-04 gap |
| `docs/provenance/gcs-manifest.md` (mirror record) | doc | REPRO-01 |
</output>
