---
phase: 07-reports-docs
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - benchmarks/basketball/reports/VLM_VS_FINETUNED.md
  - docs/methodology.md
  - README.md
autonomous: true
requirements: [REPORT-03, REPORT-04, REPORT-05]
user_setup: []

must_haves:
  truths:
    - "VLM_VS_FINETUNED.md presents zero-shot VLMs vs fine-tuned detectors under the shared protocol, with a per-class failure analysis calling out the rim collapse and the zero-AP ball/referee cases — computed (not transcribed) from the committed results/vlm/*.json via the generator (REPORT-03)"
    - "Every VLM table is generator-injected between TABLE markers from results/vlm/*.json (recomputed through compute_metrics with the merged5 id_to_name), and generate_report.py --report vlm_vs_finetuned --check exits 0 (REPORT-01, REPORT-03)"
    - "docs/methodology.md documents train-matched preprocessing, protocol parity across detectors and VLMs, the single de-transform back to original pixels, and the 94-image statistical limitation (REPORT-04)"
    - "README.md replaces the stale 'scaffold' status with a harness overview and a stated reproduction path (clone → fetch weights from the registry → run the benchmark → reproduce the published table) plus the weight registry, linking (not duplicating) the two reports as the single source of truth (REPORT-05)"
  artifacts:
    - benchmarks/basketball/reports/VLM_VS_FINETUNED.md
    - docs/methodology.md
    - README.md
  key_links:
    - "the VLM per-class table is emitted by load_vlm_metrics → vlm_per_class_table (Plan 07-02), which recomputes AP with resolve_taxonomy('merged5') id_to_name so the rim/ball/referee rows are class-name keyed and correct (REPORT-03, Pitfall 2)"
    - "methodology.md and README carry NO duplicated data tables — they link to benchmarks/basketball/reports/*.md so there is only ever one generated copy of each number (REPORT-01, REPORT-04, REPORT-05)"
---

<objective>
Ship the remaining three Phase 7 documents: `benchmarks/basketball/reports/VLM_VS_FINETUNED.md`
(REPORT-03, zero-shot vs fine-tuned with per-class failure analysis), `docs/methodology.md`
(REPORT-04, train-matched preprocessing / protocol parity / de-transform / 94-image limitation),
and `README.md` (REPORT-05, harness overview + reproduction path + weight registry). The VLM
report's tables are generator-injected from the committed `results/vlm/*.json`; methodology and
README are pure prose that link to the two reports rather than duplicating any number.

Purpose: REPORT-03 completes the blog-companion pair; REPORT-04 gives the methodology a citable
home; REPORT-05 turns the stale "scaffold" README into the reproduction on-ramp a reader lands on.
Keeping methodology/README table-free (link-only) is the simplest way to preserve REPORT-01 — one
generated copy of every number, no drift surface. (REPORT-03, REPORT-04, REPORT-05)
Output: the VLM report, the extended methodology doc, and the rewritten README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-reports-docs/07-RESEARCH.md
@.planning/phases/07-reports-docs/07-02-SUMMARY.md
@CLAUDE.md
@docs/methodology.md
@README.md
@src/object_detection_eval/registry/download.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: VLM_VS_FINETUNED.md — shared-protocol comparison + per-class failure analysis</name>
  <read_first>
    - .planning/phases/07-reports-docs/07-02-SUMMARY.md: the vlm_vs_finetuned marker names (vlm_summary, vlm_per_class) and how the generator recomputes AP from results/vlm/*.json.
    - .planning/phases/05-zero-shot-vlm/05-03-SUMMARY.md: the box-run zero-shot numbers (Gemini 0.2497, OWLv2 0.2324, OmDet-Turbo 0.1724, Grounding DINO 0.1471, Florence-2 0.1056; SmolVLM2 0.0) and the shared-protocol description — PROSE reference; numbers come from the generator.
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT.md (grep the VLM per-class section, ~L410-530): the SHAPE of the failure finding (rim collapse; zero-AP ball/referee for two methods) — a sanity-check for the narrative only, NOT a source of numbers (Anti-Pattern: do not transcribe from this superseded file).
  </read_first>
  <files>benchmarks/basketball/reports/VLM_VS_FINETUNED.md</files>
  <action>Author VLM_VS_FINETUNED.md: open by framing the question (how far does zero-shot get under the exact same ground truth, taxonomy, de-transform, and scorer as the fine-tuned detectors — the shared protocol that makes the comparison fair), then a hand-authored contrast of the zero-shot ceiling against the fine-tuned @640 results (link to FINAL_COMPARISON_640.md for the detector numbers rather than re-tabulating them). Place an empty <!-- TABLE:vlm_summary START/END --> marker for the per-VLM mAP@50:95 summary and an empty <!-- TABLE:vlm_per_class START/END --> marker for the per-class AP@50 breakdown. Write the per-class failure analysis as prose around the per-class table: the rim collapse and the zero-AP ball and referee cases for the weaker methods, with the interpretation (open-vocab VLMs miss small/domain-specific classes) — the numbers in the table are recomputed by the generator from results/vlm/*.json via compute_metrics with the merged5 id_to_name (Pitfall 2), so the class rows are correctly named. Note SmolVLM2 produced no on-target detections. Then run the generator to inject both VLM tables. Type no table number by hand.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python scripts/generate_report.py --report vlm_vs_finetuned --write && /Users/ortizeg/.pixi/bin/pixi run python scripts/generate_report.py --report vlm_vs_finetuned --check && /Users/ortizeg/.pixi/bin/pixi run python -c "import pathlib; t=pathlib.Path('benchmarks/basketball/reports/VLM_VS_FINETUNED.md').read_text(); assert 'TABLE:vlm_summary' in t and 'TABLE:vlm_per_class' in t; assert 'rim' in t and 'referee' in t; print('vlm report OK')"</automated>
  </verify>
  <done>The VLM report contrasts zero-shot vs fine-tuned under the shared protocol, carries both generator-injected VLM tables recomputed from results/vlm/*.json, and its per-class prose calls out the rim collapse and the zero-AP ball/referee cases; --check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: docs/methodology.md — preprocessing, protocol parity, de-transform, 94-image limitation</name>
  <read_first>
    - docs/methodology.md (in context): the existing content (supervision drift, the variant-selection resolution) — extend it, do not rewrite; the preprocessing/protocol/de-transform/limitation material is additive.
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT_FINAL.md §5 (Methodology) and §1 capacity caveat — PROSE reference for the train-matched-preprocessing and 94-image-limitation narrative.
  </read_first>
  <files>docs/methodology.md</files>
  <action>Extend docs/methodology.md with the REPORT-04 methodology narrative (additive to the existing supervision-drift and variant-selection sections): (1) train-matched preprocessing — each model is evaluated with the letterbox/normalization/channel-order it was trained with, driven by its model-card preprocessing block, and why mismatches (not architecture) drive the headline swings; (2) protocol parity — detectors and VLMs share the same ground truth, taxonomy (merged5), de-transform, and scorer, so the zero-shot vs fine-tuned comparison is apples-to-apples; (3) the single de-transform — detections map back to original-image pixels through one tested function before scoring; (4) the 94-image statistical limitation — the test split is small, which is why the paired image-level bootstrap CIs (and the ties they expose) matter and why single-number rankings are read with care. Reference (link) the two reports for the concrete tables rather than duplicating any number. Prose only — no data tables.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python -c "import pathlib; t=pathlib.Path('docs/methodology.md').read_text().lower(); assert 'de-transform' in t or 'de transform' in t; assert '94' in t; assert 'protocol parit' in t or 'protocol parity' in t; assert 'train-matched' in t or 'train matched' in t; print('methodology OK')" && /Users/ortizeg/.pixi/bin/pixi run lint</automated>
  </verify>
  <done>methodology.md documents train-matched preprocessing, protocol parity, the de-transform, and the 94-image limitation, additive to the existing content, linking the reports for tables and duplicating no number.</done>
</task>

<task type="auto">
  <name>Task 3: README.md — harness overview + reproduction path + weight registry</name>
  <read_first>
    - README.md (in context): the current "Status: scaffold" banner (stale, must be replaced) and the existing preprocessing framing to keep.
    - src/object_detection_eval/registry/download.py (in context): download_weights() and WeightsNotRedistributableError — the public weight-fetch API the reproduction path describes; the README's PUBLIC path is clone → fetch weights from the registry → run the benchmark → reproduce the table (NOT the source-repo --source-repo/--yolox-root dev flags, which are a dev/CI-mirror convenience per 07-RESEARCH.md State of the Art).
    - registry/ (the model cards — 8 redistributable + 2 AGPL) for the weight-registry description.
  </read_first>
  <files>README.md</files>
  <action>Rewrite README.md's status/overview: remove the "Status: scaffold ... nothing below is wired up" banner and describe the harness as it now is — one evaluation protocol applied to fine-tuned detectors and zero-shot VLMs, built on the train-matched-preprocessing finding, validated against a COCO reference run. Add a reproduction-path section stating the public steps: clone the repo, fetch verified weights from the model registry via download_weights() (noting AGPL models raise WeightsNotRedistributableError and point to reproduction docs — binaries are not redistributed), run scripts/run_benchmark.py, and reproduce the published @640 table. Add a weight-registry subsection (the registry/ cards: 8 with SHA-256-verified weights on the HF Hub, 2 AGPL cards with reproduction instructions). Link to benchmarks/basketball/reports/FINAL_COMPARISON_640.md and VLM_VS_FINETUNED.md as the single source of truth for the numbers (do not paste any table into the README). Keep it prose + commands; no data tables. If the HF-Hub weight availability cannot be smoke-tested (download_weights() against a real card), state the reproduction path using the documented registry flow and note weight availability as the one external precondition, rather than asserting a specific downloaded artifact.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python -c "import pathlib; t=pathlib.Path('README.md').read_text(); tl=t.lower(); assert 'scaffold' not in tl; assert 'download_weights' in t or 'registry' in tl; assert 'run_benchmark' in t; assert 'FINAL_COMPARISON_640.md' in t and 'VLM_VS_FINETUNED.md' in t; print('README OK')"</automated>
  </verify>
  <human-check>
    Read the rendered README and both linked reports (end-of-phase human verify): the reproduction path (clone → fetch weights → run → reproduce) is followable and accurate, the weight-registry description matches registry/, the stale scaffold banner is gone, and the reports open correctly via their links.
  </human-check>
  <done>README describes the harness, a followable clone → fetch weights → run → reproduce path, and the weight registry, links both reports as the single source of truth, drops the scaffold banner, and duplicates no table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed results/vlm JSON → generated VLM tables | per-class AP crosses from prediction dumps into a published document |
| README reproduction claim → external reader | a reader follows the stated path expecting it to work |
| methodology/README prose ↔ report tables | link-only, to avoid a second copy of any number |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Tampering (of trust) | VLM per-class numbers transcribed from the superseded EVAL_REPORT.md | high | mitigate | Tables recomputed by the generator from results/vlm/*.json via compute_metrics with merged5 id_to_name; the source file is a narrative-shape reference only (Pitfall 2, Anti-Pattern) |
| T-07-12 | Tampering (of trust) | a hand-typed number in methodology/README drifting from the data | medium | mitigate | methodology and README carry no data tables; they link the two generated reports so there is one copy of every number (REPORT-01) |
| T-07-13 | Repudiation | README stating a reproduction path that does not actually run | medium | mitigate | The path uses the documented download_weights()/run_benchmark flow; where HF-Hub weight availability is unverified, it is stated as the one external precondition rather than asserted, and confirmed at the end-of-phase human read-through |
</threat_model>

<artifacts>
Artifacts this plan produces:
- `benchmarks/basketball/reports/VLM_VS_FINETUNED.md` — REPORT-03 zero-shot vs fine-tuned report (authored prose + generator-injected VLM tables)
- `docs/methodology.md` — extended with REPORT-04 methodology narrative
- `README.md` — REPORT-05 harness overview + reproduction path + weight registry
</artifacts>

<verification>
- `generate_report.py --report vlm_vs_finetuned --check` exits 0.
- Both VLM table markers present and filled; per-class prose names rim/ball/referee failures.
- methodology.md covers train-matched preprocessing, protocol parity, de-transform, 94-image limitation.
- README drops the scaffold banner, states the reproduction path + registry, and links both reports.
- `pixi run lint` clean; end-of-phase human read-through of both reports + README (manual-only per human_verify_mode: end-of-phase).
</verification>

<success_criteria>
VLM_VS_FINETUNED.md presents zero-shot vs fine-tuned under the shared protocol with a generator-computed per-class failure analysis (rim collapse, zero-AP ball/referee); methodology.md documents preprocessing/protocol-parity/de-transform/94-image limitation; README carries the harness overview, the clone → fetch → run → reproduce path, and the weight registry, linking the two reports as the single source of truth. (REPORT-03, REPORT-04, REPORT-05)
</success_criteria>

<output>
Create `.planning/phases/07-reports-docs/07-04-SUMMARY.md` when done, noting the VLM report tables, the methodology additions, and the README reproduction path (and any HF-Hub weight-availability caveat surfaced).
</output>
