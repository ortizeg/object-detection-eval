---
phase: 07-reports-docs
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - benchmarks/basketball/reports/FINAL_COMPARISON_640.md
autonomous: true
requirements: [REPORT-02]
user_setup: []

must_haves:
  truths:
    - "FINAL_COMPARISON_640.md leads with the preprocessing finding (YOLOX-M 30.8 → 72.3, YOLO26m 48.9 → 71.6 on identical weights) and states the YOLOX-M/YOLO26m joint-best tie up front, not in an appendix (REPORT-02)"
    - "Every numeric table in the report sits between TABLE markers and is generator-emitted from a results file — the 7-model primary table, the CI table, and the 5-class per-class AP table (with the 10-class table as an appendix/callout) — no number is hand-transcribed (REPORT-01, REPORT-02)"
    - "The pairwise-significance claim reads '5 of 6 adjacent pairs significant; RTMDet-M vs DAMO-YOLO-M is a statistical tie', derived from the bootstrap file's ci_excludes_zero column — never 'every adjacent pair is significant' (REPORT-02, Pitfall 4)"
    - "The report carries the fairness audit (per-method handicaps) as hand-authored prose and a §6 latency section whose table is captioned with the verbatim honest-label and the 4.0–7.1 ms source band as the headline figure (REPORT-02, Pitfall 1)"
    - "scripts/generate_report.py --check on the committed report exits 0 (report matches its results files) (REPORT-01, REPORT-02)"
  artifacts:
    - benchmarks/basketball/reports/FINAL_COMPARISON_640.md
  key_links:
    - "the report's tables are injected by generate_report.py --report final_comparison --write, reading results/accuracy/reproduction_640_{merged5,raw10}.json, results/accuracy/bootstrap_7models.json, and results/latency/trt_fp16_toboxes.json (REPORT-01)"
    - "the fairness-audit and preprocessing-lede prose is hand-authored around the markers and survives --write byte-for-byte (marker injection touches only table interiors) (REPORT-02)"
---

<objective>
Author `benchmarks/basketball/reports/FINAL_COMPARISON_640.md` (REPORT-02): the blog-companion
report that leads with the preprocessing finding, carries the 7-model @640 comparison with
confidence intervals and per-class AP, states the YOLOX-M/YOLO26m tie up front, includes the
fairness audit and the honest-labeled latency section, and corrects the over-claimed pairwise
significance to "5 of 6 adjacent pairs significant". All numeric tables are inserted by the Plan
07-02 generator between `<!-- TABLE:name -->` markers from the committed results files; only the
narrative is hand-authored.

Purpose: this is the primary deliverable a blog post links to. Its credibility rests on REPORT-01
(every table traceable to committed data) and on carrying the corrections Phases 4/6 already
landed — the RTMDet-M/DAMO-YOLO-M tie and the latency honest-label — rather than copy-forwarding
the source report's confounded claims. (REPORT-02)
Output: `benchmarks/basketball/reports/FINAL_COMPARISON_640.md` with generated tables + authored
prose, `--check`-clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-reports-docs/07-RESEARCH.md
@.planning/phases/07-reports-docs/07-02-SUMMARY.md
@CLAUDE.md
@docs/methodology.md
@benchmarks/basketball/results/latency/trt_fp16_toboxes.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Report skeleton + preprocessing lede + accuracy tables (primary, CI, 5c per-class)</name>
  <read_first>
    - .planning/phases/07-reports-docs/07-02-SUMMARY.md: the marker names the generator registry expects for final_comparison (primary_7model, ci_table, per_class_5c, per_class_10c, latency_section) and how to run --write / --check.
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT_FINAL.md §1, §2, §2-CIs, §3 (the source content for the capacity caveat, the primary table framing, the CI presentation, and the 5-class per-class narrative — use as PROSE reference; all numbers come from the committed results files via the generator, NOT from this file).
    - .planning/phases/04-reproduction-gate/04-03-SUMMARY.md: the exact corrected significance framing (5 of 6 significant; RTMDet-M vs DAMO-YOLO-M tie, CI straddles zero) to state in prose.
  </read_first>
  <files>benchmarks/basketball/reports/FINAL_COMPARISON_640.md</files>
  <action>Author FINAL_COMPARISON_640.md leading with the preprocessing finding: open with the train-matched-preprocessing story (YOLOX-M 30.8 → 72.3, YOLO26m 48.9 → 71.6 on identical weights, corrected by evaluating each model with the preprocessing it was trained with and de-transforming detections back to original pixels), then immediately state the headline: YOLOX-M and YOLO26m are joint-best (a statistical tie), not a single winner. Include the capacity caveat prominently (medium-model comparison). Place empty marker pairs for the generated tables in reading order: <!-- TABLE:primary_7model START/END --> (the 7-model @640 table), <!-- TABLE:ci_table START/END --> (95% CIs, paired image-level bootstrap), and <!-- TABLE:per_class_5c START/END --> (5-class per-class AP@50), each with a hand-authored sentence introducing it. Write the pairwise-significance sentence as "5 of 6 adjacent pairs are significant; RTMDet-M vs DAMO-YOLO-M is a statistical tie (CI straddles zero)" — sourced from the bootstrap ci_excludes_zero data the CI table renders (Pitfall 4). Then run the generator to inject the three accuracy tables from the committed results files. Do NOT type any table number by hand.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python scripts/generate_report.py --report final_comparison --write && /Users/ortizeg/.pixi/bin/pixi run python scripts/generate_report.py --report final_comparison --check</automated>
  </verify>
  <done>The report opens on the preprocessing finding and states the YOLOX-M/YOLO26m tie up front; the primary, CI, and 5-class per-class tables are generator-injected between markers from the committed results files; the significance prose says 5 of 6 with the RTMDet-M/DAMO-YOLO-M tie; --check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Fairness audit + 10-class appendix + §6 latency section (honest-label)</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/EVAL_REPORT_FINAL.md §5a (fairness audit — per-method handicaps to state), §4 (10-class per-class narrative: DETR-family wins the 10-class task; player-layup-dunk has zero support), §6 (latency framing) — PROSE reference only.
    - benchmarks/basketball/results/latency/trt_fp16_toboxes.json (in context): reproducibility.label / source_band_ms_2026_07_21 / second_run.outcome — the honest-label the latency_section renderer captions with; the report prose must NOT claim the second-T4 run "confirms" the band (it confirms the method, not the absolute numbers; Pitfall 1).
    - .planning/phases/06-latency/06-03-SUMMARY.md: the LAT-04 honest-label resolution wording.
  </read_first>
  <files>benchmarks/basketball/reports/FINAL_COMPARISON_640.md</files>
  <action>Append to the report: (1) a hand-authored fairness-audit section stating the per-method handicaps checked (so a reader sees no model was silently advantaged/disadvantaged); (2) a 10-class per-class AP section as an appendix/collapsed callout with a one-paragraph framing (DETR-family leads the fine-grained 10-class task while YOLO26m's ball/number recall leads the coarse 5-class task) and an empty <!-- TABLE:per_class_10c START/END --> marker — the renderer emits an em dash for player-layup-dunk (zero test support), which the prose notes explicitly rather than reading as a failure (Pitfall 3); (3) a §6 latency section introducing an empty <!-- TABLE:latency_section START/END --> marker, with prose that presents the 4.0–7.1 ms fp16 to-boxes band as the published headline and explicitly carries the "manually measured 2026-07-21, not reproducible from this repo" caveat — framing the second-T4 run as confirming the METHOD, not porting the absolute latency (Pitfall 1). Re-run the generator to inject the 10-class and latency tables. Keep all prose outside the markers; type no table number by hand.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python scripts/generate_report.py --report final_comparison --write && /Users/ortizeg/.pixi/bin/pixi run python scripts/generate_report.py --report final_comparison --check && /Users/ortizeg/.pixi/bin/pixi run python -c "import pathlib; t=pathlib.Path('benchmarks/basketball/reports/FINAL_COMPARISON_640.md').read_text(); assert 'manually measured 2026-07-21, not reproducible from this repo' in t; assert all(m in t for m in ['TABLE:primary_7model','TABLE:ci_table','TABLE:per_class_5c','TABLE:per_class_10c','TABLE:latency_section']); print('report sections OK')"</automated>
  </verify>
  <human-check>
    Read the rendered FINAL_COMPARISON_640.md end to end (end-of-phase human verify): the preprocessing lede and YOLOX-M/YOLO26m tie read correctly and up front; the fairness-audit prose is accurate; the §6 latency caption carries the honest-label and presents 4.0–7.1 ms as the headline (not the second-T4 numbers); the significance prose says 5 of 6 with the RTMDet-M/DAMO-YOLO-M tie.
  </human-check>
  <done>The report carries the fairness audit, the 10-class appendix with player-layup-dunk as an em dash, and the honest-labeled §6 latency section; all five table markers are present and generator-filled; --check exits 0; the honest-label string is present verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed results JSON → generated report tables | numbers cross from data into a published document |
| hand-authored prose ↔ machine-owned table regions | two edit surfaces coexist in one committed .md |
| published report → blog reader | the report is the credibility surface a reviewer attacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering (of trust) | a hand-typed table number drifting from the results file | high | mitigate | Every table is generator-injected between markers; --check gates the committed report; no numeric literal is authored into a table by hand |
| T-07-09 | Repudiation | copy-forwarding the source report's "every adjacent pair significant" over-claim | medium | mitigate | Significance is stated as 5 of 6 with the RTMDet-M/DAMO-YOLO-M tie, derived from the bootstrap ci_excludes_zero column, matching the Phase 4 correction (Pitfall 4) |
| T-07-10 | Repudiation | presenting the latency band as freshly reproduced | high | mitigate | The §6 caption carries the verbatim honest-label and presents the 4.0–7.1 ms source band as the headline, framing the second-T4 run as method-confirming only (Pitfall 1) |
</threat_model>

<artifacts>
Artifacts this plan produces:
- `benchmarks/basketball/reports/FINAL_COMPARISON_640.md` — REPORT-02 blog-companion report (authored prose + generator-injected tables)
</artifacts>

<verification>
- `generate_report.py --report final_comparison --check` exits 0 (report matches committed results).
- The five table markers (primary_7model, ci_table, per_class_5c, per_class_10c, latency_section) are present and filled.
- The verbatim honest-label string is present in the §6 latency section.
- End-of-phase human read-through confirms the lede, the tie, the fairness audit, and the honest-label framing (manual-only per human_verify_mode: end-of-phase).
</verification>

<success_criteria>
FINAL_COMPARISON_640.md leads with the preprocessing finding, states the YOLOX-M/YOLO26m tie up front, and carries the 7-model comparison with CIs, per-class AP (5c body + 10c appendix), the fairness audit, and the honest-labeled latency section — every table generated from a committed results file, the significance corrected to 5 of 6, and --check clean. (REPORT-02)
</success_criteria>

<output>
Create `.planning/phases/07-reports-docs/07-03-SUMMARY.md` when done, noting the section order, the five injected tables, and the corrections carried (5-of-6 significance, latency honest-label).
</output>
