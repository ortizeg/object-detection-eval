---
phase: 05-zero-shot-vlm
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/object_detection_eval/inference/vlm/filters.py
  - scripts/run_vlm_benchmark.py
  - benchmarks/basketball/conf/vlm_zeroshot.yaml
  - tests/inference/vlm/test_filters.py
  - tests/scripts/test_run_vlm_benchmark.py
autonomous: false
requirements: [VLM-01, VLM-02, VLM-04]

user_setup:
  - service: vast.ai GPU box
    why: "The 5 HF VLMs download weights and run inference on an RTX 4090; the run is not CI-wired"
    dashboard_config:
      - task: "Provision + rsync repo to /root/object-detection-eval; create /root/vlmenv (python3.11, torch+cu121, transformers>=4.49<4.52, timm, einops, peft, supervision==0.29.1)"
        location: "ssh -p 12120 root@ssh7.vast.ai"
      - task: "Stage the 94-image basketball test set + GT COCO json to the box (from source repo .deploy_comparison/basketball-data.tar.gz or data/basketball-player-detection-3)"
        location: "box: /root/data/basketball/test/"
  - service: gemini
    why: "The Gemini 26.5 row makes 94 billed API calls; the 5 HF rows do not need it"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> API keys (confirmed with the user separately; never committed)"

must_haves:
  truths:
    - "Each of the six VLMs is scored over the 94-image basketball test split through the SAME load_coco_gt + resolve_taxonomy(merged5) + remap_detections + compute_metrics path as the detectors, producing a predictions results file of the same shape load_predictions reads (VLM-01)"
    - "On the box, the five HF VLMs reproduce their published test mAP@50:95 within tolerance (Gemini 26.5, OWLv2 24.7, OmDet-Turbo 17.3, Grounding DINO 14.7, Florence-2 10.4); SmolVLM2 runs and emits a results file with no target asserted (VLM-02)"
    - "The two VLM-only filters (single-best-per-class for ball/rim, area-outlier) are torch-free, unit-tested, and applied to VLM detections before scoring (VLM-01)"
    - "Default CI stays green: the filter + manifest tests run torch-free; the script-import tests are marked vlm and deselected (VLM-04)"
  artifacts:
    - src/object_detection_eval/inference/vlm/filters.py
    - scripts/run_vlm_benchmark.py
    - benchmarks/basketball/conf/vlm_zeroshot.yaml
  key_links:
    - "merged5.yaml already carries the COCO/VLM prompt aliases (person->player, sports ball->ball, basketball player->player, hoop->rim, jersey number->number) so remap_detections maps VLM labels into the 5-class eval space with no code edit (VLM-01)"
    - "run_vlm_benchmark writes predictions as filename -> [{bbox_xyxy, class_id, confidence}] (pixel, eval-id space) so the output loads through metrics.bootstrap.load_predictions and scores identically to detector predictions (VLM-01/VLM-02)"
    - "filters.py lives under inference/vlm/ but imports only stdlib + schemas.detection — torch-free, so its test runs in default CI (VLM-04)"
---

<objective>
Build the zero-shot VLM run-and-reproduce path: a committed manifest of the six VLMs (model ids, thresholds, the Gemini prompt, expected test mAP@50:95, tolerance), the two VLM-only detection filters ported from the source eval task, and `scripts/run_vlm_benchmark.py` that runs each VLM over the 94-image basketball test split through the identical scorer as the detectors, writes same-shape predictions results files, and asserts the published zero-shot numbers within a stated tolerance. The five HF VLMs run on the provisioned GPU box without a key; the Gemini row is a separable, key-gated step.

Purpose: This is where protocol parity (VLM-01) and the published-ceiling reproduction (VLM-02) are proven. The actual GPU run is executor-run on the box and precondition-gated — never wired into CI — mirroring Phase 4's full runs. CI covers only the torch-free filter logic and the offline manifest/gate helpers. (VLM-01, VLM-02, VLM-04)
Output: `inference/vlm/filters.py`, `scripts/run_vlm_benchmark.py`, `benchmarks/basketball/conf/vlm_zeroshot.yaml`, two offline tests, and the six committed results files (via the box run at the checkpoint).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@scripts/run_benchmark.py
@src/object_detection_eval/data/taxonomy.py
@src/object_detection_eval/metrics/detection_map.py
@src/object_detection_eval/metrics/bootstrap.py
@src/object_detection_eval/data/coco_gt.py
@benchmarks/basketball/conf/taxonomy/merged5.yaml
@benchmarks/basketball/conf/reproduction_640.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VLM-only detection filters (torch-free, unit-tested)</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/tasks/eval_detection_task.py (the two DEFERRED filters: _filter_single_best_per_class around L1100 and _filter_area_outliers around L1137)
    - src/object_detection_eval/schemas/detection.py (Detection.bbox is normalised xywh; area = w*h)
  </read_first>
  <files>src/object_detection_eval/inference/vlm/filters.py, tests/inference/vlm/test_filters.py</files>
  <behavior>
    - single_best_per_class: for singleton eval classes (ball=1, rim=3 by default), keep only the highest-confidence detection; all other classes pass through unchanged; order-independent input yields the same kept set.
    - area_outliers: drop any detection whose normalised area (bbox.w * bbox.h) exceeds max_area_fraction (default 0.05); boxes exactly at the threshold are kept.
    - Empty input returns empty; neither filter mutates its input list.
  </behavior>
  <action>Port `_filter_single_best_per_class` and `_filter_area_outliers` as two module-level pure functions in `src/object_detection_eval/inference/vlm/filters.py`, operating on `list[Detection]` and importing ONLY stdlib + loguru + `object_detection_eval.schemas.detection` (no torch, no transformers — this keeps the module importable in default CI per VLM-04). Keep the source signatures and defaults: `single_best_per_class(detections, single_class_ids=frozenset({1, 3}))` and `area_outliers(detections, max_area_fraction=0.05)`. Write `tests/inference/vlm/test_filters.py` (NOT marked vlm — it must run in default torch-free CI) covering the behaviors above, including the singleton-dedup, the area threshold boundary (== threshold kept), and pass-through of non-singleton classes.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/inference/vlm/test_filters.py --no-cov -q</automated>
  </verify>
  <done>Both filters pass unit tests in the default (torch-free) environment; filters.py imports no torch.</done>
</task>

<task type="auto">
  <name>Task 2: VLM manifest + run_vlm_benchmark.py + offline gate/manifest tests</name>
  <read_first>
    - scripts/run_benchmark.py (the sibling REPRO-01 gate — mirror its Manifest/ManifestEntry pydantic pattern, _assert_preconditions per-path halting, within_tolerance + rank helpers, and the load_coco_gt -> per-image predict -> remap_detections -> detections_to_sv -> compute_metrics loop)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/conf/task/eval_detection.yaml (per-VLM thresholds: owlv2 box 0.01 / nms 0.3; grounding_dino box+text 0.01 / nms 0.5; omdet box 0.01; florence2 task "<OD>"; the inline Gemini prompt + gemini_classes)
    - tests/scripts/test_run_benchmark.py (the CI-safe offline coverage pattern for a not-wired script)
  </read_first>
  <files>benchmarks/basketball/conf/vlm_zeroshot.yaml, scripts/run_vlm_benchmark.py, tests/scripts/test_run_vlm_benchmark.py</files>
  <action>Create `benchmarks/basketball/conf/vlm_zeroshot.yaml`: one entry per VLM (gemini, owlv2, omdet_turbo, grounding_dino, florence2, smolvlm2) carrying its inferencer key, HF model id (or Gemini model name), per-model thresholds/prompt from eval_detection.yaml, and `expected_map5095` for the five with published numbers (gemini 0.265, owlv2 0.247, omdet_turbo 0.173, grounding_dino 0.147, florence2 0.104); smolvlm2 has NO expected value (an optional/null field — it runs and emits a results file only, no target asserted, per VLM-02). Include a `tolerance` (reasonable for VLM noise — state it explicitly in a comment, e.g. 0.02; a divergence beyond it is a defect to investigate, not a number to widen). Write `scripts/run_vlm_benchmark.py` mirroring run_benchmark.py's structure: pydantic Manifest/Entry, per-path `_assert_preconditions`, a lazy factory dict importing each inferencer from its own `inference.vlm.*` submodule (import inside the factory so the module is importable without torch when only inspecting the manifest), and for each model run predict over the 94 test images, then apply the pipeline in the SOURCE order (BLOCKER-3 fix — order matters): `remap_detections` via `resolve_taxonomy("merged5")` FIRST, then `filters.area_outliers`, then `filters.single_best_per_class`. This ordering is required because `single_best_per_class`'s default `single_class_ids=frozenset({1, 3})` means ball,rim in the POST-remap merged5 eval space (merged5.yaml: [player,ball,referee,rim,number]); filtering before remap would apply {1,3} to arbitrary raw VLM label indices and silently corrupt VLM-02. Then write a results file `benchmarks/basketball/results/vlm/{name}.json` in load_predictions shape (filename -> [{bbox_xyxy, class_id, confidence}]), then `compute_metrics` and compare to `expected_map5095` within tolerance (skip the assert for smolvlm2). Provide `--only <name>` to run a single row (so the Gemini row is separable) and `--data-root`/`--providers` args. This script is NOT wired into pytest (reads external weights + images). Write `tests/scripts/test_run_vlm_benchmark.py` mirroring test_run_benchmark.py. BLOCKER-1 fix — if the test imports the script module (which references the vlm inferencer submodules / torch transitively), lead it with `pytest.importorskip("torch")` BEFORE that import so default-CI collection is safe; mark it `pytest.mark.vlm`. Cover the manifest shape (six entries, smolvlm2 expected is null), the within_tolerance/rank helpers, and the null-target skip logic — all offline, no weights. (If the manifest/helpers can be tested WITHOUT importing torch-referencing code, a torch-free unmarked test is even better — but any module that imports the script must be importorskip-guarded.)</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e vlm pytest tests/scripts/test_run_vlm_benchmark.py -m vlm --no-cov -q</automated>
  </verify>
  <done>Manifest validates with six entries (smolvlm2 target null); gate helpers + skip logic tested offline; `python scripts/run_vlm_benchmark.py --help` runs without importing torch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Box-run reproduction of the six-VLM zero-shot ceiling</name>
  <what-built>The committed `run_vlm_benchmark.py` + `vlm_zeroshot.yaml` reproduce the published zero-shot ceiling on the RTX 4090 box, and the six same-shape results files are committed back.</what-built>
  <precondition>GPU box reachable (ssh -p 12120 root@ssh7.vast.ai) with /root/vlmenv installed, the 94-image basketball test set + GT staged on the box, and — for the Gemini row only — GEMINI_API_KEY exported in the box shell (confirmed with the user separately).</precondition>
  <how-to-verify>
    On the box, in /root/vlmenv:
    1. Run the five HF rows (no key needed):
       `python scripts/run_vlm_benchmark.py --data-root /root/data/basketball --providers CUDAExecutionProvider` (or `--only owlv2` etc. per row).
       Confirm each prints Measured within tolerance of: OWLv2 0.247, OmDet-Turbo 0.173, Grounding DINO 0.147, Florence-2 0.104; SmolVLM2 emits a results file with no assertion.
    2. Only after the user confirms the Gemini key is set, run the Gemini row:
       `GEMINI_API_KEY=... python scripts/run_vlm_benchmark.py --only gemini --data-root /root/data/basketball`
       Confirm Measured is within tolerance of 0.265 (94 billed calls).
    3. Copy the six `benchmarks/basketball/results/vlm/{name}.json` files back to the local repo and confirm each loads via `metrics.bootstrap.load_predictions` and scores to the same number.
    A divergence beyond tolerance is a defect to investigate (prompt/threshold/preprocessing), NOT a tolerance to widen.
  </how-to-verify>
  <resume-signal>Type "approved" with the six measured numbers, or describe the divergence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → Gemini API | 94 billed calls; images leave the machine |
| HF Hub → box | Five model weight sets downloaded + executed on the rented box |
| box → local repo | Results JSON copied back and committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | Gemini API key on the box | high | mitigate | Key exported only in the box shell for the single `--only gemini` invocation, confirmed with the user first; never committed, never in the manifest |
| T-05-08 | Spoofing/Tampering | Results files copied back from a rented box | medium | mitigate | Each returned file must re-load via load_predictions and re-score to the same number locally before it is trusted/committed (checkpoint step 3) |
| T-05-09 | Tampering | HF weight supply chain | medium | mitigate | Pinned model ids in the committed manifest; no arbitrary URLs |
| T-05-10 | Denial of Service | 94-call Gemini cost/quota | low | accept | Gemini isolated behind `--only gemini`; the 5 HF rows carry the reproduction if the key is unavailable |
</threat_model>

<artifacts>
Artifacts this phase (plan) produces:
- `src/object_detection_eval/inference/vlm/filters.py` — two torch-free VLM detection filters
- `scripts/run_vlm_benchmark.py` — not-CI-wired run+reproduce script for the six VLMs
- `benchmarks/basketball/conf/vlm_zeroshot.yaml` — committed manifest (ids, thresholds, Gemini prompt, expected mAP, tolerance)
- `benchmarks/basketball/results/vlm/{gemini,owlv2,omdet_turbo,grounding_dino,florence2,smolvlm2}.json` — six same-shape results files (produced at the box-run checkpoint)
- offline tests: `tests/inference/vlm/test_filters.py`, `tests/scripts/test_run_vlm_benchmark.py`
</artifacts>

<verification>
- `pixi run pytest tests/inference/vlm/test_filters.py --no-cov` passes in the default torch-free environment.
- `pixi run -e vlm pytest tests/scripts/test_run_vlm_benchmark.py -m vlm --no-cov` passes.
- Default CI selection `-m "not vlm and not trt and not external"` still green (filters test runs, script-import test deselected).
- Box run (checkpoint): five HF rows within tolerance, Gemini within tolerance, SmolVLM2 emits a results file.
</verification>

<success_criteria>
The six VLMs score through the identical protocol as the detectors and reproduce the published test mAP@50:95 within a stated tolerance (SmolVLM2 runs without a target); results files are same-shape and reload via load_predictions; CI stays green with the extra uninstalled. (VLM-01, VLM-02, VLM-04)
</success_criteria>

<output>
Create `.planning/phases/05-zero-shot-vlm/05-03-SUMMARY.md` when done.
</output>
