---
phase: 05-zero-shot-vlm
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/object_detection_eval/inference/vlm/florence2.py
  - src/object_detection_eval/inference/vlm/smolvlm2.py
  - src/object_detection_eval/inference/vlm/gemini.py
  - tests/inference/vlm/test_florence2.py
  - tests/inference/vlm/test_smolvlm2.py
  - tests/inference/vlm/test_gemini.py
autonomous: true
requirements: [VLM-01, VLM-04]

must_haves:
  truths:
    - "Florence-2 (<OD>), SmolVLM2, and Gemini each subclass BaseInferencer and return list[Detection] in the shared normalised-xywh schema (VLM-01)"
    - "Florence-2 and SmolVLM2 have offline tests that mock transformers + torch and pass under -m vlm; Gemini has an offline test that mocks google.genai and is additionally marked external (VLM-04)"
    - "GeminiInferencer reads its API key only from GEMINI_API_KEY/GOOGLE_API_KEY at construction and never hardcodes or logs it"
  artifacts:
    - src/object_detection_eval/inference/vlm/florence2.py
    - src/object_detection_eval/inference/vlm/smolvlm2.py
    - src/object_detection_eval/inference/vlm/gemini.py
  key_links:
    - "Gemini returns text labels + 0-1000 xyxy boxes; the port maps labels to class ids case-insensitively and converts to 0-1 xywh Detection so it scores through the identical pipeline as the detectors (VLM-01)"
    - "None of the three are re-exported from inference/vlm/__init__.py — the package stays torch/genai-free for CI (VLM-04)"
---

<objective>
Port the two generative HuggingFace VLMs — Florence-2 (`<OD>` task) and SmolVLM2-2.2B — plus the external Gemini inferencer into `src/object_detection_eval/inference/vlm/`, each conforming to `BaseInferencer` and returning `list[Detection]`. Florence-2 and SmolVLM2 parse model *text* output into boxes; Gemini calls `google.genai` with a JSON response schema and is credential-gated. Each ships an offline test (mocked model/API) marked `vlm`; the Gemini test is additionally marked `external`.

Purpose: Completes the six-inferencer set (with plan 05-01's three) so wave 3 can run all six through the shared scorer. Gemini is separated as a credential-gated row: its inferencer raises a clear error when no key is set, and its tests never require a live key. (VLM-01, VLM-04)
Output: `inference/vlm/{florence2,smolvlm2,gemini}.py` + three offline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@src/object_detection_eval/inference/base.py
@src/object_detection_eval/schemas/detection.py
@src/object_detection_eval/utils/boxes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Florence-2 (&lt;OD&gt;) inferencer</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/florence2_inferencer.py (source Florence-2 — AutoModelForCausalLM + AutoProcessor, "<OD>" task token, post_process_generation parsing to bboxes+labels; einops-dependent)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_florence2_inferencer.py
    - src/object_detection_eval/inference/vlm/owlv2.py (sibling from 05-01 — shared imports/return shape)
  </read_first>
  <files>src/object_detection_eval/inference/vlm/florence2.py, tests/inference/vlm/test_florence2.py</files>
  <action>Port `Florence2Inferencer` into `inference/vlm/florence2.py` conforming to `BaseInferencer` and returning `list[Detection]`. Preserve the source's `<OD>` task prompt, generate + `post_process_generation` decode, and its mapping of Florence's returned pixel-xyxy boxes + label strings to class ids against the class list (labels outside the taxonomy are dropped). Convert boxes to normalised xywh via `pixel_xyxy_to_normalized_xywh` and emit the shared `Detection` schema. Keep `torch`/`transformers`/`einops` imports at module top (extra-only). Do NOT add to the package `__init__`. Adapt the source test into `tests/inference/vlm/test_florence2.py`. BLOCKER-1 fix — lead the module with `pytest.importorskip("torch")` and `pytest.importorskip("transformers")` BEFORE importing the SUT (collection-safe in torch-free CI), then `pytestmark = pytest.mark.vlm`, patch transformers + torch in the `florence2` module namespace; assert init, a decode path yielding Detections, and that an out-of-taxonomy label is dropped.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e vlm pytest tests/inference/vlm/test_florence2.py -m vlm --no-cov -q</automated>
  </verify>
  <done>Florence-2 test passes offline under -m vlm; predict() returns list[Detection] in normalised xywh.</done>
</task>

<task type="auto">
  <name>Task 2: SmolVLM2 inferencer</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/smolvlm2_inferencer.py (source SmolVLM2 — AutoModelForImageTextToText + AutoProcessor, chat prompt, _SmolVLM2BBox/_SmolVLM2Detection JSON parsing)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_smolvlm2_inferencer.py
  </read_first>
  <files>src/object_detection_eval/inference/vlm/smolvlm2.py, tests/inference/vlm/test_smolvlm2.py</files>
  <action>Port `SmolVLM2Inferencer` (and its private `_SmolVLM2BBox` / `_SmolVLM2Detection` pydantic parse models) into `inference/vlm/smolvlm2.py` conforming to `BaseInferencer`. Preserve the chat-template prompt, `generate`, and the JSON-from-text parse that tolerates malformed output (returns [] rather than raising). Map parsed label strings to class ids case-insensitively and emit the shared `Detection` schema in normalised xywh. Keep transformers>=4.49 (`AutoModelForImageTextToText`) — note SmolVLM2 has no published reproduction target (VLM-02 covers it only as "must run", handled in 05-03). Do NOT add to the package `__init__`. Adapt the source test into `tests/inference/vlm/test_smolvlm2.py`. BLOCKER-1 fix — lead the module with `pytest.importorskip("torch")` and `pytest.importorskip("transformers")` BEFORE importing the SUT (collection-safe in torch-free CI), then `pytestmark = pytest.mark.vlm`, patch transformers + torch in the `smolvlm2` module namespace; assert init, a valid-JSON decode path, and graceful [] on unparseable text.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e vlm pytest tests/inference/vlm/test_smolvlm2.py -m vlm --no-cov -q</automated>
  </verify>
  <done>SmolVLM2 test passes offline under -m vlm, including the malformed-output-returns-empty case.</done>
</task>

<task type="auto">
  <name>Task 3: Gemini inferencer (external, credential-gated)</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/gemini_inferencer.py (source Gemini — google.genai Client, GeminiBBox/GeminiDetection response schema, retry/backoff, 0-1000 xyxy -> 0-1 xywh, label substring resolution, text fallback)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/conf/task/eval_detection.yaml (the inline Gemini prompt + gemini_classes; carry the prompt text into a default constant)
  </read_first>
  <files>src/object_detection_eval/inference/vlm/gemini.py, tests/inference/vlm/test_gemini.py</files>
  <action>Port `GeminiInferencer` (with `GeminiBBox` and `GeminiDetection`) into `inference/vlm/gemini.py` conforming to `BaseInferencer`. Preserve the response-schema config, the 0-1000 xyxy → 0-1 xywh conversion, the case-insensitive + substring `_resolve_label`, the retry/backoff on 429/503/UNAVAILABLE, and the JSON text fallback. Read the key from the environment at construction (accept BOTH GEMINI_API_KEY and GOOGLE_API_KEY; prefer GEMINI_API_KEY) and raise a clear RuntimeError naming the env vars when absent — never hardcode or log the key value. Emit the shared `Detection` schema. Do NOT add to the package `__init__`. Write `tests/inference/vlm/test_gemini.py`. BLOCKER-1 fix — lead the module with `pytest.importorskip("google.genai")` BEFORE importing the SUT (the gemini module imports `google.genai` at top; this keeps default-CI collection safe when the `[vlm]` extra is absent), then `pytestmark = [pytest.mark.vlm, pytest.mark.external]`, patch `google.genai` in the `gemini` module namespace and set a dummy key via monkeypatch.setenv; assert: construction fails with the named error when no key is set, a parsed response maps to Detections with correct xywh, and an out-of-taxonomy label is dropped. The mocked test must NOT make a network call.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e vlm pytest tests/inference/vlm/test_gemini.py -m "vlm and external" --no-cov -q</automated>
  </verify>
  <done>Gemini test passes fully offline (mocked genai) under -m "vlm and external"; missing-key construction raises a named RuntimeError; no test hits the network.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → Gemini API | Basketball images are uploaded to an external Google endpoint; a per-image billed call |
| API/model text → parser | Gemini/SmolVLM/Florence return free-form text or JSON that is parsed into Detection |
| credential store → process | GEMINI_API_KEY/GOOGLE_API_KEY is read from the environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Gemini API key | high | mitigate | Key read only from env at construction, never a constructor arg, never logged; RuntimeError names the env var, not the value |
| T-05-05 | Tampering | Free-form model text parsing (SmolVLM2/Gemini fallback) | medium | mitigate | Preserve try/except + pydantic validation that skips unparseable items and returns [] instead of raising |
| T-05-06 | Denial of Service | Gemini transient 429/503 | low | accept | Bounded exponential backoff (5 retries) already ported; beyond that the row fails cleanly and is retried by the operator |
</threat_model>

<artifacts>
Artifacts this phase (plan) produces:
- `src/object_detection_eval/inference/vlm/florence2.py`, `smolvlm2.py`, `gemini.py` — three inferencers behind BaseInferencer
- `tests/inference/vlm/test_florence2.py`, `test_smolvlm2.py`, `test_gemini.py` — offline mocked tests; Gemini additionally `external`
</artifacts>

<verification>
- `pixi run -e vlm pytest tests/inference/vlm -m vlm --no-cov` passes (all six inferencer tests, three from 05-01 + three here).
- Default CI selection `pytest -m "not vlm and not trt and not external"` selects none of these tests.
- `pixi run lint` passes with zero suppressions.
</verification>

<success_criteria>
All six zero-shot VLM inferencers now conform to BaseInferencer and return list[Detection]; each has an offline test under `-m vlm`, with Gemini additionally `external`; no VLM module is imported by the package `__init__`. (VLM-01, VLM-04)
</success_criteria>

<output>
Create `.planning/phases/05-zero-shot-vlm/05-02-SUMMARY.md` when done.
</output>
