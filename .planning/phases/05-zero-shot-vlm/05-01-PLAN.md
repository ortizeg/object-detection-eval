---
phase: 05-zero-shot-vlm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/object_detection_eval/inference/vlm/__init__.py
  - src/object_detection_eval/inference/vlm/owlv2.py
  - src/object_detection_eval/inference/vlm/grounding_dino.py
  - src/object_detection_eval/inference/vlm/omdet_turbo.py
  - tests/inference/vlm/__init__.py
  - tests/inference/vlm/test_owlv2.py
  - tests/inference/vlm/test_grounding_dino.py
  - tests/inference/vlm/test_omdet_turbo.py
autonomous: true
requirements: [VLM-01, VLM-04]

must_haves:
  truths:
    - "OWLv2, Grounding DINO, and OmDet-Turbo each subclass BaseInferencer and return list[Detection] with normalised xywh boxes and confidences (VLM-01)"
    - "Each of the three inferencers has an offline test that mocks transformers + torch and passes without downloading weights, under -m vlm (VLM-04)"
    - "The core import graph stays torch-free: importing object_detection_eval and object_detection_eval.inference.vlm pulls in no torch (VLM-04)"
  artifacts:
    - src/object_detection_eval/inference/vlm/owlv2.py
    - src/object_detection_eval/inference/vlm/grounding_dino.py
    - src/object_detection_eval/inference/vlm/omdet_turbo.py
    - src/object_detection_eval/inference/vlm/__init__.py
  key_links:
    - "vlm/__init__.py is a bare package marker with NO torch imports so the package is importable in torch-free CI; inferencers are imported from their own submodules, never re-exported from __init__ (VLM-04)"
    - "Each inferencer's predict() returns Detection objects in the SAME schema the detectors use (schemas.detection.Detection), so downstream remap_detections + compute_metrics work unchanged (VLM-01)"
---

<objective>
Port the three open-vocabulary HuggingFace zero-shot detectors — OWLv2, Grounding DINO, and OmDet-Turbo — from the source repo into `src/object_detection_eval/inference/vlm/`, behind the already-committed `[vlm]` extra, each conforming to the `BaseInferencer` ABC and returning `list[Detection]`. Establish the `inference/vlm/` package (bare, torch-free `__init__.py`) and the `tests/inference/vlm/` test package. Each inferencer ships an offline test that mocks `transformers`/`torch` and runs under `-m vlm` with no weight download.

Purpose: These three share the HuggingFace zero-shot-object-detection API shape (processor + `post_process_grounded_object_detection`, box threshold, per-class NMS). Landing them together with the package scaffold gives waves 2-3 a stable, torch-free package to extend. (VLM-01, VLM-04)
Output: `inference/vlm/{__init__,owlv2,grounding_dino,omdet_turbo}.py` + three mocked tests + the `tests/inference/vlm/` package; the torch-free-import gate still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@src/object_detection_eval/inference/base.py
@src/object_detection_eval/schemas/detection.py
@src/object_detection_eval/utils/boxes.py
@benchmarks/basketball/conf/taxonomy/merged5.yaml
@tests/test_no_torch_import.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: VLM package scaffold + OWLv2 inferencer</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/owlv2_inferencer.py (source OWLv2 — verbatim behavior to port)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_owlv2_inferencer.py (source mocked-transformers test pattern to adapt)
    - src/object_detection_eval/inference/base.py (the BaseInferencer ABC: predict(image, image_width, image_height) -> list[Detection])
    - src/object_detection_eval/utils/boxes.py (pixel_xyxy_to_normalized_xywh already exists — reuse, do not re-implement)
  </read_first>
  <files>src/object_detection_eval/inference/vlm/__init__.py, src/object_detection_eval/inference/vlm/owlv2.py, tests/inference/vlm/__init__.py, tests/inference/vlm/test_owlv2.py</files>
  <action>Create `inference/vlm/__init__.py` as a bare package marker: a module docstring only, NO imports of any inferencer and NO torch import, so importing the package is safe in torch-free CI (VLM-04 — this is why inferencers are addressed by submodule, never re-exported). Port `OWLv2Inferencer` into `inference/vlm/owlv2.py`: subclass `object_detection_eval.inference.base.BaseInferencer`, import `Detection`/`BoundingBox` from `object_detection_eval.schemas.detection`, and reuse `pixel_xyxy_to_normalized_xywh` from `object_detection_eval.utils.boxes`. Keep the source behavior intact — device auto-resolve, `Owlv2Processor`/`Owlv2ForObjectDetection` from transformers, `post_process_grounded_object_detection`, the per-class greedy NMS, and the try/except that returns [] on inference failure. Keep `torch`, `PIL`, and `transformers` imports at module top (they only load under the `[vlm]` extra; the package `__init__` never imports this module, preserving the torch-free core per VLM-04). Create `tests/inference/vlm/__init__.py` (empty package marker) and `tests/inference/vlm/test_owlv2.py`, adapting the source test: patch the transformers classes and `torch` in the `owlv2` module namespace, mark the module `pytestmark = pytest.mark.vlm`, assert initialization, a decode-to-Detection path, and NMS deduplication — all offline with no weight download.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e vlm pytest tests/inference/vlm/test_owlv2.py -m vlm --no-cov -q</automated>
  </verify>
  <done>OWLv2 test passes offline under -m vlm; `inference/vlm/__init__.py` contains no torch/inferencer imports.</done>
</task>

<task type="auto">
  <name>Task 2: Grounding DINO inferencer</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/grounding_dino_inferencer.py (source Grounding DINO — AutoProcessor + AutoModelForZeroShotObjectDetection, box + text thresholds, NMS)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_grounding_dino_inferencer.py
    - src/object_detection_eval/inference/vlm/owlv2.py (sibling written in Task 1 — match its structure and imports)
  </read_first>
  <files>src/object_detection_eval/inference/vlm/grounding_dino.py, tests/inference/vlm/test_grounding_dino.py</files>
  <action>Port `GroundingDINOInferencer` into `inference/vlm/grounding_dino.py` conforming to `BaseInferencer` and returning `list[Detection]`. Preserve the source's prompt construction (dot-joined class phrases), `box_threshold` + `text_threshold`, `post_process_grounded_object_detection`, label-string-to-class-id resolution against the class list, and per-class NMS. Import `Detection`/`BoundingBox` from `object_detection_eval.schemas.detection` and reuse `pixel_xyxy_to_normalized_xywh`. Do NOT add it to the package `__init__` (VLM-04). Adapt the source test into `tests/inference/vlm/test_grounding_dino.py` with `pytestmark = pytest.mark.vlm`, patching transformers + torch in the `grounding_dino` module namespace; assert init, a decode path producing Detections, and that a below-threshold box is dropped.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e vlm pytest tests/inference/vlm/test_grounding_dino.py -m vlm --no-cov -q</automated>
  </verify>
  <done>Grounding DINO test passes offline under -m vlm; predict() returns list[Detection] in normalised xywh.</done>
</task>

<task type="auto">
  <name>Task 3: OmDet-Turbo inferencer + torch-free-core re-verification</name>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/omdet_turbo_inferencer.py (source OmDet-Turbo — timm backbone via AutoBackbone, box threshold)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_omdet_turbo_inferencer.py
    - tests/test_no_torch_import.py (the Phase-2 gate that must stay green)
  </read_first>
  <files>src/object_detection_eval/inference/vlm/omdet_turbo.py, tests/inference/vlm/test_omdet_turbo.py</files>
  <action>Port `OmDetTurboInferencer` into `inference/vlm/omdet_turbo.py` conforming to `BaseInferencer`. Preserve the source's classes-as-text-queries input, `box_threshold`, and `post_process_grounded_object_detection` decode to `list[Detection]`; reuse `pixel_xyxy_to_normalized_xywh` and the shared `Detection` schema. `timm` is only needed at model-load time under the `[vlm]` extra — keep it out of the package `__init__`. Adapt the source test into `tests/inference/vlm/test_omdet_turbo.py` with `pytestmark = pytest.mark.vlm`, patching transformers + torch in the `omdet_turbo` module namespace. Then confirm the torch-free core is intact: `object_detection_eval` and `object_detection_eval.inference.vlm` must both import with no torch in `sys.modules`.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run -e vlm pytest tests/inference/vlm/test_omdet_turbo.py -m vlm --no-cov -q && /Users/ortizeg/.pixi/bin/pixi run pytest tests/test_no_torch_import.py --no-cov -q</automated>
  </verify>
  <done>OmDet-Turbo test passes offline; test_no_torch_import passes in the default (torch-free) environment, proving importing the vlm package does not pull in torch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF Hub → local | Model weights + processor configs are downloaded and executed on the box |
| model output → parser | Post-processed boxes/labels are untrusted structured output decoded into Detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | HF weight download (supply chain) | medium | mitigate | Pin exact HF model IDs (google/owlv2-large-patch14-ensemble, IDEA-Research/grounding-dino-base, omlab/omdet-turbo-swin-tiny-hf) in defaults; no arbitrary remote code beyond the pinned transformers classes |
| T-05-02 | Denial of Service | Malformed model output | low | mitigate | Preserve the source try/except that returns [] on decode failure; a bad box list never crashes the run |
| T-05-03 | Tampering | torch leaking into core import graph | high | mitigate | vlm/__init__.py imports nothing heavy; test_no_torch_import re-run in Task 3 proves the boundary holds (VLM-04) |
</threat_model>

<artifacts>
Artifacts this phase (plan) produces:
- `src/object_detection_eval/inference/vlm/__init__.py` — bare, torch-free package marker
- `src/object_detection_eval/inference/vlm/owlv2.py`, `grounding_dino.py`, `omdet_turbo.py` — three open-vocab inferencers behind BaseInferencer
- `tests/inference/vlm/{__init__,test_owlv2,test_grounding_dino,test_omdet_turbo}.py` — offline mocked tests, marked `vlm`
</artifacts>

<verification>
- `pixi run -e vlm pytest tests/inference/vlm -m vlm --no-cov` passes (all three inferencer tests).
- `pixi run pytest tests/test_no_torch_import.py --no-cov` passes (torch-free core preserved).
- `pixi run lint` passes with zero suppressions (T20/loguru-only).
</verification>

<success_criteria>
Three open-vocabulary VLM inferencers conform to BaseInferencer, return list[Detection], and are covered by offline mocked tests under `-m vlm`; the `inference/vlm` package is importable without torch. (VLM-01, VLM-04)
</success_criteria>

<output>
Create `.planning/phases/05-zero-shot-vlm/05-01-SUMMARY.md` when done.
</output>
