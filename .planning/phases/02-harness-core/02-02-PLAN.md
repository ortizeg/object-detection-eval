---
phase: 02-harness-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/object_detection_eval/data/__init__.py
  - src/object_detection_eval/data/coco_gt.py
  - src/object_detection_eval/data/taxonomy.py
  - src/object_detection_eval/data/image.py
  - tests/data/__init__.py
  - tests/data/test_coco_gt.py
  - tests/data/test_taxonomy.py
  - tests/data/test_image.py
  - tests/fixtures/tiny.png
autonomous: true
requirements: [CORE-01, CORE-05, CORE-09]
must_haves:
  truths:
    - "load_coco_gt(path, name_to_id) returns filename -> sv.Detections, one entry per image incl. empty images (CORE-01)"
    - "resolve_taxonomy('merged5'|'raw10') returns the YAML-driven maps; resolve_taxonomy('identity', coco) derives from the COCO categories (CORE-05)"
    - "remap_detections translates an inferencer's class ids to eval ids via name lookup, dropping unmapped classes (CORE-05)"
    - "ImageLoader.read() returns a BGR uint8 array and raises FileNotFoundError on a missing path (CORE-01)"
  artifacts:
    - src/object_detection_eval/data/coco_gt.py
    - src/object_detection_eval/data/taxonomy.py
    - src/object_detection_eval/data/image.py
  key_links:
    - "data/coco_gt.py::load_coco_gt consumes schemas/taxonomy.py name_to_id and is called by metrics + bootstrap (Plans 03-04)"
    - "data/taxonomy.py::remap_detections consumes schemas/detection.py::Detection and feeds pred maps into metrics"
---

<objective>
Build the torch-free data tier: the public `load_coco_gt()` (CORE-01), the YAML-backed taxonomy resolution + detection remap + identity-from-COCO functions (CORE-05), and the `ImageLoader` wrapper. This promotes the private `_load_coco_gt`/`_remap_detections`/`_identity_taxonomy_from_coco`/`_resolve_taxonomy` symbols into a public, typed, tested package.

Purpose: These functions are imported directly by scripts today (the private-symbol coupling CORE-01/CORE-02/CORE-04 must break) and are the entry point for all scoring.
Output: `data/` package with three public modules + tests.
</objective>

<authoritative_requirement>CORE-05 is co-claimed with 02-01 (which owns the `TaxonomySpec` schema + YAMLs). **This plan (02-02) is authoritative for CORE-05's pass/fail at phase verification** — it owns the runtime resolution (`resolve_taxonomy` dispatching merged5/raw10/identity with no code edit) and the detection remap that make the taxonomy actually swappable.</authoritative_requirement>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-harness-core/02-RESEARCH.md
@src/object_detection_eval/schemas/detection.py
@src/object_detection_eval/schemas/taxonomy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public load_coco_gt() (CORE-01)</name>
  <files>src/object_detection_eval/data/__init__.py, src/object_detection_eval/data/coco_gt.py, tests/data/__init__.py, tests/data/test_coco_gt.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/tasks/eval_detection_task.py lines 119-179 (source `_load_coco_gt`: parses COCO json, groups annotations by image, maps category name -> eval id via lowercased name_to_id, drops unmapped categories, COCO bbox [x,y,w,h] -> xyxy, empty images -> sv.Detections.empty())
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_eval_detection_task.py lines 24-110 (source `_make_coco_json` fixture + TestLoadCocoGt: basic loading with merge, empty-image handling — the correctness anchor)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md lines 453-469 (empty-image handling contract to preserve)
  </read_first>
  <behavior>
    - Every image in coco["images"] gets a dict entry, even with zero matching annotations (value = sv.Detections.empty()).
    - A category name absent from name_to_id is dropped (not scored).
    - Two source categories that both map to eval id 0 both appear as class_id 0 (merge behavior).
    - COCO bbox [x,y,w,h] becomes xyxy = [x, y, x+w, y+h] float32.
  </behavior>
  <action>Create `data/coco_gt.py` with public `load_coco_gt(coco_json_path: Path, name_to_id: dict[str, int]) -> dict[str, sv.Detections]`, ported line-for-line from the source `_load_coco_gt` but with `name_to_id` a required parameter (no module-default basketball map — that lived in the monolith and must not reappear in src/). Keep the case-insensitive `.lower()` category lookup, the drop-if-unmapped behavior, and the empty-image `sv.Detections.empty()` fallback exactly. Add a defensive `FileNotFoundError` if the json path does not exist before parsing. Adapt the source `_make_coco_json` fixture and TestLoadCocoGt cases into `tests/data/test_coco_gt.py`, passing an explicit merged-5 `name_to_id` (import via `resolve_taxonomy` or a small hardcoded test map). Use loguru only.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/data/test_coco_gt.py -x -q</automated>
  </verify>
  <done>`from object_detection_eval.data.coco_gt import load_coco_gt` imports; a 2-image synthetic COCO with a merged category yields 2 entries with the merged class present; an annotation-free image yields an empty Detections entry; unmapped categories are dropped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Taxonomy resolution + detection remap + identity-from-COCO (CORE-05)</name>
  <files>src/object_detection_eval/data/taxonomy.py, tests/data/test_taxonomy.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/tasks/eval_detection_task.py lines 94-116 (source `_identity_taxonomy_from_coco`: derive contiguous ids 0..N-1 from a COCO json's own categories, ascending category-id order, lowercased name_to_id)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/tasks/eval_detection_task.py lines 595-609 (source `_resolve_taxonomy`: branch on 'merged5' | 'raw10' | 'identity')
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/tasks/eval_detection_task.py lines 1047-1097 (source `_remap_detections`: label_map[class_id] -> name -> name_to_id[name.lower()] -> new Detection; drop when name or eval id missing)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md lines 396-414 (identity is a function over an arbitrary COCO file — do not force it into the YAML load path)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_eval_detection_task.py class `TestRemapDetections` (GOLDEN correctness anchor — adapt these exact cases and their expected drop/translate outcomes into tests/data/test_taxonomy.py; do NOT reinvent)
  </read_first>
  <behavior>
    - resolve_taxonomy("merged5") and resolve_taxonomy("raw10") return (name_to_id, id_to_name) equal to the corresponding YAML spec's derived maps (load via load_taxonomy_spec).
    - resolve_taxonomy("identity", coco_json_path) returns maps derived from that COCO's categories, ascending category-id order, contiguous ids, lowercased names.
    - resolve_taxonomy with an unknown name raises ValueError naming the accepted values.
    - remap_detections drops a detection whose class_id is not in label_map, and drops one whose resolved name is not in name_to_id; otherwise emits a Detection with the same bbox/confidence and the eval class_id.
  </behavior>
  <action>Create `data/taxonomy.py` with: `identity_taxonomy_from_coco(coco_json_path: Path) -> tuple[dict[str,int], dict[int,str]]` (verbatim from source, add a FileNotFoundError guard); `resolve_taxonomy(name: str, coco_json_path: Path | None = None) -> tuple[dict[str,int], dict[int,str]]` that loads merged5.yaml/raw10.yaml via `load_taxonomy_spec` for the named taxonomies (taking the YAML path from a `benchmarks/basketball/conf/taxonomy/{name}.yaml` convention or an injected directory arg), dispatches `name == "identity"` to `identity_taxonomy_from_coco(coco_json_path)`, and raises ValueError otherwise; and `remap_detections(detections: list[Detection], label_map: dict[int,str], name_to_id: dict[str,int]) -> list[Detection]` ported verbatim from `_remap_detections` (as a plain module function, not a staticmethod), preserving the debug-log-and-drop behavior via loguru. In `tests/data/test_taxonomy.py`, assert resolve_taxonomy('merged5')/('raw10') match the legacy maps, resolve_taxonomy('identity', synthetic_coco) derives contiguous ids, unknown name raises, and remap_detections drops unmapped and translates mapped detections.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/data/test_taxonomy.py -x -q</automated>
  </verify>
  <done>resolve_taxonomy dispatches all three taxonomies with no code edit needed to switch (YAML/name selection only); identity derives from a synthetic COCO; remap_detections translates and drops per the source contract.</done>
</task>

<task type="auto">
  <name>Task 3: ImageLoader (BGR uint8 wrapper)</name>
  <files>src/object_detection_eval/data/image.py, tests/data/test_image.py, tests/fixtures/tiny.png</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/io/image.py (source `ImageLoader`: FileNotFoundError on missing path, cached cv2.imread BGR uint8, width/height/filename properties)
  </read_first>
  <action>Port `ImageLoader` verbatim into `data/image.py`, updating the loguru import only. Create a tiny valid PNG fixture at `tests/fixtures/tiny.png` (write it with cv2.imwrite in a small helper or commit a minimal image). In `tests/data/test_image.py`, assert: constructing on a nonexistent path raises FileNotFoundError; read() on the fixture returns a uint8 ndarray of shape (H, W, 3); width/height/filename match. Keep loguru; no print().</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/data/test_image.py -x -q</automated>
  </verify>
  <done>ImageLoader importable; missing path raises FileNotFoundError; fixture image reads as BGR uint8 with correct dimensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| COCO json `file_name` -> image path | filename from an external annotation file is joined to an image dir |
| COCO/YAML file on disk -> parsed dict | untrusted-ish files parsed at load time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | `load_coco_gt`/`identity_taxonomy_from_coco` file parsing | medium | mitigate | Guard with an explicit `FileNotFoundError` before `json.load`; callers join `image_dir / filename` (preserved from source), never trust an absolute path from the json |
| T-02-04 | Info disclosure | path traversal via `file_name` with `..` | low | accept | Out of Phase 2 scope to fully sanitize; callers construct paths by join; documented for the Phase 3/4 orchestration layer |
</threat_model>

<verification>
- `pixi run pytest tests/data/ -x` green.
- `pixi run lint` and `pixi run typecheck` clean on `src/object_detection_eval/data/`.
- No torch import introduced (numpy/cv2/supervision/pydantic only).
</verification>

<success_criteria>
`load_coco_gt`, `resolve_taxonomy`, `remap_detections`, `identity_taxonomy_from_coco`, and `ImageLoader` are public, typed, tested, and usable without any task object; taxonomy switching is a name/YAML selection.
</success_criteria>

<output>
Create `.planning/phases/02-harness-core/02-02-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `src/object_detection_eval/data/coco_gt.py` (`load_coco_gt`) | module | CORE-01 |
| `src/object_detection_eval/data/taxonomy.py` (`resolve_taxonomy`, `remap_detections`, `identity_taxonomy_from_coco`) | module | CORE-05 |
| `src/object_detection_eval/data/image.py` (`ImageLoader`) | module | CORE-01 |
| `tests/fixtures/tiny.png` | fixture | CORE-01 |
