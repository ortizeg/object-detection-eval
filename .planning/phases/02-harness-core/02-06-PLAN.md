---
phase: 02-harness-core
plan: 06
type: execute
wave: 4
depends_on: [02-02, 02-03, 02-04, 02-05]
files_modified:
  - src/object_detection_eval/inference/postprocess.py
  - src/object_detection_eval/inference/detectors/__init__.py
  - src/object_detection_eval/inference/detectors/yolox.py
  - src/object_detection_eval/inference/detectors/yolo26.py
  - src/object_detection_eval/inference/detectors/rtmdet.py
  - src/object_detection_eval/inference/detectors/deim.py
  - src/object_detection_eval/inference/detectors/rtdetrv2.py
  - src/object_detection_eval/inference/detectors/damo.py
  - src/object_detection_eval/inference/detectors/rfdetr.py
  - src/object_detection_eval/__init__.py
  - tests/inference/test_postprocess.py
  - tests/inference/detectors/__init__.py
  - tests/inference/detectors/test_detectors_letterbox.py
  - tests/inference/detectors/test_detectors_square.py
  - tests/test_no_torch_import.py
autonomous: true
requirements: [CORE-06, CORE-07, CORE-08, CORE-09]
must_haves:
  truths:
    - "all 7 detectors (YOLOX, YOLO26, RTMDet, DEIM, RT-DETRv2, DAMO, RF-DETR) subclass the one BaseInferencer ABC and return list[Detection] from predict() (CORE-07)"
    - "RT-DETRv2 is its own importable module, not a config pointer at DEIM (CORE-07)"
    - "postprocessors de-transform via the explicit LetterboxTransform + detransform_boxes, with no mutable set_letterbox_params state (CORE-06)"
    - "importing object_detection_eval and every core submodule leaves torch absent from sys.modules (CORE-08)"
    - "the public API (load_coco_gt, compute_metrics, PR/F1, bootstrap, detectors) is importable from the package root (CORE-07)"
  artifacts:
    - src/object_detection_eval/inference/postprocess.py
    - src/object_detection_eval/inference/detectors/rtdetrv2.py
    - tests/test_no_torch_import.py
  key_links:
    - "each detector composes inference/preprocess.py::Letterbox + inference/onnx.py::ONNXInferencer + inference/postprocess.py::<Model>PostProcessor"
    - "object_detection_eval/__init__.py re-exports the public API assembled across data/metrics/inference"
---

<objective>
Complete the inference tier and the package's public face: port the per-model postprocessors (numpy NMS verbatim, de-letterbox via the explicit `detransform_boxes`/`LetterboxTransform` instead of hidden mutable state), wire all 7 detectors behind the single `BaseInferencer` ABC with RT-DETRv2 as its own module (CORE-07), enforce the torch-free import graph with a dedicated test (CORE-08), and re-export the public API from the package root.

Purpose: CORE-07 (7 detectors behind one ABC, RT-DETRv2 standalone), CORE-08 (torch-free, tested), and the CORE-06 de-transform wiring all land here; this plan makes `pixi run test` pass with no torch and `pixi run lint` pass with T20.
Output: `inference/postprocess.py`, `inference/detectors/*.py` (7), the torch-free gate test, and the public `__init__.py`.
</objective>

<authoritative_requirement>CORE-08 is co-claimed with 02-05 (which keeps its own modules torch-free). **This plan (02-06) is authoritative for CORE-08's pass/fail at phase verification** — it owns `tests/test_no_torch_import.py`, the whole-core-graph walk that is the requirement's actual gate. CORE-06 is co-claimed but 02-05 is authoritative for it (this plan only consumes the Letterbox/de-transform).</authoritative_requirement>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-harness-core/02-RESEARCH.md
@src/object_detection_eval/inference/preprocess.py
@src/object_detection_eval/inference/onnx.py
@src/object_detection_eval/schemas/detection.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-model postprocessors with explicit de-transform (CORE-06)</name>
  <files>src/object_detection_eval/inference/postprocess.py, tests/inference/test_postprocess.py, tests/inference/detectors/__init__.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/postprocess.py (source `BasePostProcessor._make_detection` clamp-to-[0,1]; `YOLOXPostProcessor` + numpy `_iou`/`_nms` per-class greedy NMS; `YOLO26PostProcessor` NMS-free xyxy; `RFDETRPostProcessor` top-k multi-label sigmoid decode, normalized cxcywh, no rescale)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/rtmdet_letterbox_inferencer.py (RTMDetLetterboxPostProcessor: dets[.,5]+labels, mean/std constants; de-transform divides by ratio, no pad offset)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/deim_inferencer.py (DeimPostProcessor: labels/boxes/scores, boxes already original-pixel via orig_target_sizes)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/damo_inferencer.py (DamoPostProcessor: scores/bboxes, per-class numpy NMS, coord/input_size)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/yolox_letterbox_inferencer.py and yolo26_letterbox_inferencer.py (the letterbox de-transform math to REPLACE with detransform_boxes; drop set_letterbox_params mutable state)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md lines 244-270 (Pattern 2 landmine: replace mutable set_letterbox_params with explicit transform threading)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_onnx_inference.py classes `TestYOLOXPostProcessor`, `TestRFDETRPostProcessor` (GOLDEN correctness anchors — adapt these exact fixed-output cases and their expected decoded Detections into tests/inference/test_postprocess.py; do NOT reinvent the expected counts/class-ids/coords)
  </read_first>
  <action>Create `inference/postprocess.py` with `BasePostProcessor` (the `_make_detection` clamp helper + an abstract `__call__(outputs, image_width, image_height, transform: LetterboxTransform | None = None)`). Port `YOLOXPostProcessor` (with its numpy `_iou`/`_nms` per-class greedy NMS verbatim — do NOT rewrite the NMS math), `YOLO26PostProcessor`, `RTMDetPostProcessor`, `DeimPostProcessor`, `DamoPostProcessor` (with its own per-class numpy `_nms` verbatim), and `RFDETRPostProcessor`. For the three letterbox models (YOLOX, YOLO26, RTMDet) replace the source per-image mutable-state de-transform (the setter documented in RESEARCH Pattern 2, lines 244-270) with a call to `inference.preprocess.detransform_boxes(boxes_model_space, transform, w, h)` using the `LetterboxTransform` passed into `__call__`. Keep DEIM (in-graph de-transform), DAMO (coord/input_size), and RF-DETR (normalized cxcywh, no rescale) exactly. In `tests/inference/test_postprocess.py`, feed each postprocessor fixed numpy arrays matching its documented output shape (see the source docstrings / research preprocessing table) plus a known LetterboxTransform, and assert the returned Detections have clamped [0,1] bboxes and the expected count/class ids. loguru only.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/inference/test_postprocess.py -x -q && ! grep -rn 'set_letterbox_params' src/</automated>
  </verify>
  <done>All 6 postprocessor classes import and decode fixed synthetic outputs into clamped Detections; letterbox de-transform goes through detransform_boxes (no set_letterbox_params anywhere in src/).</done>
</task>

<task type="auto">
  <name>Task 2a: Letterbox-family detectors — YOLOX, YOLO26, RTMDet (CORE-07)</name>
  <files>src/object_detection_eval/inference/detectors/__init__.py, src/object_detection_eval/inference/detectors/yolox.py, src/object_detection_eval/inference/detectors/yolo26.py, src/object_detection_eval/inference/detectors/rtmdet.py, tests/inference/detectors/test_detectors_letterbox.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/yolox_letterbox_inferencer.py, yolo26_letterbox_inferencer.py, rtmdet_letterbox_inferencer.py (the three letterbox inferencer preprocess flows to fold into detectors using the Plan-05 Letterbox)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/src/object_detection_eval/inference/preprocess.py (Plan-05 Letterbox + LetterboxTransform + detransform_boxes to compose)
  </read_first>
  <action>Create the three letterbox-family detector classes under `inference/detectors/`, each subclassing `ONNXInferencer` (or `BaseInferencer`) and composing the Plan-05 `Letterbox` (correct `LetterboxConfig` per model) + its matching postprocessor: `yolox.py::YOLOXDetector` (top-left letterbox, BGR, no-norm, YOLOXPostProcessor), `yolo26.py::YOLO26Detector` (centered letterbox, RGB /255, YOLO26PostProcessor), `rtmdet.py::RTMDetDetector` (top-left letterbox, BGR mean/std, RTMDetPostProcessor). Each overrides `predict()` to: call `Letterbox` -> (tensor, transform), run the session, and call the postprocessor with the explicit `transform` (no mutable state). Register these three in `detectors/__init__.py` (Task 2b appends the rest). In `tests/inference/detectors/test_detectors_letterbox.py`, mock `onnxruntime.InferenceSession` (patch `object_detection_eval.inference.onnx.ort.InferenceSession`) so `session.run` returns fixed arrays of each model's documented output shape; assert each detector `isinstance(BaseInferencer)` and `predict()` returns `list[Detection]`. loguru only; do NOT edit pyproject/pixi.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/inference/detectors/test_detectors_letterbox.py -x -q</automated>
  </verify>
  <done>YOLOXDetector, YOLO26Detector, RTMDetDetector import from `object_detection_eval.inference.detectors`, subclass BaseInferencer, and return list[Detection] under a mocked ORT session; de-transform is explicit.</done>
</task>

<task type="auto">
  <name>Task 2b: Square-resize family + RF-DETR — DEIM, RT-DETRv2, DAMO, RF-DETR (CORE-07)</name>
  <files>src/object_detection_eval/inference/detectors/__init__.py, src/object_detection_eval/inference/detectors/deim.py, src/object_detection_eval/inference/detectors/rtdetrv2.py, src/object_detection_eval/inference/detectors/damo.py, src/object_detection_eval/inference/detectors/rfdetr.py, tests/inference/detectors/test_detectors_square.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/deim_inferencer.py (DeimInferencer.predict feeds a second `orig_target_sizes` [[w,h]] int64 input — the D-FINE deploy format shared with RT-DETRv2)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/inference/damo_inferencer.py (DamoInferencer square-resize raw 0-255 flow)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/tasks/eval_detection_task.py lines 1195-1224, 1393-1439 (how RF-DETR uses the generic ONNXInferencer with ImageNet mean/std, and how DEIM is constructed)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md lines 258-263, 344-350 (RT-DETRv2 has no source code today; CORE-07 requires a real rtdetrv2.py — a thin subclass of the DEIM detector + a docstring explaining the shared D-FINE deploy format; NOT a config pointer)
  </read_first>
  <action>Create the four remaining detector classes: `deim.py::DeimDetector` (square resize RGB /255 antialias + `orig_target_sizes` second input, DeimPostProcessor), `damo.py::DamoDetector` (square resize raw 0-255, DamoPostProcessor), `rfdetr.py::RFDETRDetector` (generic ImageNet square resize, RFDETRPostProcessor), and `rtdetrv2.py::RTDETRv2Detector` as a thin subclass of `DeimDetector` with a module docstring stating it reuses the D-FINE `labels/boxes/scores` deploy export (identical preprocessing/de-transform to DEIM) and exists as its own module so the 7-model comparison is explicit rather than a config alias. Append all four to `detectors/__init__.py` so all 7 are exposed. Note: the DEIM antialias path imports `PIL.Image`; Pillow is already present transitively via matplotlib — do NOT add a dependency or edit pyproject/pixi. In `tests/inference/detectors/test_detectors_square.py`, mock the ORT session (patch `object_detection_eval.inference.onnx.ort.InferenceSession`) with fixed per-model output arrays; assert each detector `isinstance(BaseInferencer)`, `predict()` returns `list[Detection]`, and that `RTDETRv2Detector` yields output identical to `DeimDetector` on the same mocked outputs. loguru only.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/inference/detectors/test_detectors_square.py -x -q && /Users/ortizeg/.pixi/bin/pixi run python -c "from object_detection_eval.inference.detectors import YOLOXDetector, YOLO26Detector, RTMDetDetector, DeimDetector, RTDETRv2Detector, DamoDetector, RFDETRDetector; print('7 detectors ok')"</automated>
  </verify>
  <done>DeimDetector, DamoDetector, RFDETRDetector, and a standalone RTDETRv2Detector import and return list[Detection] under a mocked ORT session; RT-DETRv2 produces DEIM-identical output; all 7 detectors are exposed from `detectors/__init__.py`; no pyproject/pixi edit made here.</done>
</task>

<task type="auto">
  <name>Task 3: Torch-free import-graph gate + public API re-exports (CORE-08)</name>
  <files>tests/test_no_torch_import.py, src/object_detection_eval/__init__.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/tests/test_package.py (existing test_core_imports_without_torch — the seed to extend into a full import-graph walk)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md lines 582, 608 (CORE-08 dedicated torch-import test walking the core submodules)
  </read_first>
  <action>Create `tests/test_no_torch_import.py` that imports the package root plus every core submodule (schemas.detection, schemas.annotation, schemas.taxonomy, utils.boxes, data.coco_gt, data.taxonomy, data.image, metrics.detection_map, metrics.prf1, metrics.curves, metrics.bootstrap, inference.base, inference.onnx, inference.preprocess, inference.postprocess, and each detectors.* module) in a fresh assertion and asserts `"torch" not in sys.modules` after all imports. Update `src/object_detection_eval/__init__.py` to re-export the public API assembled across tiers: `load_coco_gt`, `resolve_taxonomy`, `remap_detections`, `compute_metrics`, `compute_prf1_at_threshold`, `find_best_threshold`, `compute_pr_curve`, `run_bootstrap`, `build_report`, `BaseInferencer`, and the 7 detector classes, plus the existing `__version__`. Keep `__all__` accurate. loguru only; ensure no re-export pulls torch.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/test_no_torch_import.py tests/test_package.py -x -q && /Users/ortizeg/.pixi/bin/pixi run python -c "import object_detection_eval as o; [getattr(o, n) for n in ('load_coco_gt','compute_metrics','run_bootstrap','BaseInferencer')]; print('api ok')"</automated>
  </verify>
  <done>tests/test_no_torch_import.py imports the whole core graph and asserts torch absent; the package root re-exports the public API (load_coco_gt, compute_metrics, run_bootstrap, BaseInferencer, the 7 detectors) and they are all accessible; existing test_package.py still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ONNX model file -> onnxruntime session | detector loads a model file (real weights are Phase 3/4; tests mock the session) |
| raw ONNX output arrays -> Detection | numpy decode + clamp of untrusted-shaped arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | NMS / decode math silently changed during port | high | mitigate | Port the numpy NMS and decode bodies verbatim (no rewrite) and cover each postprocessor with fixed-output tests; the Phase-4 gate reproduces exact numbers |
| T-02-12 | Tampering | a leaked torch import into the core graph | medium | mitigate | Dedicated tests/test_no_torch_import.py walks every core submodule and asserts torch absent; runs in default CI |
| T-02-13 | DoS | malformed ONNX model file | low | accept | onnxruntime attack surface; out of Phase 2 scope (real models arrive in Phase 3) |
</threat_model>

<verification>
- `pixi run test` (full suite) green with no torch installed and `--cov-fail-under=80` satisfied.
- `pixi run lint` clean with `T20` and zero suppressions; `pixi run typecheck` clean.
- `tests/test_no_torch_import.py` passes; RT-DETRv2 importable from its own module.
</verification>

<success_criteria>
All 7 detectors run behind one BaseInferencer ABC with RT-DETRv2 as its own module; letterbox de-transform is explicit (no mutable state); the core import graph is torch-free and enforced by a test; the public API is importable from the package root; `pixi run test` and `pixi run lint` (T20) pass.
</success_criteria>

<output>
Create `.planning/phases/02-harness-core/02-06-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `src/object_detection_eval/inference/postprocess.py` (`BasePostProcessor`, `YOLOXPostProcessor`, `YOLO26PostProcessor`, `RTMDetPostProcessor`, `DeimPostProcessor`, `DamoPostProcessor`, `RFDETRPostProcessor`) | module | CORE-06 |
| `src/object_detection_eval/inference/detectors/{yolox,yolo26,rtmdet,deim,rtdetrv2,damo,rfdetr}.py` (7 detector classes) | modules | CORE-07 |
| `src/object_detection_eval/__init__.py` (public API re-exports) | module | CORE-07 |
| `tests/test_no_torch_import.py` | test | CORE-08 |
