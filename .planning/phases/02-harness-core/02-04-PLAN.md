---
phase: 02-harness-core
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - src/object_detection_eval/metrics/bootstrap.py
  - tests/metrics/test_bootstrap.py
  - scripts/measure_supervision_drift.py
  - docs/methodology.md
  - pyproject.toml
  - pixi.toml
autonomous: true
requirements: [CORE-02, CORE-04, CORE-09]
must_haves:
  truths:
    - "run_bootstrap uses one shared draw per iteration across all models (paired) and is deterministic under a fixed seed (CORE-04)"
    - "the same seed produces identical per-model CIs and identical pairwise-difference CIs across two runs (CORE-04)"
    - "supervision is pinned in pyproject.toml + pixi.toml to the version that reproduces the published 5-class test mAP within tolerance (CORE-02)"
    - "the measured drift and pin rationale are recorded in docs/methodology.md (CORE-02)"
  artifacts:
    - src/object_detection_eval/metrics/bootstrap.py
    - scripts/measure_supervision_drift.py
    - docs/methodology.md
  key_links:
    - "metrics/bootstrap.py reuses metrics/detection_map.py::compute_metrics and data/coco_gt.py::load_coco_gt (the private-symbol coupling this breaks)"
    - "the supervision pin gates Phase 4 reproduction — the recorded drift is the baseline the reproduction gate compares against"
---

<objective>
Two coupled deliverables about statistical reproduction fidelity:
1. Port the seeded, paired, image-level bootstrap (`scripts/bootstrap_ci.py`) into a public `metrics/bootstrap.py` (CORE-04), reusing the ported `compute_metrics`/`load_coco_gt` — breaking the private-symbol coupling.
2. Empirically measure the `supervision` version drift (the Phase-4 landmine) by re-scoring the saved 7-model merged-5 test predictions through the ported `compute_metrics`, and pin `supervision` in `pyproject.toml`+`pixi.toml` to whichever version reproduces the published numbers, recording the measured drift in `docs/methodology.md`.

Purpose: CORE-04 requires a reproducible paired bootstrap; the pin de-risks the Phase-4 reproduction gate NOW (locked decision), while the drift measurement doubles as a first end-to-end validation that the ported scorer reproduces the published table.
Output: `metrics/bootstrap.py` + the drift measurement script + a pinned `supervision` + a methodology drift note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-harness-core/02-RESEARCH.md
@src/object_detection_eval/metrics/detection_map.py
@src/object_detection_eval/data/coco_gt.py
@pyproject.toml
@pixi.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paired image-level bootstrap (CORE-04)</name>
  <files>src/object_detection_eval/metrics/bootstrap.py, tests/metrics/test_bootstrap.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/scripts/bootstrap_ci.py (full: `_load_predictions`, `_resample_map`, `_percentile_ci`, `_run_bootstrap`, `_build_report`; note the private import of `_compute_metrics`/`_load_coco_gt` from the task module — the coupling to break)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md lines 471-486 (paired-draw invariant: ONE draw per iteration reused across every model; positional keys `{filename}__{position}` so duplicate draws count repeatedly — do NOT use a set of filenames)
  </read_first>
  <behavior>
    - run_bootstrap(seed=0) run twice yields byte-identical per-model CI bounds and identical pairwise-difference CI bounds.
    - Within one iteration, all models are resampled with the SAME draw array (paired); the draw is generated once per iteration via np.random.default_rng(seed).integers.
    - resample_map keys are `f"{filename}__{position}"`, so an image drawn 3 times contributes 3 distinct scored entries.
    - build_report emits per-model {point_estimate, bootstrap_mean, bootstrap_std, ci_2.5, ci_97.5} and pairwise {point_diff, mean_diff, ci_2.5, ci_97.5, ci_excludes_zero}.
  </behavior>
  <action>Create `metrics/bootstrap.py` porting `load_predictions`, `resample_map`, `percentile_ci`, `run_bootstrap`, and `build_report` as public functions, importing `compute_metrics` from `object_detection_eval.metrics.detection_map` and (where a report is assembled from a GT path) `load_coco_gt` from `object_detection_eval.data.coco_gt` — never from a task module. Preserve the shared-draw-per-iteration paired structure and the positional `{filename}__{position}` resample keys exactly. In `tests/metrics/test_bootstrap.py`, build a small synthetic gt_map plus 2 model pred_maps and assert: (a) two run_bootstrap calls with the same seed give identical CI arrays for a single model, (b) the pairwise-difference CI is identical across the two seeded runs, (c) a different seed changes the draw. loguru only; no print().</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/metrics/test_bootstrap.py -x -q</automated>
  </verify>
  <done>run_bootstrap/build_report importable from `object_detection_eval.metrics.bootstrap`; same-seed determinism holds for both single-model and pairwise-difference CIs; bootstrap imports compute_metrics/load_coco_gt from the public modules, not any task.</done>
</task>

<task type="auto">
  <name>Task 2: Measure supervision drift and pin the reproducing version (CORE-02)</name>
  <files>scripts/measure_supervision_drift.py, docs/methodology.md, pyproject.toml, pixi.toml</files>
  <precondition>The source-repo artifacts exist locally: predictions under `/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/eval_output/official_2026-07-13/` and `.../object-detection-training/.deploy_comparison/eval_new/`, the GT tarball `.../object-detection-training/.deploy_comparison/basketball-data.tar.gz`, and the anchor `.../object-detection-training/.deploy_comparison/bootstrap_5c_test_7models.json`. Assert these paths exist before running; halt with a clear message if not.</precondition>
  <reversibility rating="costly">The chosen supervision pin is the reproduction baseline the Phase-4 gate compares against; changing it later invalidates the recorded drift note and may reopen the reproduction gate.</reversibility>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md lines 86-100, 335-342, 499-537 (the version-drift landmine: published numbers produced under supervision 0.27.0.post1, target env has 0.29.1; measure by re-scoring saved predictions; recommend pinning the reproducing version)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/.deploy_comparison/bootstrap_5c_test_7models.json (the anchor: per_model[*].mAP_50_95.point_estimate produced under 0.27.0.post1 — YOLO26m 0.7155, DEIM-M 0.6863, YOLOX-M 0.6718, RF-DETR-M 0.6463, RTMDet-M 0.6277, DAMO-YOLO-M 0.6191, RT-DETRv2-M 0.5814)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/scripts/bootstrap_ci.py lines 39-63 (the saved-predictions JSON format `_load_predictions` reads: filename -> list of {bbox_xyxy, class_id, confidence})
  </read_first>
  <action>Write `scripts/measure_supervision_drift.py` that: (1) extracts the test-split GT `basketball-player-detection-3/test/_annotations.coco.json` from the source-repo tarball into a temp dir and loads it with `load_coco_gt` under the merged-5 taxonomy (`resolve_taxonomy("merged5")`); (2) loads each of the 7 models' merged-5 test predictions using `metrics.bootstrap.load_predictions`, gunzipping the `.json.gz` files (YOLO26m at `eval_output/official_2026-07-13/YOLO26m-640/merged5/predictions_yolo26_test.json.gz`, YOLOX-M at `.../YOLOX-M-800/merged5/predictions_yolox_test.json.gz`) and reading the plain `.json` files under `.deploy_comparison/eval_new/{deim_m,rfdetr_m,rtmdet_m_rewarmup,damo_m,rtdetrv2_m}/merged5/` into a temp dir; (3) scores each with the ported `compute_metrics` under the currently-installed supervision and prints a per-model table of mAP@50:95 vs the anchor point estimates with the absolute delta; (4) applies a tolerance of 0.003 (0.3 pt) — rationale (record it in the methodology note): 0.3 pt is well under the known ~0.9 pt supervision-vs-pycocotools systematic gap documented in the source EVAL_REPORT, so any intra-supervision-version drift exceeding 0.3 pt is a material, unexpected shift worth pinning against rather than absorbing — if every model reproduces within tolerance, pin the CURRENT (reproducing) supervision version; otherwise the executor installs `supervision==0.27.0.post1`, re-runs to confirm reproduction, and pins that. Take the source-repo root as a `--source-repo` argument (default the absolute path above). Then edit `pyproject.toml` (replace the bare `"supervision"` dependency with `"supervision==<pinned>"`) and `pixi.toml` (replace `supervision = "*"` with `supervision = "==<pinned>"`). Write `docs/methodology.md` with a "Supervision version and measured drift" section recording: the anchor version (0.27.0.post1), the version measured, the per-model deltas, the tolerance, and the pinned version with rationale. Route all output through loguru; the script must not use `print()`. Do NOT wire this script into pytest (it reads source-repo-only artifacts absent from CI).</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run python scripts/measure_supervision_drift.py 2>&1 | tail -20 && grep -E 'supervision *==' pyproject.toml pixi.toml && grep -qi 'measured drift' docs/methodology.md && echo OK</automated>
  </verify>
  <done>The drift script runs, printing a 7-model mAP@50:95 vs anchor table with deltas; `supervision` is pinned to an exact version in BOTH pyproject.toml and pixi.toml; `docs/methodology.md` records the measured drift, tolerance, and pin rationale. If the current version reproduces within 0.003, that version is pinned and documented; otherwise 0.27.0.post1 is pinned after confirming it reproduces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved prediction JSON / GT tarball -> scorer | source-repo artifacts read once for the drift measurement |
| version pin edit -> dependency resolution | pinning changes the resolved supervision build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | supervision numeric drift silently shifting published mAP | high | mitigate | Empirically measure drift vs the 0.27.0.post1 anchor and pin the reproducing version in pyproject+pixi; record the measurement so Phase 4 treats any further drift as a defect, not a tolerance |
| T-02-08 | Repudiation | undocumented dependency change | low | mitigate | The pin rationale + measured deltas are written to docs/methodology.md, committed with the change |
</threat_model>

<verification>
- `pixi run pytest tests/metrics/test_bootstrap.py -x` green (determinism).
- `scripts/measure_supervision_drift.py` runs end-to-end against the source-repo artifacts and reports per-model deltas.
- `supervision==<pinned>` present in both pyproject.toml and pixi.toml; `docs/methodology.md` carries the drift note.
- `pixi run lint` clean (T20).
</verification>

<success_criteria>
The paired image-level bootstrap is public, typed, tested, and deterministic under a fixed seed for both single-model and pairwise-difference CIs; the supervision version is pinned to the one that reproduces the published 5-class test table within tolerance, with the measured drift documented.
</success_criteria>

<output>
Create `.planning/phases/02-harness-core/02-04-SUMMARY.md` when done, including the measured per-model drift table and the pinned supervision version.
</output>

## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `src/object_detection_eval/metrics/bootstrap.py` (`load_predictions`, `resample_map`, `percentile_ci`, `run_bootstrap`, `build_report`) | module | CORE-04 |
| `scripts/measure_supervision_drift.py` | script | CORE-02 |
| `docs/methodology.md` (supervision drift + pin section) | doc | CORE-02 |
| pinned `supervision==<version>` in pyproject.toml + pixi.toml | config | CORE-02 |
