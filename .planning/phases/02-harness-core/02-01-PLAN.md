---
phase: 02-harness-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/object_detection_eval/schemas/__init__.py
  - src/object_detection_eval/schemas/detection.py
  - src/object_detection_eval/schemas/annotation.py
  - src/object_detection_eval/schemas/taxonomy.py
  - benchmarks/basketball/conf/taxonomy/merged5.yaml
  - benchmarks/basketball/conf/taxonomy/raw10.yaml
  - benchmarks/basketball/conf/taxonomy/identity.yaml
  - src/object_detection_eval/utils/__init__.py
  - src/object_detection_eval/utils/boxes.py
  - tests/schemas/__init__.py
  - tests/schemas/test_detection.py
  - tests/schemas/test_taxonomy.py
  - tests/utils/__init__.py
  - tests/utils/test_boxes.py
autonomous: true
requirements: [CORE-05, CORE-08, CORE-09]
must_haves:
  truths:
    - "TaxonomySpec loaded from merged5.yaml resolves to the exact name_to_id/id_to_name of the legacy merged-5 maps (CORE-05)"
    - "TaxonomySpec loaded from raw10.yaml resolves to the exact 10-class name_to_id/id_to_name (CORE-05)"
    - "grep of src/ for basketball class-name constants returns nothing (CORE-05)"
    - "Detection.confidence outside [0,1] raises a validation error; models are frozen (CORE-09)"
  artifacts:
    - src/object_detection_eval/schemas/detection.py
    - src/object_detection_eval/schemas/annotation.py
    - src/object_detection_eval/schemas/taxonomy.py
    - benchmarks/basketball/conf/taxonomy/merged5.yaml
    - benchmarks/basketball/conf/taxonomy/raw10.yaml
    - benchmarks/basketball/conf/taxonomy/identity.yaml
  key_links:
    - "schemas/taxonomy.py::TaxonomySpec is consumed by data/taxonomy.py (Plan 02) and data/coco_gt.py (Plan 02)"
    - "schemas/detection.py::Detection is consumed by metrics (Plan 03), inference (Plans 05-06)"
---

<objective>
Establish the typed foundation for the harness: port the frozen Pydantic `Detection`/`BoundingBox`/`DetectionAnnotation` schemas verbatim, and replace the hardcoded basketball taxonomy constants with a YAML-driven `TaxonomySpec` (CORE-05). The three basketball taxonomy YAMLs live under `benchmarks/`, never `src/`.

Purpose: Every downstream tier (data, metrics, inference) depends on these schemas, and CORE-05 requires the taxonomy to be swappable via YAML with zero basketball constants left in `src/`.
Output: `schemas/` package + three taxonomy YAMLs + their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-harness-core/02-RESEARCH.md
@docs/FORK_PLAN.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Detection/BoundingBox/DetectionAnnotation schemas verbatim</name>
  <files>src/object_detection_eval/schemas/__init__.py, src/object_detection_eval/schemas/detection.py, src/object_detection_eval/schemas/annotation.py, tests/schemas/__init__.py, tests/schemas/test_detection.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/schemas/detection.py (source: BoundingBox + Detection, frozen=True, confidence Field(ge=0.0, le=1.0))
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/schemas/annotation.py (source: AnnotationInfo + DetectionAnnotation)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_onnx_inference.py lines 40-110 (source tests for BoundingBox/Detection: creation, frozen, confidence bounds — the correctness anchor to adapt)
  </read_first>
  <action>Create the `schemas/` package under the new package root `object_detection_eval`. Copy `BoundingBox` and `Detection` into `detection.py` unchanged (normalised xywh, frozen models, `confidence` bounded to [0,1]). Copy `AnnotationInfo` and `DetectionAnnotation` into `annotation.py`, updating the intra-package import to `object_detection_eval.schemas.detection`. Adapt the source BoundingBox/Detection tests (creation, frozen-immutability, confidence-range rejection) into `tests/schemas/test_detection.py` against the new import paths. Use no `print()`; if any logging is added, use `loguru`.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/schemas/test_detection.py -x -q</automated>
  </verify>
  <done>`from object_detection_eval.schemas.detection import Detection, BoundingBox` and `from object_detection_eval.schemas.annotation import DetectionAnnotation, AnnotationInfo` both import; test_detection.py passes; a Detection with confidence=1.5 raises; assigning to a frozen field raises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NEW TaxonomySpec + merged5/raw10/identity YAMLs (CORE-05)</name>
  <files>src/object_detection_eval/schemas/taxonomy.py, benchmarks/basketball/conf/taxonomy/merged5.yaml, benchmarks/basketball/conf/taxonomy/raw10.yaml, benchmarks/basketball/conf/taxonomy/identity.yaml, tests/schemas/test_taxonomy.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/tasks/eval_detection_task.py lines 34-116 (the legacy hardcoded taxonomy: the merged-5 label map, the player sub-class merge set, ball-in-basket + COCO/VLM alias entries, and the raw-10 name tuple — the exact mappings TaxonomySpec must reproduce)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/docs/FORK_PLAN.md section 6.3 (TaxonomySpec YAML sketch: name, classes, merge)
  </read_first>
  <behavior>
    - Loading merged5.yaml and resolving it yields id_to_name == {0:player,1:ball,2:referee,3:rim,4:number} (order per source line 46-52).
    - Resolving merged5 yields name_to_id equal to the legacy merged map INCLUDING the player sub-class aliases, ball-in-basket, and the COCO/VLM prompt aliases (source lines 55-70), all lowercased.
    - Loading raw10.yaml resolves to the 10-class contiguous id_to_name and its inverse (source lines 76-91), no merging.
    - identity.yaml carries `name: identity` and empty classes; TaxonomySpec accepts it (classes optional only when name == "identity").
    - `load_taxonomy_spec(path)` uses yaml.safe_load, never yaml.load.
  </behavior>
  <action>Create `schemas/taxonomy.py` with a frozen Pydantic `TaxonomySpec` carrying `name: str`, `classes: list[str]` (canonical eval classes, index = eval id), `merge: dict[str, list[str]]` (canonical name -> list of source category names that collapse into it), and `aliases: dict[str, str]` (extra source name -> canonical name, for COCO-vocabulary / VLM-prompt strings). Add a validator: `classes` must be non-empty unless `name == "identity"`. Add `TaxonomySpec.name_to_id` and `TaxonomySpec.id_to_name` derived properties: `id_to_name` = enumerate(classes); `name_to_id` maps each canonical class -> its index, each merge source -> its canonical's index, and each alias -> its target canonical's index, all keys lowercased. Add module function `load_taxonomy_spec(path: Path) -> TaxonomySpec` using `yaml.safe_load`. Author the three YAMLs under `benchmarks/basketball/conf/taxonomy/`, encoding the canonical classes, the player sub-class merge, the ball merge, and the alias entries by copying the exact strings from the source lines cited in read_first (these strings live in benchmarks/, not src/). In `tests/schemas/test_taxonomy.py`, hardcode the legacy expected `name_to_id`/`id_to_name` dicts (tests may contain basketball names) and assert `load_taxonomy_spec("benchmarks/basketball/conf/taxonomy/merged5.yaml").name_to_id == expected_merged5_name_to_id` and the raw10 equivalents, plus that identity.yaml loads with empty classes. Route all messages through loguru, never `print()`.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/schemas/test_taxonomy.py -x -q && grep -rEl 'player-jump-shot|player-in-possession|player-layup-dunk|player-shot-block|ball-in-basket' src/ && { echo 'FAIL: basketball class constants found in src/'; exit 1; } || echo 'OK: no basketball constants in src/'</automated>
  </verify>
  <done>merged5.yaml and raw10.yaml each round-trip to the legacy name_to_id/id_to_name dicts; identity.yaml loads; `load_taxonomy_spec` uses safe_load; a recursive grep of `src/` for the five listed basketball class-name literals returns no files.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve utils/boxes.py disposition — port the two live functions to numpy, drop the rest (CORE-08)</name>
  <files>src/object_detection_eval/utils/__init__.py, src/object_detection_eval/utils/boxes.py, tests/utils/__init__.py, tests/utils/test_boxes.py</files>
  <read_first>
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/src/object_detection_training/utils/boxes.py (source: `cxcywh_to_xyxy`, `xyxy_to_cxcywh`, `box_iou_1_to_n`, `pad_and_clamp_bbox`, `pixel_xyxy_to_normalized_xywh` — note which use torch)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-training/tests/test_utils_boxes.py (source tests, incl. `TestPadAndClampBbox` — the correctness anchor to adapt, do not reinvent)
    - /Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval/.planning/phases/02-harness-core/02-RESEARCH.md (Torch-Coupling Audit + Open Question 2 — the disposition this task resolves)
  </read_first>
  <action>Create `src/object_detection_eval/utils/boxes.py` (+ package `__init__.py`). Port ONLY the two functions the harness actually consumes, rewritten in pure numpy so no torch enters the import graph: `pad_and_clamp_bbox` and `pixel_xyxy_to_normalized_xywh` (both are already torch-free in source — copy their exact clamp/normalize semantics verbatim, converting any tensor ops to numpy). DROP `box_iou_1_to_n` (redundant with the NMS-internal IoU already ported in postprocess.py — Plan 06) and DROP the torch-only converters `cxcywh_to_xyxy`/`xyxy_to_cxcywh` (no Phase-2 consumer; the CORE-06 de-transform in Plan 05 is new numpy code). Add a module docstring stating this disposition and why the dropped functions are safe to drop. Adapt `TestPadAndClampBbox` (and any `pixel_xyxy_to_normalized_xywh` cases) from the source test file into `tests/utils/test_boxes.py` against the new numpy signatures — keep their exact expected values. In `02-RESEARCH.md`, mark Open Question 2 resolved with a one-line note pointing here. No `print()`; loguru only. No torch import anywhere in this module.</action>
  <verify>
    <automated>cd "/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/object-detection-eval" && /Users/ortizeg/.pixi/bin/pixi run pytest tests/utils/test_boxes.py -x -q && /Users/ortizeg/.pixi/bin/pixi run python -c "import object_detection_eval.utils.boxes, sys; assert 'torch' not in sys.modules, 'torch leaked into utils.boxes import graph'"</automated>
  </verify>
  <done>`utils/boxes.py` exposes `pad_and_clamp_bbox` + `pixel_xyxy_to_normalized_xywh` in numpy; `box_iou_1_to_n`/`cxcywh_to_xyxy`/`xyxy_to_cxcywh` are intentionally absent with a docstring rationale; importing the module pulls no torch; adapted `TestPadAndClampBbox` cases pass with their original expected values; RESEARCH Open Question 2 marked resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file -> TaxonomySpec | taxonomy YAMLs are repo-controlled config parsed at load time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/RCE | `load_taxonomy_spec` YAML parsing | medium | mitigate | Parse with `yaml.safe_load` only; a Pydantic validator rejects malformed specs (missing classes for non-identity) at load time |
| T-02-02 | Tampering | `Detection`/`BoundingBox` field ranges | low | mitigate | Frozen models + `confidence` Field(ge=0,le=1) reject out-of-range values (verbatim from source) |
</threat_model>

<verification>
- `pixi run pytest tests/schemas/ -x` green.
- `pixi run lint` clean on `src/object_detection_eval/schemas/` (T20: no print).
- `grep -rE 'player-jump-shot|ball-in-basket' src/` returns nothing.
</verification>

<success_criteria>
Detection/BoundingBox/DetectionAnnotation importable and typed; merged5/raw10 taxonomies resolve to the legacy maps from YAML; identity.yaml recognised; no basketball class constants anywhere in `src/`.
</success_criteria>

<output>
Create `.planning/phases/02-harness-core/02-01-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `src/object_detection_eval/schemas/detection.py` (`BoundingBox`, `Detection`) | module | CORE-09 |
| `src/object_detection_eval/schemas/annotation.py` (`AnnotationInfo`, `DetectionAnnotation`) | module | CORE-09 |
| `src/object_detection_eval/schemas/taxonomy.py` (`TaxonomySpec`, `load_taxonomy_spec`) | module | CORE-05 |
| `benchmarks/basketball/conf/taxonomy/merged5.yaml` | config | CORE-05 |
| `benchmarks/basketball/conf/taxonomy/raw10.yaml` | config | CORE-05 |
| `benchmarks/basketball/conf/taxonomy/identity.yaml` | config | CORE-05 |
| `src/object_detection_eval/utils/boxes.py` (`pad_and_clamp_bbox`, `pixel_xyxy_to_normalized_xywh`; numpy, torch-free) | module | CORE-08 |
